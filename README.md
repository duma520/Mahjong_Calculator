# 国标麻将算番器 · 截图 v2.7.2

<img width="682" height="972" alt="image" src="https://github.com/user-attachments/assets/c586c757-7de2-43d0-88dd-e6333bc4eec5" />

<img width="682" height="972" alt="image" src="https://github.com/user-attachments/assets/fbf44722-124c-499f-99ef-d56e58c6545c" />

<img width="682" height="972" alt="image" src="https://github.com/user-attachments/assets/37193678-b15a-45b5-8e93-96312ad6b8b8" />

<img width="1072" height="994" alt="image" src="https://github.com/user-attachments/assets/e01f8bb6-b541-401f-8a80-9c9f865db746" />

<img width="1257" height="948" alt="image" src="https://github.com/user-attachments/assets/c1e6c5f2-e80f-4d3e-a5fa-66163009d51b" />

<img width="1257" height="948" alt="image" src="https://github.com/user-attachments/assets/adfca06e-4e4b-4c2e-be15-abf2c76b4cfb" />

<img width="1257" height="948" alt="image" src="https://github.com/user-attachments/assets/f4bb3cdd-0790-4e5d-97f7-697a424d2020" />


---

# 国标麻将算番器 · 完全使用手册

**从零基础到高级应用的全方位指南**

---

| 项目 | 内容 |
| --- | --- |
| **程序名** | 国标麻将算番器（中国国标麻将 / 竞技麻将算番与听牌分析） |
| **当前版本** | **2.7.2**（2026-09-25） |
| **开发时间** | 2026-09-18 |
| **作者** | 杜玛 |
| **版权** | 永久 © 杜玛 |
| **开源许可证** | MIT |
| **GitHub** | https://github.com/duma520 |
| **网站** | https://github.com/duma520 |
| **项目地址** | https://github.com/duma520/Mahjong_Calculator |
| **问题报告** | 通过 GitHub Issues 提交 |
| **邮箱** | 不提供（所有技术支持通过公开渠道进行，以便其他用户也能受益） |

> **版权声明**：© 永久 杜玛 保留所有权利。本文档内容未经书面许可不得转载或用于商业用途。
> **注意**：我们不提供私人邮箱支持，所有技术支持都通过公开渠道（GitHub Issues）进行，以便其他用户也能受益。

---

# 目录

- [第一部分 · 零基础入门](#第一部分--零基础入门)
  - [第 1 章 这是什么？能帮你做什么？](#第-1-章-这是什么能帮你做什么)
  - [第 2 章 5 分钟上手](#第-2-章-5-分钟上手)
  - [第 3 章 国标麻将是什么？](#第-3-章-国标麻将是什么)
  - [第 4 章 一分钟看懂番数是怎么算的](#第-4-章-一分钟看懂番数是怎么算的)
- [第二部分 · 界面操作详解](#第二部分--界面操作详解)
  - [第 5 章 界面总览](#第-5-章-界面总览)
  - [第 6 章 牌选择区](#第-6-章-牌选择区)
  - [第 7 章 模式按钮：立牌 / 吃 / 碰 / 明杠 / 暗杠 / 重置](#第-7-章-模式按钮立牌--吃--碰--明杠--暗杠--重置)
  - [第 8 章 选项区四行](#第-8-章-选项区四行)
  - [第 9 章 已选牌区（副露 / 立牌 / 和张 / 花牌）](#第-9-章-已选牌区副露--立牌--和张--花牌)
  - [第 10 章 听牌候选与算番结果](#第-10-章-听牌候选与算番结果)
  - [第 11 章 「不用右键也能减牌」（手机 / 触屏友好）](#第-11-章-不用右键也能减牌手机--触屏友好)
  - [第 12 章 番表页与用法页](#第-12-章-番表页与用法页)
- [第三部分 · 国标麻将算番规则详解](#第三部分--国标麻将算番规则详解)
  - [第 13 章 和牌的基本条件](#第-13-章-和牌的基本条件)
  - [第 14 章 基本牌型与特殊牌型](#第-14-章-基本牌型与特殊牌型)
  - [第 15 章 81 个番种全表](#第-15-章-81-个番种全表)
  - [第 16 章 计分五原则](#第-16-章-计分五原则)
  - [第 17 章 容易踩坑的 20 条口径](#第-17-章-容易踩坑的-20-条口径)
- [第四部分 · 给程序员的 API 完全指南](#第四部分--给程序员的-api-完全指南)
  - [第 18 章 三种调用方式总览](#第-18-章-三种调用方式总览)
  - [第 19 章 Python 直接 import](#第-19-章-python-直接-import)
  - [第 20 章 命令行 / 子进程调用](#第-20-章-命令行--子进程调用)
  - [第 21 章 Web 版 HTTP 接口](#第-21-章-web-版-http-接口)
  - [第 22 章 各语言调用示例（C#/Java/JS/Excel/Go/Rust…）](#第-22-章-各语言调用示例cjavajsexcelgorust)
  - [第 23 章 牌张编码规范](#第-23-章-牌张编码规范)
  - [第 24 章 请求 / 响应数据结构](#第-24-章-请求--响应数据结构)
- [第五部分 · 高级应用与二次开发](#第五部分--高级应用与二次开发)
  - [第 25 章 Web 版（手机 / 平板浏览器算番）](#第-25-章-web-版手机--平板浏览器算番)
  - [第 26 章 免口令访问白名单](#第-26-章-免口令访问白名单)
  - [第 27 章 设置持久化机制](#第-27-章-设置持久化机制)
  - [第 28 章 自定义规则与二次开发](#第-28-章-自定义规则与二次开发)
  - [第 29 章 打包分发](#第-29-章-打包分发)
  - [第 30 章 测试体系与校验方法](#第-30-章-测试体系与校验方法)
- [第六部分 · 版本更新简介](#第六部分--版本更新简介)
  - [简明版（给普通用户）](#简明版给普通用户)
  - [详细版（给技术用户）](#详细版给技术用户)
  - [完整迭代记录](#完整迭代记录)
- [第七部分 · 常见问题 FAQ](#第七部分--常见问题-faq)
- [第八部分 · 术语表](#第八部分--术语表)
- [附录 A · 81 番种完整清单](#附录-a--81-番种完整清单)
- [附录 B · 文件清单](#附录-b--文件清单)
- [附录 C · 快捷键与菜单一览](#附录-c--快捷键与菜单一览)
- [附录 D · 技术支持与贡献指南](#附录-d--技术支持与贡献指南)

---

# 第一部分 · 零基础入门

## 第 1 章 这是什么？能帮你做什么？

### 1.1 一句话说明

**国标麻将算番器**是一个帮你**自动计算国标麻将（竞技麻将）番数**的电脑程序。

你只要把手上和桌上的牌点进去，它就告诉你：

- **这手牌一共多少番？**
- **和了哪些番种？**（比如「清一色 24 番、平和 2 番、门前清 2 番…」）
- **够不够 8 分起和标准？**
- **我现在听哪几张牌？每张和了分别多少番？**

### 1.2 谁需要它？

| 你是… | 它能帮你… |
| --- | --- |
| **刚学国标麻将的新手** | 不用背 81 个番种，点几下就知道多少番；边玩边学 |
| **经常打竞技麻将的老手** | 快速核对算番，避免争议；听牌分析帮你选最优打法 |
| **麻将裁判 / 教练** | 有争议的牌一算就清楚，判罚有依据 |
| **写麻将程序的开发者** | 提供 `mahjong_core.py` 引擎和 JSON API，拿来即用 |
| **做麻将教学的老师** | 番表页 81 个番种带定义和「不计」说明，教学素材齐全 |
| **做麻将比赛软件的人** | 支持命令行调用、HTTP 接口、局域网多设备，容易集成 |
| **数学 / 算法爱好者** | 全量枚举 2842 万手牌验证，工程实现可学习 |

### 1.3 它**不**做什么？

为避免误解，先说清楚：

- ❌ **它不是游戏**，不能打牌，只能算番。
- ❌ **它不联网**（除非你自己开启 Web 版给手机用）。
- ❌ **它不涉及赌博**，国标麻将本身就是反对赌博的竞技运动。
- ❌ **它不保存你的牌局历史**（每次打开都是干净的，只有设置会记住）。
- ❌ **它不能替代裁判**，最终判罚以《中国麻将竞赛规则》为准。

### 1.4 支持哪些系统？

- **桌面版主程序**：Windows（推荐 Windows 10/11 64 位），用 PySide6 (Qt6) 写界面。
  理论上 Linux / macOS 装了 Python 和 PySide6 也能跑，但主要在 Windows 上测试。
- **Web 版**：任何有浏览器的设备（手机、平板、电脑），只要能连上运行服务的电脑就行。
- **引擎 `mahjong_core.py`**：纯 Python 3.8+，无第三方依赖，任何平台都能跑。

---

## 第 2 章 5 分钟上手

### 2.1 最快路径（直接跑源码）

**前提**：装好 Python 3.8 以上、装好 PySide6。

```bash
# 1. 下载项目
git clone https://github.com/duma520/Mahjong_Calculator.git
cd Mahjong_Calculator

# 2. 装依赖（只有界面需要，引擎本身零依赖）
pip install PySide6

# 3. 运行
python mahjong_gui.py
```

或者直接双击项目里的 `1.bat`（它是 `python mahjong_gui.py` 的快捷方式）。

### 2.2 用打包好的 exe（如果有）

如果你的朋友或作者给了你 `国标麻将算番器.exe`，直接双击即可，**不需要装 Python**。

> ⚠️ 注意：exe 必须和 `麻将图\` 文件夹、`国标麻将标准规则.json` 放在一起（打包版通常已经放好）。

### 2.3 第一次算番：跟着做

假设你手上有这么一手牌（清一色 + 平和 + 门前清，听 4 万 / 7 万）：

```
一万 二万 三 万 | 四万 五 万 六 万 | 七万 八 万 九 万 | 三 万 四 万 五 万 | 六 万 六 万
```

**操作步骤：**

1. **打开程序** → 看到「牌选择区」四行牌（索 / 筒 / 万 / 字牌）。
2. **点「万」那一行**：
   - 点 1 万一次 → 角标显示 1
   - 点 2 万一次 → 角标显示 1
   - 点 3 万两次（因为有两张 3 万）→ 角标显示 2
   - …一直点到你点满 **13 张**立牌。
3. **看下面「听牌候选」区**：程序自动列出可以和的牌，每张牌上方有番数。
4. **点候选牌里的「4 万」**（或点牌区的 4 万）→ 它成为「和张」。
5. **看右下角「算番结果」**：显示「共 N 番」+ 番种列表。

就这样，你完成了一次完整的算番。

### 2.4 这手牌会算出什么？

如果就是上面那手（123 456 789 345 万 + 66 万将，和 4 万或 7 万），结果大概是：

- **清一色** 24 番
- **清龙** 16 番（1~9 万相连）
- **平和** 2 番
- **门前清** 2 番
- **无字** 1 番
- 其他可能的番种（如连六、一般高、单钓将等，看具体分解）

**总计约 45 番以上**，远超过 8 分起和标准。

---

## 第 3 章 国标麻将是什么？

### 3.1 定义

**国标麻将**，全称**中国国标麻将（竞技麻将）**，是指依据 **1998 年 7 月国家体育总局审定**、**人民体育出版社出版**的《**中国麻将竞赛规则（试行）**》进行的麻将比赛。

它与你在家里、茶馆里打的地方麻将最大区别是：

| 项目 | 地方麻将（休闲） | 国标麻将（竞技） |
| --- | --- | --- |
| 番种数量 | 各地不同，少的十几种 | **81 种**，全国统一 |
| 起和分 | 无 / 很低 | **8 分**（花牌不算） |
| 天和/地和/人和 | 有 | **没有**（《问答》Q12：机遇性太大） |
| 连庄制 | 有 | **没有**（每人一庄） |
| 计分 | 各地不同 | 底分 + 基本分 + 罚分，公式统一 |
| 赌博 | 常见 | **明令禁止**，提倡健康娱乐 |

### 3.2 历史

- **起源**：麻将起源于中国，历史可追溯到三四千年前，清朝中定型。
- **1997**：国家体委社会体育指导中心办试验邀请赛，起草规则。
- **1998 年 7 月**：国家体育总局审定。
- **1998 年 9 月**：人民体育出版社出版《中国麻将竞赛规则（试行）》。
- **2006**：《〈中国麻将竞赛规则（试行）〉问答》（杜维忠著）出版，对规则逐条解释。

本程序**严格按这两本书实现**，规则数据都在 `国标麻将标准规则.json` 里。

### 3.3 牌具

一副国标麻将共 **144 张**，分三类：

| 类别 | 明细 | 张数 |
| --- | --- | --- |
| **序数牌** | 万子 1~9 各 4 张（36）、筒子 1~9 各 4 张（36）、索子 1~9 各 4 张（36） | 108 |
| **字牌** | 风牌 东南西北 各 4 张（16）、箭牌 中发白 各 4 张（12） | 28 |
| **花牌** | 春 夏 秋 冬 梅 兰 竹 菊 各 1 张 | 8 |

**花牌特殊**：每张 1 分，但**不计入 8 分起和分**，也不占 14 张手牌。

### 3.4 和牌条件

必须同时满足：

1. **牌型符合规定**（4 面子 + 1 将，或特殊牌型如七对、十三幺、全不靠）。
2. **番种分之和 ≥ 8 分**（花牌分不算）。
3. **和牌方式合规**（自摸 / 点和 / 抢杠和等）。

不符合条件却宣布和牌，就是**诈和**，要受罚。

---

## 第 4 章 一分钟看懂番数是怎么算的

### 4.1 番种 = 有分值的牌张组合 / 和牌方式

比如：

- 你的牌**全是同一种花色**（比如全是万）→ **清一色** = 24 番
- 你的牌有 **1~9 相连**的顺子（123 456 789）→ **清龙** = 16 番
- 你的牌**全是顺子 + 数牌作将**→ **平和** = 2 番
- 你**没有吃碰杠，点和**→ **门前清** = 2 番

一手牌可以**同时命中多个番种，分数累加**（但不能重复计，见「计分五原则」）。

### 4.2 举例：一个 8 番的最低和牌

```
一万 二万 三 万 | 四万 五 万 六 万 | 七万 八 万 九 万 | 三条 四条 五 条 | 东 东
```

- 清龙 16 番
- 平和 2 番
- 门前清 2 番
- 无字 1 番（没有风箭牌）
- 喜相逢 1 番（万 123 + 条 345 数字相同但不同花色？不，是 123 和 345 不算喜相逢…）

具体以程序结果为准。**点几下就知道**，不用心算。

### 4.3 花牌：白送的分

- 每张花牌 **1 分**，但**不算在 8 分起和里**。
- 比如你本来 8 番正好起和，摸到 2 张花牌，最终记 **10 番**。
- 花牌**不占 14 张手牌**，补花后另抓。

### 4.4 起和分 8 分

**番种分之和 ≥ 8** 才能和牌。比如：

- 只有「碰碰和 6 番」→ 不够 8 分，**不能和**。
- 「碰碰和 6 番 + 断幺 2 番」→ 正好 8 分，**能和**。
- 花牌分**不算**这 8 分（但算进最终总分）。

---

# 第二部分 · 界面操作详解

## 第 5 章 界面总览

### 5.1 整体布局

程序主窗口自上而下分：

```
┌────────────────────────────────────────────────────┐
│ 菜单栏：文件(F)  工具(T)  帮助(H)                  │
├────────────────────────────────────────────────────┤
│                                                    │
│   算番页 / 番表页 / 用法页（三选一，通过底部切换） │
│                                                    │
├────────────────────────────────────────────────────┤
│ 底部导航：[🧮 算番] [📖 番表] [❓ 用法]           │
└────────────────────────────────────────────────────┘
```

**算番页**（默认）自上而下：

1. **牌选择区**（4 行 × 9 列）
2. **模式按钮行**（立牌 / 吃 / 碰 / 明杠 / 暗杠 / 重置）
3. **选项区**（固定四行：复选框 / 圈风 / 风位 / 花牌）
4. **已选牌区**（副露 / 立牌 / 和张 / 花牌 + 提示）
5. **听牌候选区**
6. **算番结果区**

### 5.2 窗口尺寸

- **默认**：680 × 940 像素
- **最小**：600 × 560 像素
- **可拉伸**：拉伸后的尺寸自动保存，下次打开还是这个大小

### 5.3 底部导航

| 按钮 | 页面 | 内容 |
| --- | --- | --- |
| 🧮 算番 | 算番页 | 主功能：选牌 + 算番 |
| 📖 番表 | 番表页 | 81 个番种按分值分组，带定义和「不计」，顶部有搜索框 |
| ❓ 用法 | 用法页 | 操作说明 + 规则口径说明 |

---

## 第 6 章 牌选择区

### 6.1 布局

四行牌，每行从左到右：

| 行 | 内容 |
| --- | --- |
| 第 1 行 | **索** T1 T2 T3 T4 T5 T6 T7 T8 T9 |
| 第 2 行 | **筒** B1 B2 B3 B4 B5 B6 B7 B8 B9 |
| 第 3 行 | **万** W1 W2 W3 W4 W5 W6 W7 W8 W9 |
| 第 4 行 | **字牌** F1(东) F2(南) F3(西) F4(北) J1(中) J2(发) J3(白) |

### 6.2 基本操作

| 操作 | 效果 |
| --- | --- |
| **左键点一下** | 加一张（最多 4 张） |
| **右键点一下** | 减一张 |
| **点牌上红色数字角标** | 减一张（v2.7.2 新增，手机友好） |
| **鼠标悬停** | 显示牌名提示（如「三万」） |

### 6.3 红色角标

每张牌**右上角**有个**红色圆形数字**，表示**这张牌已经选了几张**：

- 未选：不显示角标
- 选了 1 张：显示「1」
- 选了 2 张：显示「2」
- …最多「4」

**v2.7.2 起**：**点这个红色数字 = 减一张**（不用右键，手机/触屏也能操作）。

### 6.4 选中状态的视觉

- 已选的牌：**蓝色边框 + 浅蓝背景**
- 未选的牌：**透明边框 + 浅灰背景**
- 悬停：**淡蓝背景 + 淡蓝边框**

### 6.5 什么时候不能再加？

- 这张牌**已经有 4 张**（物理上限）→ 状态栏提示「这种牌最多 4 张」
- **立牌已经满 13 张**（有副露时更少）→ 再点会**自动记为「和张」**

---

## 第 7 章 模式按钮：立牌 / 吃 / 碰 / 明杠 / 暗杠 / 重置

### 7.1 五个模式（互斥单选）

| 按钮 | 作用 | 操作方式 |
| --- | --- | --- |
| **立牌**（默认） | 点出的牌算手上暗牌 | 点一下加一张 |
| **吃** | 组一个顺子（只能吃上家） | **依次点 3 张同花色相连的牌** |
| **碰** | 组一个刻子（3 张相同） | **点一次牌即成组**（自动补 3 张） |
| **明杠** | 杠别人打出的牌（4 张相同） | **点一次牌即成组**（自动补 4 张） |
| **暗杠** | 自己手里 4 张相同，暗中开杠 | **点一次牌即成组**（自动补 4 张暗杠） |

### 7.2 吃（chi）的用法

**吃**只能吃上家打出的牌，组一个同花色的**顺子**（如 123、456、789）。

操作步骤：

1. 点「**吃**」按钮。
2. **依次点 3 张**要吃的牌，比如：
   - 点一万
   - 点二万
   - 点三万
3. 三张都点完后，自动成立一个吃副露。

**如果点的 3 张不合法**（不同花色、不相连、有重复），程序会提示「吃需要 3 张相连的同花色牌，请重新选择」。

**撤销吃**：
- 正在吃但还没点满 3 张时，点已选的牌可以取消当前这张
- 已经成立的吃，在「已选牌 → 副露」里点一下**撤销整组**（v2.7.2）

### 7.3 碰（peng）的用法

**碰**可以碰**任何一家**打出的牌，组一个**刻子**（3 张相同）。

操作步骤：

1. 点「**碰**」按钮。
2. 点一次要碰的牌（如 5 万）→ **自动补 3 张 5 万**成组。

**限制**：
- 这种牌加起来不能超过 4 张（含手里其他地方的）
- 副露最多 4 组

### 7.4 明杠（minggang）

**明杠**有两种情况：
- 别人打出一张，与自己手里的**暗刻**相同 → 报杠
- 自己抓进一张与**已经碰出的明刻**相同 → 报杠

操作步骤：

1. 点「**明杠**」按钮。
2. 点一次要杠的牌 → **自动补 4 张**成组。

**显示**：明杠在已选牌区**4 张全部正面**（因为已经亮明）。

### 7.5 暗杠（angang）

**暗杠**是自己手里抓到 **4 张相同**的牌，暗中开杠。

操作步骤：

1. 点「**暗杠**」按钮。
2. 点一次要杠的牌 → **自动补 4 张**，标记为**暗杠**。

**显示**：暗杠在已选牌区显示为 **面·背·背·面**（中间两张用牌背图 `empty.png`），因为暗杠是扣着的，其他家看不到中间两张。

**为什么暗杠重要**：
- 暗杠**不影响门前清**（虽然占一个面子）
- 暗杠**既算暗刻**（用于三暗刻/四暗刻判定），**又单独计 2 分**
- 四暗杠可算**四暗刻 64 番**

### 7.6 重置（橙色按钮）

**点一下「重置」= 全部回到初始状态**（v2.4.4 起）：

- 清空**手牌**（立牌）
- 清空**副露**
- 清空**和张**
- 清空**待选**（正在吃但没完成的）
- 模式回到「**立牌**」
- **花牌全取消**
- **四个复选框**（自摸、和绝张、抢杠和/杠上开花、海底捞月/妙手回春）**全不勾**
- **圈风**回「东风圈」
- **风位**回「东风位」
- **立即存进设置**（下次打开也是干净的）

### 7.7 副露的显示规则

| 副露类型 | 显示方式 |
| --- | --- |
| **吃** | 3 张正面顺子（如 1 万 2 万 3 万） |
| **碰** | 3 张正面刻子（如 5 万 5 万 5 万） |
| **明杠** | 4 张正面 |
| **暗杠** | 面·背·背·面（中间两张是牌背） |
| 不同副露之间 | 10px 空隙 |

---

## 第 8 章 选项区四行

选项区**固定四行**，每行有特定功能。

### 8.1 第 1 行：四个复选框

| 复选框 | 勾上后 | 说明 |
| --- | --- | --- |
| **自摸** | 表示自己抓牌成和 | +1 番（且和后面两个复选框联动） |
| **和绝张** | 和的这张牌，牌池里只剩最后一张 | +4 番 |
| **抢杠和** / **杠上开花** | 未勾自摸时 = 抢杠和；勾了自摸时 = 杠上开花 | 8 番 |
| **海底捞月** / **妙手回春** | 未勾自摸时 = 海底捞月；勾了自摸时 = 妙手回春 | 8 番 |

**注意「自摸」的联动**：

- 勾上自摸 → 第 3、4 个复选框**文案变成「杠上开花」「妙手回春」**
- 取消自摸 → 文案变回「抢杠和」「海底捞月」
- **切换时自动清空旧勾选**（防止误算）

### 8.2 第 2 行：圈风（单选）

| 选项 | 说明 |
| --- | --- |
| **东风圈** | 第 1 圈 |
| **南风圈** | 第 2 圈 |
| **西风圈** | 第 3 圈 |
| **北风圈** | 第 4 圈 |

**圈风影响**：如果你有**与圈风相同的风刻**，加**圈风刻 2 番**。

### 8.3 第 3 行：风位（单选）

| 选项 | 说明 |
| --- | --- |
| **东风位** | 你是庄家 |
| **南风位** | 你是庄家下家 |
| **西风位** | 你是庄家对家 |
| **北风位** | 你是庄家上家 |

**风位影响**：如果你有**与门风相同的风刻**，加**门风刻 2 番**。

**圈风 + 风位可以叠加**：比如你是东风圈 + 东风位，手上有东东东，**圈风刻 2 + 门风刻 2 = 4 番**。

### 8.4 第 4 行：花牌

8 张小图：**春 夏 秋 冬 梅 兰 竹 菊**

- **点一下**：选中（蓝色边框）
- **再点**：取消
- 右侧显示**已选张数**「N 张」

**花牌特点**：
- 每张 **1 分**，加在**最终总分**里
- **不计入 8 分起和分**
- 不占 14 张手牌
- 花牌分**不影响算番逻辑**（只是总分 +N）

---

## 第 9 章 已选牌区（副露 / 立牌 / 和张 / 花牌）

### 9.1 副露行

显示所有**吃、碰、明杠、暗杠**。

**显示顺序**：按照你添加的顺序，从左到右，副露之间留空隙。

**暗杠显示**：面·背·背·面

**点击操作**（v2.7.2）：
- **点副露里的任何一张牌** → **撤销整组副露**
- 包括**点暗杠的牌背**也能撤销

### 9.2 立牌行

显示**手上暗牌**（14 张里扣除副露的面子 + 和张）。

**点击操作**（v2.7.2）：
- **点一张** → **减一张**（不用右键）

### 9.3 和张行

显示你**指定的和张**（那张和牌的牌）。

**自动指定**：
- 立牌选满 13 张后，程序自动列出听牌候选
- 你点候选或点牌区的某张牌 → 成为「和张」

**点击操作**：
- **点和张** → **取消和张**

### 9.4 花牌行

显示你选的花牌（春夏秋冬梅兰竹菊）。

**点击操作**：
- **点一张花牌** → **取消那张花牌**

### 9.5 提示行

底部有一行蓝色提示，会随状态变化：

- 「还需选择 X 张牌（当前 Y / Z）」
- 「已选满 13 张，下面列出可以和的牌」
- 「和张：三万（点下方候选或牌区的牌可换）」
- 「牌数超出，请点已选的牌减一张或重置」

---

## 第 10 章 听牌候选与算番结果

### 10.1 听牌候选

**触发条件**：立牌恰好**选满 13 张**（有副露时 = 13 − 3 × 副露数）。

**显示**：

```
┌──────┐ ┌──────┐ ┌──────┐
│ 45番 │ │ 48番 │ │ 32番 │
│ 4万  │ │ 7万  │ │ 1万  │
└──────┘ └──────┘ └──────┘
```

- 每个候选牌**上方是总番数**（大字）
- 达到 8 分起和：**蓝色**
- 未达 8 分：**红色**
- **点候选牌** → 设为和张

### 10.2 算番结果

算番结果区显示：

- **总番数**：大字（如「共 45 番」）
- **牌型**：如「基本牌型」或「组合龙」
- **番种列表**：每行「N番 番种名」，按分值降序
- **提示信息**：起和分提示（如「已达起和标准（8 分）」或「番种合计 6 分，未达 8 分起和标准」）

### 10.3 不同状态下的显示

| 状态 | 显示 |
| --- | --- |
| 还没满 14 张 | 「还需在牌选择区选 X 张牌」 |
| 满了 14 张但没指定和张 | 自动选一张能和的牌作为和张 |
| 满了但牌型不和 | 「牌型不能和牌（请检查已选的牌）」（红色） |
| 张数不足 14 张 | 「手牌张数不足，无法算番」 |

---

## 第 11 章 「不用右键也能减牌」（手机 / 触屏友好）

### 11.1 背景

**传统鼠标操作**：左键加、右键减。

**问题**：**手机 / 平板 / 触屏没有右键**，减牌很麻烦。

### 11.2 三个减牌入口（v2.7.2 起）

| 方式 | 操作 | 适用 |
| --- | --- | --- |
| **右键** | 右键点牌 | 鼠标用户（习惯不变） |
| **点红色角标** | 点牌右上角红色数字 | **手机 / 触屏 / 鼠标都行** |
| **点已选牌** | 点「已选牌」区里的牌 | **手机 / 触屏 / 鼠标都行** |

### 11.3 「点已选牌」的详细效果

| 在哪个行点 | 效果 |
| --- | --- |
| **副露** | 撤销**那一组副露**（原来只能整个重置） |
| **立牌** | 减**一张** |
| **和张** | 取消和张 |
| **花牌** | 取消**那张**花牌 |

### 11.4 视觉反馈

- **悬停**：已选牌区里的可点牌会**高亮**（浅粉底 + 红边框）
- **光标**：变成**手型**（表示可点）
- **提示**：悬停显示「点一下取消这张」

### 11.5 举例

**场景**：你在手机上选了一手牌，发现多选了一张「五万」。

**解决**：

- **方法 1**：点五万牌的**红色角标** → 减一张
- **方法 2**：在下面「立牌」行找到五万 → **点它** → 减一张

---

## 第 12 章 番表页与用法页

### 12.1 番表页

**菜单**：底部「📖 番表」或帮助菜单 →「番种表(T)」。

**内容**：81 个番种按**分值分组**（88 / 64 / 48 / … / 2 / 1 分）：

```
88 分
  大四喜  由4副风刻（杠）组成的和牌。（不计：圈风刻、门风刻、三风刻、碰碰和、幺九刻）
  大三元  和牌中，有中、发、白3副刻子。（不计：箭刻）
  绿一色  由23468条及发字中的任何牌组成的顺子、刻子、将的和牌。（不计：混一色）
  ...
```

**搜索框**：输入番种名（如「清一色」「龙」「杠」）→ 实时筛选。

### 12.2 用法页

**菜单**：底部「❓ 用法」或帮助菜单 →「用法说明(H)」。

**内容**：

- 一、怎么算一手牌
- 二、特殊和牌方式
- 三、界面约定
- 四、算番口径
- 五、给程序用的算番 API
- 六、Web 版（手机 / 平板浏览器算番）

---

# 第三部分 · 国标麻将算番规则详解

## 第 13 章 和牌的基本条件

### 13.1 三个前提（缺一不可）

依据《中国麻将竞赛规则》第八条：

1. **必须符合规则规定的牌型**
   - 基本牌型：4 面子 + 1 将
   - 特殊牌型：七对、十三幺、全不靠

2. **番种分值之和至少为 8 分**
   - 花牌分**不算**在这 8 分里

3. **符合规定的和牌方式**
   - 自摸和
   - 点和
   - 抢杠和

### 13.2 基本牌型

《规则》第八条规定了 5 种基本牌型：

| 编号 | 牌型 | 说明 |
| --- | --- | --- |
| 1 | `11、123、123、123、123` | 4 顺子 + 将 |
| 2 | `11、123、123、123、111（1111）` | 3 顺子 + 1 刻子（或杠）+ 将 |
| 3 | `11、123、123、111、111` | 2 顺子 + 2 刻子 + 将 |
| 4 | `11、123、111、111、111` | 1 顺子 + 3 刻子 + 将 |
| 5 | `11、111、111、111、111` | 4 刻子 + 将（碰碰和） |

**符号说明**：
- `1` = 单张
- `11` = 将（对子）
- `111` = 刻子
- `1111` = 杠
- `123` = 顺子

### 13.3 特殊牌型

| 牌型 | 说明 |
| --- | --- |
| **七对** | `11 11 11 11 11 11 11`，7 个对子 |
| **十三幺** | 3 种序数牌的 1、9 + 7 种字牌 + 其中一对作将 |
| **全不靠** | 单张 3 种花色的 147/258/369 不能错位 + 字牌 |
| **七星不靠** | 全不靠的加强版：必须有 7 种字牌 |
| **连七对** | 一种花色序数相连的 7 个对子 |

### 13.4 和牌方式

| 方式 | 说明 | 计番 |
| --- | --- | --- |
| **自摸和** | 自己抓进成和的牌 | 加计「自摸」1 番 |
| **点和** | 和别人打出的牌 | 无额外番种 |
| **杠上开花** | 开杠后抓进的牌成和 | +8 番（不计自摸） |
| **抢杠和** | 和别人自抓开明杠的牌 | +8 番 |
| **妙手回春** | 自摸牌墙最后一张和牌 | +8 番（不计自摸） |
| **海底捞月** | 和打出的最后一张牌 | +8 番 |

---

## 第 14 章 基本牌型与特殊牌型

### 14.1 标准型分解

程序的引擎会**枚举所有可能的分解方式**，对每种分解算一遍番，取**番数最高**的那种。

**举例**：手牌 `111 234 567 789 99` 万

**可能分解方式**：
- 方式 A：`123 + 456 + 789 + 11(1) + 99` — 但 111 拆成 11+1 和 1 不符合
- 方式 B：`111 + 234 + 567 + 789 + 99` — 标准分解
- 方式 C：`11 + 123 + 456 + 789 + ?` — 3 张 1 万里 2 张作将，剩 1 张…

程序会算出每种合法分解的番数，取最高。

### 14.2 七对

**条件**：7 个对子，**门前无副露**（14 张）。

**关键细节**（v2.2.1 修正）：

- **4 张相同的牌算作两对**（国标口径）
  - ✅ `一一一一 二二 三三 四四 五五 六六` = 七对（1 个四张 + 5 对）
  - ✅ `一一一一 二二二二 三三 四四 五五` = 七对（2 个四张 + 3 对）
  - ✅ `一一一一 二二二二 三三三三 四四` = 七对（3 个四张 + 1 对）
- **已开杠的 4 张不能当两对**（杠属于副露，七对必须门前无副露）

**不计番种**：不求人、门前清、单钓将。

**可另加计**（《问答》Q71）：
- 字一色、清一色、混一色、清幺九、混幺九、全大、全中、全小、大于五、小于五、推不倒、五门齐
- **四归一是必然并存，不计**

### 14.3 连七对

**条件**：**同一花色 + 序数相连**的 7 个对子。

**例**：`一一 二二 三三 四四 五五 六六 七七`（全万）

**反例**：
- `六六 七七 八八 九九 一一 二二 三三`（万 6~9 + 万 1~3）→ 跨了序数，**只算七对 24 番**，不是连七对
- `筒6筒7筒8筒9 + 索1索2索3` → 跨花色，**只算七对**

**不计**：七对、清一色、不求人、门前清、单钓将、一般高、连六、无字。

### 14.4 十三幺

**条件**：`1万 9万 1索 9索 1筒 9筒 东 南 西 北 中 发 白` 各一张 + 其中任意一张作将。

**共 14 张**，必须是**门前**（无副露）。

**不计**：五门齐、不求人、单钓将。

### 14.5 全不靠 / 七星不靠

**全不靠**：14 张牌**互不相同**，三门花色各落在 **147 / 258 / 369 不同的列**上，加任意字牌。

**七星不靠**：全不靠 + **必须有 7 种字牌**（东南西北中发白）。

**花色"列"的含义**：
- 147 是一列（1、4、7 属于同一列）
- 258 是一列
- 369 是一列
- 三门花色**分别对应不同的列**（不能错位）

**允许**：万 147、筒 258、索 369（或万 258、筒 369、索 147 等排列）
**不允许**：万 147、筒 147、索 147（同一列）

### 14.6 组合龙

**条件**：三门花色分别组成 147 / 258 / 369 三组，**共 9 张**。

**加组合龙 12 番**，再加上其他面子 + 将。

**示例**：
```
万 1 4 7 | 筒 2 5 8 | 索 3 6 9 | 万 5 6 7 | 东 东
```

---

## 第 15 章 81 个番种全表

（详见附录 A，此处仅列分值级与数量）

| 分值 | 数量 | 代表番种 |
| --- | --- | --- |
| 88 | 7 | 大四喜、大三元、绿一色、九莲宝灯、四杠、连七对、十三幺 |
| 64 | 6 | 清幺九、小四喜、小三元、字一色、四暗刻、一色双龙会 |
| 48 | 2 | 一色四同顺、一色四节高 |
| 32 | 3 | 一色四步高、三杠、混幺九 |
| 24 | 9 | 七对、七星不靠、全双刻、清一色、一色三同顺、一色三节高、全大、全中、全小 |
| 16 | 6 | 清龙、三色双龙会、一色三步高、全带五、三同刻、三暗刻 |
| 12 | 5 | 全不靠、组合龙、大于五、小于五、三风刻 |
| 8 | 9 | 花龙、推不倒、三色三同顺、三色三节高、无番和、妙手回春、海底捞月、杠上开花、抢杠和 |
| 6 | 7 | 碰碰和、混一色、三色三步高、五门齐、全求人、双暗杠、双箭刻 |
| 4 | 4 | 全带幺、不求人、双明杠、和绝张 |
| 2 | 10 | 箭刻、圈风刻、门风刻、门前清、平和、四归一、双同刻、双暗刻、暗杠、断幺 |
| 1 | 12 | 一般高、喜相逢、连六、老少副、幺九刻、明杠、缺一门、无字、边张、坎张、单钓将、自摸 |
| 花牌 | 1 | 每张 1 分，不计起和分 |

**总计 81 个番种 + 花牌。**

---

## 第 16 章 计分五原则

依据《规则》第十条：

### 16.1 不重复原则

> 当某个番种，由于组牌的条件所决定，在其成立的同时，必然并存着其他番种，则其他番种不重复计分。

**例**：**大四喜**必然包含 3 风刻、圈风刻、门风刻、幺九刻、碰碰和 → 这些不另计。

### 16.2 不拆移原则

> 确定一个番种后，不能将其自身再拆开互相组成新的番种计分。

**例**：**清龙**（123 456 789）是主体番种，不能拆开再算「连六」「老少副」。

### 16.3 不得相同的原则

> 凡已组合过某一番种的牌，不能再同其他一副牌组成相同的番种计分。

### 16.4 就高不就低原则

> 有两副以上的牌，有可能组成两个以上的番种，而只能选其中一种计分时，可选择分高的番种计分。

**程序实现**：`score()` 会枚举所有分解方式，取**总分最高**的那种。

### 16.5 套算一次原则

> 如有尚未组合过的一副牌，只可同已组合过的相应的一副牌套算一次。

**例**：一般高（同花色同起始的顺子）如果有 3 副相同，计 2 次（n−1），不是 3 次。

---

## 第 17 章 容易踩坑的 20 条口径

依据《问答》（杜维忠著）与 1998 首届健康麻将邀请赛领队会规定，以下口径容易搞错：

### 17.1 边张 / 坎张 / 单钓将

> **听两种及以上牌时，不算边张 / 坎张 / 单钓将。**

- `1233 和 3`：算**边张**
- `7789 和 7`：算**边张**
- `4556 和 5`：算**坎张**
- `12345 和 3`：**不算**边张（听两种）
- `45567 和 6`：**不算**坎张
- 听 1、4 万和 4 万：**不算**单钓将

### 17.2 自摸 vs 不求人

> **已计不求人，不再计自摸分。**

- **不求人** = 4 副牌 + 将，**没有吃碰明杠**，**自摸和**
- 一旦成立不求人，自摸 1 番不再计

### 17.3 杠的计分（《问答》第 41 条）

| 情况 | 番数 |
| --- | --- |
| 1 个明杠 | 1 |
| 1 个暗杠 | 2 |
| 2 个明杠 | 双明杠 4 |
| **1 明 + 1 暗** | **双明杠 4 + 暗杠 2 = 6** |
| 2 个暗杠 | 双暗杠 6 |
| 3 个杠 | 三杠 32（暗杠另加计） |
| 4 个杠 | 四杠 88（四暗杠另加四暗刻 64） |

### 17.4 七对含四张

> **4 张相同的牌算两对**（国标口径，v2.2.1 修正）。

但：
- **已开杠的 4 张不能当两对**（杠是副露）
- **四归一**在七对里是必然并存，**不计**

### 17.5 圈风刻 + 门风刻

> 东风圈 + 东风位，手上有东东东 → **圈风刻 2 + 门风刻 2 = 4 番**。

**大四喜**中不计圈风刻、门风刻。

### 17.6 箭刻 vs 幺九刻

> **箭刻（中发白刻子）计 2 分，不计幺九刻分。**

**双箭刻 6 分**，也不计幺九刻。

### 17.7 清幺九的「不计」

**《规则》原文**：不计碰碰和、同刻、无字。

**注意**：部分资料写作「不计碰碰和、混幺九、幺九刻」，**实现以规则原文为准**。

### 17.8 花牌

- 每张 **1 分**
- **不计入 8 分起和分**
- 花牌补花成和**计自摸 1 分**，**不计杠上开花**
- 抓到最后一张是花牌且无牌可补 → **自然荒牌**

### 17.9 九莲宝灯

**必须 13 张全部自摸抓进**，听牌前**不能吃、不能碰**。

**不计**：清一色、不求人、门前清、幺九刻。

### 17.10 绿一色

**由 2 3 4 6 8 索 + 发字组成**。

**发字不是必备**：
- 没有发字，可**另加计清一色**
- 有发字（发发发），可**另加计箭刻**

### 17.11 海底捞月

> **和打出的最后一张牌**（不一定是牌墙抓的最后一张）。

### 17.12 妙手回春

> **自摸牌墙最后一张和牌**，不计自摸。

### 17.13 杠上开花

> **开杠抓进的牌成和**（不包括补花）。
> 其中的杠**另加计分**（《问答》Q98）。

### 17.14 抢杠和

> **和他人自抓开明杠的牌**，不计和绝张。
> 
> **只对「他人自抓开明杠」成立**。碰后打第 4 张，不构成抢杠和，只是点和。

### 17.15 全求人

> **全靠吃碰（或明杠）+ 单钓别人打出的牌和**。
> 
> 手中**明杠**的牌组如果基础是碰出的明刻，**仍算全求人**（《问答》Q104）。

### 17.16 一色四同顺

> 由 12 张 4 副相同顺子组成，**另需一个对子作将**。
> 
> **必然并存**：3 个四归一、一色三节高 → **不另计**。
> 
> **配将产生的平和** → **另加计**（《问答》Q66）。

### 17.17 一色双龙会

> 一种花色的 **两个老少副**，5 为将牌。
> 
> 不计：平和、七对、清一色、老少副、一般高。

### 17.18 三色双龙会

> 2 种花色各 123 789，第三种数牌 5 作将。
> 
> 不计：喜相逢、老少副、无字、平和。

### 17.19 平和

> **4 副顺子 + 数牌作将** = 平和。
> 
> **边张、坎张、单钓将不影响平和**（1998 规则改革）。

### 17.20 无番和

> **算不出任何番种分** → 无番和 8 分。
> 
> 正好达到起和分。

---

# 第四部分 · 给程序员的 API 完全指南

## 第 18 章 三种调用方式总览

**国标麻将算番器提供三种调用方式**，你可以根据场景选择：

| 方式 | 适用场景 | 是否需要网络 | 是否需要启动服务 |
| --- | --- | --- | --- |
| **① Python import** | Python 程序内部调用 | ❌ 不需要 | ❌ 不需要 |
| **② 命令行 / 子进程** | 任何语言（C#/Java/Node/Excel…） | ❌ 不需要 | ❌ 不需要 |
| **③ Web 版 HTTP 接口** | 手机/平板浏览器、跨设备、网页 | ✅ 需要 | ✅ 需要 |

**关键说明（v2.6.0 起）**：

> **给其它程序调用的"API"是 `mahjong_core`，不是 HTTP 服务**。
>
> ① 和 ② 都**不需要 HTTP、不需要端口、不需要先启动谁**。只有 ③ 才是 HTTP。

---

## 第 19 章 Python 直接 import

### 19.1 最简用法

```python
from mahjong_core import score_hand

# 一行算番
result = score_hand("11123456789999m", win="9m")
print(result["total"], "番")
print(result["fans"])
```

**输出**：

```python
106 番
[{'name': '九莲宝灯', 'fan': 88}, {'name': '清龙', 'fan': 16}, {'name': '四归一', 'fan': 2}]
```

### 19.2 完整用法

```python
from mahjong_core import MahjongFanCalculator, Meld, Options

# 创建算番器
calc = MahjongFanCalculator()

# 牌张编码：用 code_of / tile_of / name_of 互转
from mahjong_core import code_of, tile_of, name_of

print(tile_of("W3"))   # 20（三万）
print(code_of(20))     # "W3"
print(name_of(20))     # "三万"

# 直接算番
# concealed 是「含和张的整手牌」，14 - 3*副露数 张
concealed = [tile_of(c) for c in ["W1","W2","W3","W4","W5","W6",
                                    "W7","W8","W9","W1","W2","W3",
                                    "W5","W5"]]
win_tile = tile_of("W5")
opts = Options(tsumo=True, round_wind="东", seat_wind="南", flowers=2)
score = calc.score([], concealed, win_tile, opts)

print("总番:", score.total)
print("起和分:", score.base)
print("花牌:", score.flowers)
print("达标:", score.ok)
print("牌型:", score.pattern)
for name, value in score.fans:
    print(f"  {value}番  {name}")
```

### 19.3 带副露

```python
from mahjong_core import MahjongFanCalculator, Meld, Options, tile_of

calc = MahjongFanCalculator()

# 副露：一个吃，一个暗杠
melds = [
    Meld("chi", (tile_of("W1"), tile_of("W2"), tile_of("W3")), False),
    Meld("kong", (tile_of("T5"),) * 4, True),  # 暗杠
]

# concealed 长度 = 14 - 3*2 = 8 张
concealed = [tile_of(c) for c in ["W4","W5","W6","W7","W8","W9",
                                    "T7","T7"]]
win_tile = tile_of("T7")

s = calc.score(melds, concealed, win_tile, Options())
print(s.text())
```

### 19.4 听牌分析

```python
from mahjong_core import MahjongFanCalculator, Meld, Options, code_of, name_of, tile_of

calc = MahjongFanCalculator()

# 听牌分析：给 13 - 3*副露数 张
concealed = [tile_of(c) for c in ["W1","W2","W3","W4","W5","W6",
                                    "W7","W8","W9","W1","W2","W3","W5"]]

for tid, score in calc.winning_tiles([], concealed, Options()):
    print(f"{code_of(tid)} ({name_of(tid)})  {score.total}番  {score.text()}")
```

### 19.5 旧接口（v1.x 兼容）

```python
from mahjong_core import MahjongFanCalculator

calc = MahjongFanCalculator()

# 牌代码列表
tiles = ["W1","W2","W3","W4","W5","W6","W7","W8","W9","W1","W2","W3","W5","W5"]

# 算番（返回 {番种: 番数} 字典）
fans = calc.calculate_fan(tiles, {"tsumo": True})
print(fans)

# 是否和牌
print(calc.is_winning_hand(tiles))   # True

# 听牌候选
for code, fans in calc.get_possible_winning_tiles(tiles[:13]):
    print(code, fans)
```

### 19.6 数据类速查

| 类 | 字段 | 说明 |
| --- | --- | --- |
| `Meld` | `kind` | `'chi'` / `'pong'` / `'kong'` |
| | `tiles` | 牌 id 元组 |
| | `concealed` | 是否暗杠 |
| | `added` | 是否补杠（碰后加杠） |
| `Options` | `tsumo` | 自摸 |
| | `last_tile` | 和绝张 |
| | `rob_kong` | 抢杠和 |
| | `kong_bloom` | 杠上开花 |
| | `last_draw` | 妙手回春 |
| | `last_discard` | 海底捞月 |
| | `round_wind` | 圈风（东/南/西/北） |
| | `seat_wind` | 门风（东/南/西/北） |
| | `flowers` | 花牌张数（0~8） |
| `Score` | `fans` | `[(番种名, 番数), …]`，按分值降序 |
| | `total` | 总番（含花牌） |
| | `base` | 起和分口径（不含花牌） |
| | `flowers` | 花牌数 |
| | `ok` | 是否达到 8 分 |
| | `message` | 提示信息 |
| | `pattern` | 牌型说明（"七对"/"组合龙"/"基本牌型"等） |

---

## 第 20 章 命令行 / 子进程调用

### 20.1 基本用法

**前提**：有打包好的 `国标麻将算番器.exe`，或能运行 `python mahjong_core.py`。

```bash
# 最简单的算番
国标麻将算番器.exe --hand "11123456789999m" --win 9m --json
```

**输出（JSON）**：

```json
{
  "ok": true,
  "total": 106,
  "base": 106,
  "flowers": 0,
  "fans": [
    {"name": "\u4e5d\u83b2\u5b9d\u706f", "fan": 88},
    {"name": "\u6e05\u9f99", "fan": 16},
    {"name": "\u56db\u5f52\u4e00", "fan": 2}
  ],
  "text": "\u4e5d\u83b2\u5b9d\u706f88\u756a\u3001\u6e05\u9f9916\u756a\u3001\u56db\u5f52\u4e002\u756a",
  "pattern": "\u57fa\u672c\u724c\u578b",
  "message": ""
}
```

> **注意**：JSON 输出用 `ensure_ascii=True`，中文写成 `\uXXXX`，**纯 ASCII**，任何语言读取都不乱码。解析后仍是正常中文。

### 20.2 牌串写法

**三种写法，可混用**：

| 写法 | 示例 | 说明 |
| --- | --- | --- |
| **代码** | `W1 W2 W3` | 与界面一致 |
| **中文** | `一万 二万 三万` | 直观 |
| **简写** | `123m` / `456s` / `789p` / `11z` | 麻将常用简写 |

**简写字母**：
- `m` = 万（man）
- `s` = 索（suo）
- `p` = 筒（ping / tong）
- `z` = 字牌（东南西北中发白，1~4 = 东南西北，5~7 = 中发白）

**示例**：
```
123m456s789p  = 一万二万三万 四索五索六索 七筒八筒九筒
11z           = 东东（1 是东，2 是南…）
55z           = 中中（5 是中）
```

### 20.3 常用参数

| 参数 | 说明 |
| --- | --- |
| `--hand "..."` / `-H "..."` | 整手牌（含和张） |
| `--win 9m` / `-w 9m` | 和张（可省，默认取最后一张） |
| `--meld chi:123m` / `-m ...` | 副露，可多次 |
| `--waits` | 听牌模式（给 13 − 3×副露数 张） |
| `--waits-all` | 听牌模式，不筛 8 分起和分 |
| `--tsumo` | 自摸 |
| `--last-tile` | 和绝张 |
| `--rob-kong` | 抢杠和 |
| `--kong-bloom` | 杠上开花 |
| `--last-draw` | 妙手回春 |
| `--last-discard` | 海底捞月 |
| `--round 东` | 圈风 |
| `--seat 南` | 门风 |
| `--flowers 2` | 花牌张数 |
| `--text` | 输出人话（默认 JSON） |
| `--json` | 输出 JSON（默认行为） |
| `--fan-table` | 输出 81 番种表 |
| `--version` | 输出版本信息 |
| `--help` / `-h` | 帮助 |

### 20.4 副露写法

**格式**：`类型:牌`

| 类型 | 说明 | 示例 |
| --- | --- | --- |
| `chi` | 吃 | `chi:123m` |
| `peng` / `pong` | 碰 | `peng:111z` |
| `kong` | 明杠 | `kong:5555z` |
| `angang` | 暗杠 | `angang:7777z` |

**多个副露**：多次 `--meld`。

### 20.5 退出码

| 退出码 | 含义 |
| --- | --- |
| **0** | 成功算出结果（可能不足 8 分，但程序正常） |
| **1** | 牌型不能和牌 |
| **2** | 参数错误 / 牌面错误 |

### 20.6 示例集

```bash
# 1. 九莲宝灯（88 番）
国标麻将算番器.exe --hand "11123456789999m" --win 9m --json

# 2. 中文写法
国标麻将算番器.exe --hand "一万一万 一万 二万 三万 四万 五万 六万 七万 八万 九万 九万 九万" --win 九万 --json

# 3. 带副露（明杠 + 暗杠）
国标麻将算番器.exe --hand "44556677s" --win 4s \
    --meld chi:123m --meld kong:5555z --json

# 4. 听牌分析
国标麻将算番器.exe --hand "1112345678999m" --waits --json

# 5. 自摸 + 花牌 + 门风
国标麻将算番器.exe --hand "1112345678999m" --win 9m \
    --tsumo --round 东 --seat 南 --flowers 2 --json

# 6. 人话输出
国标麻将算番器.exe --hand "1112345678999m" --win 9m --text

# 7. 81 番种表
国标麻将算番器.exe --fan-table --json

# 8. 版本
国标麻将算番器.exe --version --json
```

---

## 第 21 章 Web 版 HTTP 接口

> **⚠️ 重要区分**：
>
> - **给程序调用的 API 是 `mahjong_core`**（import 或命令行），**不要用 HTTP**。
> - **HTTP 接口是为 Web 版页面自己用的**，或者给别的语言/跨设备调用（但这时不如用命令行）。
> - 如果你确实需要 HTTP（比如手机网页调用），可以启动 Web 版服务。

### 21.1 启动服务

**方式 1：主程序菜单**

- 「工具 → 启动 Web 版」（Ctrl+Alt+A）
- 默认只监听 `127.0.0.1:8718`
- 勾「允许局域网访问（手机/平板）」→ 绑定 `0.0.0.0`，手机能用

**方式 2：命令行**

```bash
python mahjong_api.py --host 0.0.0.0 --port 8718 --token 1234
```

### 21.2 接口列表

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/` | **Web 版算番器页面**（手机/平板/电脑浏览器打开就能用） |
| GET | `/debug` | 接口自测页 |
| GET | `/manifest.webmanifest` | PWA 清单 |
| GET | `/tiles/<编码>.png` | 牌面图 |
| GET | `/api/help` | 接口说明（就是模块 docstring） |
| GET | `/api/health` | 存活探测 |
| GET | `/api/version` | 版本 / 番种数 / 规则文件 |
| GET | `/api/tiles` | 34 张牌编码表 |
| GET | `/api/fan_table` | 81 番种表 |
| GET | `/api/score?...` | 算番（GET 简写） |
| POST | `/api/score` | 算番（推荐） |
| POST | `/api/waits` | 听牌（只返回达 8 分的） |
| POST | `/api/waits_all` | 听牌（不筛起和分） |
| POST | `/api/wins` | `/api/waits` 别名 |

### 21.3 请求体格式

```json
{
  "melds": [
    {"kind": "chi", "tiles": ["W1","W2","W3"], "concealed": false},
    {"kind": "kong", "tiles": ["B7","B7","B7","B7"], "concealed": true}
  ],
  "concealed": ["W4","W5","W6","T2","T3","T4","B5","B6","B7","B9","B9"],
  "win": "W6",
  "options": {
    "tsumo": true,
    "last_tile": false,
    "rob_kong": false,
    "kong_bloom": false,
    "last_draw": false,
    "last_discard": false,
    "round_wind": "东",
    "seat_wind": "南",
    "flowers": 2
  }
}
```

### 21.4 张数口径（关键！）

> **`concealed` 是「整手牌」且包含和张**，长度必须 = `14 - 3×副露数`。

**例**：

- 无副露：`concealed` = **14 张**（含和张）
- 1 个副露：`concealed` = **11 张**
- 2 个副露：`concealed` = **8 张**
- 4 个副露：`concealed` = **2 张**

**听牌接口相反**：`/api/waits` 的 `concealed` = **13 − 3×副露数 张**（少了 1 张）。

### 21.5 响应格式

```json
{
  "ok": true,
  "total": 106,
  "base": 106,
  "flowers": 0,
  "reach_standard": true,
  "pattern": "基本牌型",
  "message": "",
  "fans": [
    {"name": "九莲宝灯", "value": 88},
    {"name": "清龙", "value": 16},
    {"name": "四归一", "value": 2}
  ],
  "win": "W9",
  "melds": []
}
```

### 21.6 错误响应

```json
{
  "ok": false,
  "error": "concealed 应为 14 张（14 - 3×副露数 0），收到 13 张"
}
```

HTTP 状态码：
- **400**：参数错误
- **401**：需要口令
- **404**：未知接口
- **500**：内部错误

### 21.7 口令（可选）

设了口令后，两种传法：

- **HTTP 头**：`X-Api-Token: 1234`
- **URL 参数**：`?token=1234`

默认**不需要口令**（v2.6.0 起）。

---

## 第 22 章 各语言调用示例（C#/Java/JS/Excel/Go/Rust…）

### 22.1 C#（.NET）

```csharp
using System;
using System.Diagnostics;
using System.Text.Json;

class Program
{
    static void Main()
    {
        var psi = new ProcessStartInfo
        {
            FileName = @"国标麻将算番器.exe",
            Arguments = @"--hand ""11123456789999m"" --win 9m --json",
            RedirectStandardOutput = true,
            UseShellExecute = false,
            CreateNoWindow = true,
            StandardOutputEncoding = System.Text.Encoding.UTF8,
        };
        using var p = Process.Start(psi);
        string json = p.StandardOutput.ReadToEnd();
        p.WaitForExit();

        using var doc = JsonDocument.Parse(json);
        var root = doc.RootElement;
        Console.WriteLine($"总番: {root.GetProperty("total").GetInt32()}");
        foreach (var fan in root.GetProperty("fans").EnumerateArray())
            Console.WriteLine($"  {fan.GetProperty("fan")}番 {fan.GetProperty("name")}");
    }
}
```

### 22.2 Java

```java
import java.io.*;
import java.nio.charset.StandardCharsets;
import com.google.gson.*;

public class MahjongCli {
    public static void main(String[] args) throws Exception {
        ProcessBuilder pb = new ProcessBuilder(
            "国标麻将算番器.exe",
            "--hand", "11123456789999m",
            "--win", "9m",
            "--json"
        );
        pb.redirectErrorStream(true);
        Process p = pb.start();

        StringBuilder sb = new StringBuilder();
        try (BufferedReader r = new BufferedReader(
                new InputStreamReader(p.getInputStream(), StandardCharsets.UTF_8))) {
            String line;
            while ((line = r.readLine()) != null) sb.append(line).append('\n');
        }
        p.waitFor();

        JsonObject obj = JsonParser.parseString(sb.toString()).getAsJsonObject();
        System.out.println("总番: " + obj.get("total").getAsInt());
        for (JsonElement e : obj.getAsJsonArray("fans")) {
            JsonObject f = e.getAsJsonObject();
            System.out.println("  " + f.get("fan").getAsInt() + "番 "
                             + f.get("name").getAsString());
        }
    }
}
```

### 22.3 JavaScript（Node.js）

```javascript
const { execFile } = require("child_process");

execFile("国标麻将算番器.exe",
    ["--hand", "11123456789999m", "--win", "9m", "--json"],
    { encoding: "utf8", maxBuffer: 10 * 1024 * 1024 },
    (err, stdout, stderr) => {
        if (err) { console.error(err); return; }
        const r = JSON.parse(stdout);
        console.log(`总番: ${r.total}`);
        for (const f of r.fans) console.log(`  ${f.fan}番 ${f.name}`);
    });
```

### 22.4 Excel VBA

```vba
Function CalculateMahjong(hand As String, winTile As String) As String
    Dim cmd As String, json As String
    Dim shell As Object, exec As Object
    
    cmd = """国标麻将算番器.exe"" --hand """ & hand & """ --win " & winTile & " --json"
    
    Set shell = CreateObject("WScript.Shell")
    Set exec = shell.Exec("cmd /c " & cmd)
    
    Do While exec.Status = 0
        DoEvents
    Loop
    json = exec.StdOut.ReadAll()
    
    CalculateMahjong = json
End Function

' 单元格用法：=CalculateMahjong("11123456789999m", "9m")
```

### 22.5 Python（子进程）

```python
import subprocess, json

def calc(hand, win=None):
    args = ["国标麻将算番器.exe", "--hand", hand, "--json"]
    if win:
        args += ["--win", win]
    r = subprocess.run(args, capture_output=True, text=True,
                       encoding="utf-8", timeout=10)
    return json.loads(r.stdout)

print(calc("11123456789999m", "9m"))
```

### 22.6 Go

```go
package main

import (
    "encoding/json"
    "fmt"
    "os/exec"
)

type Fan struct {
    Name string `json:"name"`
    Fan  int    `json:"fan"`
}
type Result struct {
    OK    bool `json:"ok"`
    Total int  `json:"total"`
    Fans  []Fan `json:"fans"`
}

func main() {
    out, err := exec.Command("国标麻将算番器.exe",
        "--hand", "11123456789999m", "--win", "9m", "--json").Output()
    if err != nil { panic(err) }
    var r Result
    json.Unmarshal(out, &r)
    fmt.Println("总番:", r.Total)
    for _, f := range r.Fans {
        fmt.Printf("  %d番 %s\n", f.Fan, f.Name)
    }
}
```

### 22.7 Rust

```rust
use std::process::Command;
use serde_json::Value;

fn main() {
    let out = Command::new("国标麻将算番器.exe")
        .args(["--hand", "11123456789999m", "--win", "9m", "--json"])
        .output()
        .expect("failed to execute");
    let v: Value = serde_json::from_slice(&out.stdout).unwrap();
    println!("总番: {}", v["total"]);
    for f in v["fans"].as_array().unwrap() {
        println!("  {}番 {}", f["fan"], f["name"]);
    }
}
```

### 22.8 PHP

```php
<?php
$output = shell_exec(
    '国标麻将算番器.exe --hand "11123456789999m" --win 9m --json'
);
$r = json_decode($output, true);
echo "总番: " . $r['total'] . "\n";
foreach ($r['fans'] as $f) {
    echo "  {$f['fan']}番 {$f['name']}\n";
}
```

### 22.9 PowerShell

```powershell
$out = & "国标麻将算番器.exe" --hand "11123456789999m" --win 9m --json
$r = $out | ConvertFrom-Json
Write-Host "总番: $($r.total)"
foreach ($f in $r.fans) {
    Write-Host "  $($f.fan)番 $($f.name)"
}
```

---

## 第 23 章 牌张编码规范

### 23.1 编码表

程序内部用 **0~33** 的整数 ID 表示牌：

| ID | 代码 | 名称 |
| --- | --- | --- |
| 0 | B1 | 一筒 |
| 1 | B2 | 二筒 |
| … | … | … |
| 8 | B9 | 九筒 |
| 9 | T1 | 一索 |
| … | … | … |
| 17 | T9 | 九索 |
| 18 | W1 | 一万 |
| … | … | … |
| 26 | W9 | 九万 |
| 27 | F1 | 东 |
| 28 | F2 | 南 |
| 29 | F3 | 西 |
| 30 | F4 | 北 |
| 31 | J1 | 中 |
| 32 | J2 | 发 |
| 33 | J3 | 白 |

### 23.2 花牌编码

花牌单独编码（不在 0~33 里）：

| 代码 | 名称 |
| --- | --- |
| S1 | 春 |
| S2 | 夏 |
| S3 | 秋 |
| S4 | 冬 |
| P1 | 梅 |
| P2 | 兰 |
| P3 | 竹 |
| P4 | 菊 |

### 23.3 辅助函数

```python
from mahjong_core import suit_of, num_of, is_suit, is_honor, code_of, tile_of, name_of

tile_of("W3")      # 20
code_of(20)        # "W3"
name_of(20)        # "三万"

suit_of(20)        # 2（0=筒 1=索 2=万 3=风 4=箭）
num_of(20)         # 3

is_suit(20)        # True
is_honor(20)       # False
is_honor(27)       # True（东）
```

---

## 第 24 章 请求 / 响应数据结构

### 24.1 Score 对象

```python
@dataclass
class Score:
    fans: List[Tuple[str, int]]   # [(番种名, 番数), …]（按分值降序）
    total: int                    # 总番（含花牌）
    base: int                     # 起和分口径（不含花牌）
    flowers: int                  # 花牌数
    ok: bool                      # base >= 8
    message: str                  # 提示
    pattern: str                  # 牌型说明

    @property
    def fan_map(self) -> Dict[str, int]: ...   # {番种名: 番数}

    def text(self) -> str: ...                  # "清一色24番、平和2番、门前清2番"
```

### 24.2 JSON 响应（Web 接口）

```json
{
  "ok": true,
  "total": 106,
  "base": 106,
  "flowers": 0,
  "reach_standard": true,
  "pattern": "基本牌型",
  "message": "",
  "fans": [
    {"name": "九莲宝灯", "value": 88},
    {"name": "清龙", "value": 16},
    {"name": "四归一", "value": 2}
  ]
}
```

### 24.3 JSON 响应（命令行）

```json
{
  "ok": true,
  "total": 106,
  "base": 106,
  "flowers": 0,
  "fans": [
    {"name": "九莲宝灯", "fan": 88},
    {"name": "清龙", "fan": 16},
    {"name": "四归一", "fan": 2}
  ],
  "text": "九莲宝灯88番、清龙16番、四归一2番",
  "pattern": "基本牌型",
  "message": ""
}
```

**注意字段名差异**：
- Web 接口用 `value`
- 命令行用 `fan`

### 24.4 听牌响应

```json
{
  "ok": true,
  "count": 9,
  "concealed": ["W1","W1","W1","W2","W3","W4","W5","W6","W7","W8","W9","W9","W9"],
  "waits": [
    {
      "tile": "W1",
      "name": "一万",
      "total": 88,
      "base": 88,
      "flowers": 0,
      "reach_standard": true,
      "pattern": "基本牌型",
      "message": "",
      "fans": [{"name": "九莲宝灯", "value": 88}]
    },
    ...
  ]
}
```

---

# 第五部分 · 高级应用与二次开发

## 第 25 章 Web 版（手机 / 平板浏览器算番）

### 25.1 使用场景

你**不想装软件**，只想用**手机浏览器**算番？或者你想**多个设备共用一套算番逻辑**？

Web 版就是干这个的。

### 25.2 启动 Web 版

**方式 1：主程序菜单**

1. 打开 `mahjong_gui.py`
2. 菜单「工具 → 启动 Web 版」（Ctrl+Alt+A）
3. 默认本机地址：`http://127.0.0.1:8718/`
4. 勾「工具 → 允许局域网访问（手机/平板）」→ 手机可用

**方式 2：命令行**

```bash
python mahjong_api.py --host 0.0.0.0 --port 8718
```

### 25.3 手机访问

1. 确保**手机和电脑在同一个 WiFi**
2. 电脑勾「允许局域网访问」
3. 菜单「工具 → Web 版地址与用法」→ **复制手机链接到剪贴板**
4. 发到手机，打开浏览器 → 粘贴 → 访问

**默认不需要口令**（v2.6.0 起）。

### 25.4 Web 版界面

**与桌面版完全一致**（v2.4.4 起）：

1. 牌选择区（索 / 筒 / 万 / 字牌四行）
2. 模式行（立牌 / 吃 / 碰 / 明杠 / 暗杠 + 重置 + 布局切换）
3. 选项区四行（复选框 / 风圈 / 风位 / 花牌）
4. 已选牌（副露 / 立牌 / 和张 / 花牌）
5. 听牌候选
6. 算番结果

### 25.5 布局切换（v2.7.0）

**Web 版有两套布局**：

| 布局 | 说明 |
| --- | --- |
| **经典（默认）** | 固定尺寸，与老版本逐像素一致 |
| **自适应** | 按屏幕宽高算牌尺寸（20~46px），手机上不会过大、平板上不会过小 |

**切换**：模式行里的「布局：经典 ⇄ 自适应」按钮，选择会记在 `localStorage`。

### 25.6 触摸操作

**减牌入口（v2.4.6）**：

- **点红色角标** = −1（单指）
- **点「已选牌 → 立牌」里的一张** = −1
- 长按 / 右键**仍保留**给鼠标用户

**禁用了**：
- 长按系统菜单
- 文本选择

### 25.7 PWA（添加到主屏）

Web 版提供 `/manifest.webmanifest`，可以**添加到手机主屏**当 App 用。

### 25.8 Web 版副露显示（v2.7.1）

与桌面版一致：

- **暗杠** = 面·背·背·面（中间两张 `empty.png`）
- **明杠 / 碰 / 吃** = 全正面
- 副露之间有间隔

---

## 第 26 章 免口令访问白名单

### 26.1 背景

v2.4.x 是**一刀切**：设了口令，就是「Web 页面免口令、`/api/*` 全部要口令」。

v2.5.0 起，**可以分项决定哪些访问免口令**。

### 26.2 五个分组

| 分组键 | 内容 | 默认 |
| --- | --- | --- |
| `web` | Web 客户端页面 + 牌面图（`/`、`/web`、`/index.html`、`/manifest`、`/favicon`、`/tiles/*`） | ✅ 免口令 |
| `debug` | 自测页 `/debug` | ✅ 免口令 |
| `info` | 查询类接口（`/api/health`、`/version`、`/tiles`、`/fan_table`、`/help`） | ❌ 要口令 |
| `score` | 算番接口 `/api/score` | ❌ 要口令 |
| `waits` | 听牌接口（`/api/waits`、`/waits_all`、`/wins`） | ❌ 要口令 |

### 26.3 配置方式

**GUI**：工具菜单 → 「免口令访问设置…」→ 逐项勾选。

**命令行**：`--anon web,info`（逗号分隔）；`--anon none` = 全部要口令。

### 26.4 使用技巧

**场景 1：Web 页面免口令，但算番接口要口令**

```
web ✅
debug ✅
info ❌
score ❌
waits ❌
```

手机浏览器打开 `http://电脑IP:8718/` 直接用；别人调 `/api/score` 需要口令。

**场景 2：全部要口令（最严格）**

```
全部 ❌
```

打开页面也需要 `http://电脑IP:8718/?token=口令`；页面里的牌面图会自动带 token，**不会裂图**。

---

## 第 27 章 设置持久化机制

### 27.1 设置文件

**位置**：程序目录 / exe 目录下的 `mahjong_settings.json`。

**写入方式**：原子写入（tmp + `os.replace`），防止写一半崩了损坏文件。

### 27.2 保存的项

```json
{
  "geometry": [x, y, w, h],
  "geo_ver": 2,
  "window_maximized": false,
  "wind_round": "东",
  "wind_seat": "东",
  "flowers": 2,
  "flowers_picked": ["S1", "P4"],
  "tsumo": false,
  "last_tile": false,
  "special_a": false,
  "special_b": false,
  "mode": "stand",
  "page": 0,
  "api_port": 8718,
  "api_auto_start": false,
  "api_lan": false,
  "api_token": "",
  "api_anon": ["web", "debug"]
}
```

### 27.3 保存时机

- 控件变更 → `autosave()`（500ms 防抖）→ 立即写盘
- 退出 `closeEvent` → 强制保存

### 27.4 恢复机制

- 启动 `_load_settings()` 恢复
- 用 `blockSignals` 防止加载时触发保存
- 旧版几何 `geo_ver < 2` → 一次性把宽度收紧到 680

---

## 第 28 章 自定义规则与二次开发

### 28.1 修改规则数据

**规则数据在 `国标麻将标准规则.json`**，可以直接编辑：

- 番种分值：`番种` 下的 `fan` 字段
- 「不计」表：`不计` 字段
- 定义与补充说明

**注意**：改完要跑 `_scaffold/test_engine.py` 和 `_scaffold/error_audit.py` 校验。

### 28.2 扩展引擎

**新增番种**：

1. 在 `mahjong_core.py` 里写一个 `fan_xxx(ctx) -> bool` 函数
2. 在 `collect_fans()` 里调用它
3. 在 `国标麻将标准规则.json` 里加番种定义
4. 在 `EXTRA_EXCLUDES` 里加必然并存关系
5. 跑 `error_audit.py` 验证

**示例**：

```python
def fan_wu_fan_he(ctx: Ctx) -> bool:
    """无番和：算不出任何番种"""
    return len(collect_fans(ctx)) == 0
```

### 28.3 集成到别的程序

**Python**：

```python
from mahjong_core import MahjongFanCalculator
```

**其它语言**：

- 推荐用**命令行子进程**（见第 22 章）
- 或者用 **Web 版 HTTP 接口**

### 28.4 二次开发注意事项

- **别改牌张编码**（0~33 是内部约定）
- **别改 `Meld` / `Options` / `Score` 的字段名**（外部依赖）
- **别删公开接口**（`score` / `winning_tiles` / `waiting_tiles` / `fan_table`）
- **改完必跑测试**（见第 30 章）

---

## 第 29 章 打包分发

### 29.1 用 Nuitka 打包

**批处理**：`0_nuitka_pyside6_clang_auto.bat`

**内部调用**：`_scaffold/build_exe.py`

**关键参数**：

```
--follow-imports
--include-data-files=国标麻将标准规则.json=国标麻将标准规则.json
--include-data-files=麻将图/*.png=麻将图/
--include-data-files=icon.ico=icon.ico
--windows-icon-from-ico=icon.ico
--windows-console-mode=attach
```

**`--windows-console-mode=attach` 很重要**：

- 双击 = 纯 GUI（不弹黑窗）
- 被 cmd/subprocess 调用 = 能挂到父控制台，`--json` 输出可见

### 29.2 分发内容

打包后目录里必须有：

```
mahjong_gui.dist/
├── 国标麻将算番器.exe
├── 国标麻将标准规则.json
├── 麻将图/
│   ├── B1.png ~ B9.png
│   ├── T1.png ~ T9.png
│   ├── W1.png ~ W9.png
│   ├── F1.png ~ F4.png
│   ├── J1.png ~ J3.png
│   ├── S1.png ~ S4.png
│   ├── P1.png ~ P4.png
│   └── empty.png
├── icon.ico
└── （其他 Nuitka 生成的运行时文件）
```

### 29.3 制作图标

**脚本**：`_scaffold/make_icon.py`

- 用 PIL + `msyh.ttf`
- **7 张尺寸各自绘制**（不是缩放同一张，小尺寸才不糊）
- 输出 `icon.ico`（手写 ICO 容器，每张是 PNG 条目）

### 29.4 常见问题

| 问题 | 解决 |
| --- | --- |
| 打包后没图标 | 检查 `--windows-icon-from-ico` 和 `--include-data-files=icon.ico=icon.ico` 是否都有 |
| 运行时报找不到规则文件 | 检查 `国标麻将标准规则.json` 是否和 exe 同目录 |
| 界面找不到牌面图 | 检查 `麻将图\` 是否和 exe 同目录 |
| exe 命令行调用看不到输出 | 用 `cmd /c "... > out.txt 2>&1"` 重定向 |

---

## 第 30 章 测试体系与校验方法

### 30.1 测试脚本一览

| 脚本 | 内容 | 项目数 |
| --- | --- | --- |
| `_scaffold/test_engine.py` | 引擎功能测试 | 73 项 |
| `_scaffold/cli_test.py` | 命令行/import 算番入口 | 34 项 |
| `_scaffold/smoke_test.py` | 界面冒烟测试 | 221 项 |
| `_scaffold/api_smoke_test.py` | Web 服务 HTTP 测试 | 132 项 |
| `_scaffold/verify_all.py` | **全量枚举**（2842 万手） | 全量 |
| `_scaffold/error_audit.py` | **错误类别故障注入**（29 个） | 29 个 |
| `_scaffold/indy.py` | **第二套独立实现**（对拍基准） | 工具 |
| `_scaffold/golden_fans.py` | **81 番种金标准牌例** | 81 个 |

### 30.2 运行测试

```powershell
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\test_engine.py"
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\smoke_test.py"
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\api_smoke_test.py"
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\cli_test.py"
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\error_audit.py"
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\verify_all.py" --quick
```

### 30.3 全量校验的用法

```bash
# 完整校验（2842 万手，数小时）
python _scaffold/verify_all.py

# 分族跑（先看稀有牌型）
python _scaffold/verify_all.py --families "组合龙,特殊,k=3"

# 快速校验（约 2 分钟）
python _scaffold/verify_all.py --quick

# 中断续跑（自动读进度文件）
python _scaffold/verify_all.py

# 从头跑
python _scaffold/verify_all.py --restart
```

### 30.4 错误注入验证

`error_audit.py` 把**25 类可能出错的方式**逐一注入，看现有校验体系能不能抓到：

- **E01** 连七对只比较牌号差（真缺陷，已修）
- **E03** 三色三同顺不查花色
- **E04** 三色三节高用花色并集
- **E06** 4 副露牌型不可分
- …
- **E29** 七对退回旧口径

**要求**：29 个注入**全部被检测器抓到**，否则说明有校验盲区。

### 30.5 测试注意事项

- **界面测试**用 `QT_QPA_PLATFORM=offscreen`
- **测试不污染真实设置**（临时文件）
- **每次从干净状态开始**
- **别用固定端口 8718**（会撞车），用 `free_port()`
- **跨实现对拍只比双方都实现的口径**

---

# 第六部分 · 版本更新简介

## 简明版（给普通用户）

### v2.7.2（2026-09-25）

**新增**：**手机上也能减牌**——点红色数字角标 = 减一张；点「已选牌」里的牌 = 取消这张（副露点一张 = 撤销整组）。

**没变**：右键仍然可用，鼠标用户习惯不变。

### v2.7.1（2026-09-25）

**修复**：Web 版的暗杠**显示得像明杠**——现在暗杠显示为「面·背·背·面」，跟桌面版一样。

### v2.7.0（2026-09-25）

**新增**：Web 版「**布局：经典 ⇄ 自适应**」切换按钮，手机上不会过大、平板上不会过小。

**默认**：仍是经典（与老版本一致）。

### v2.6.0（2026-09-25）

**新增**：**命令行算番**（不用启动程序界面）：

```bash
国标麻将算番器.exe --hand "11123456789999m" --win 9m --json
```

**变化**：**Web 版默认不需要口令**（以前会自动生成 6 位口令）。

**新增**：`mahjong_core.score_hand()` 一行算番函数。

### v2.5.0（2026-09-25）

**新增**：**免口令访问白名单**——设了口令后，可以分项决定哪些访问不用口令（含 Web 客户端）。

### v2.4.7（2026-09-25）

**新增**：**程序图标 `icon.ico`**（红中牌样式）——窗口、任务栏、exe 文件都有图标。

### v2.4.6（2026-09-25）

**改进**：Web 版减牌入口改成「**点红色角标 −1**」——手机没有右键也能操作。

### v2.4.5（2026-09-25）

**修复**：Web 版牌池**变成一列**——现在恢复成 4 行（索 / 筒 / 万 / 字牌）。

### v2.4.4（2026-09-25）

**改进**：「重置」改成**全部复位**（花牌 + 复选框 + 圈风风位都清）；Web 版布局对齐桌面版。

### v2.4.3（2026-09-25）

**改进**：窗口**默认宽度从 1180 收窄到 680**（牌池本来就只要 560）。

### v2.4.2（2026-09-25）

**修复**：牌选择区的牌**间距过大**——现在只留 4px。

### v2.4.1（2026-09-25）

**改进**：选项区改成**固定四行**（复选框 / 圈风 / 风位 / 花牌）。

### v2.4.0（2026-09-25）

**新增**：**Web 版**（手机 / 平板浏览器算番）+ 局域网访问 + 访问口令。

### v2.3.0（2026-09-25）

**新增**：**本地 HTTP API**（JSON 接口）。

### v2.2.1（2026-09-24）

**修正**：七对**允许 4 张相同的牌**（4 张算两对）。

### v2.2.0（2026-09-24）

**修复**：**连七对跳花色误判**（会多算 88 分）。

**新增**：错误类别全枚举 + 故障注入验证（29 个注入）。

### v2.1.0（2026-09-24）

**修复**：**5 个算番缺陷**（绿一色牌面 / 三色三同顺 / 三色三节高 / 4 副露牌型 / 一色双龙会）。

**优化**：听牌判定快约一个数量级。

**新增**：全量枚举 + 第二套独立实现对拍。

### v2.0.0（2026-09-24）

**推倒重来**：PySide6 重写界面 + 算番引擎重建（81 番种全部实现）。

### v1.1.0（2026-09-24）

**新增**：规则数据全量整理（`国标麻将标准规则.json`）+ 设置实时自动保存。

---

## 详细版（给技术用户）

### v2.7.2 —— 桌面版也能「点已选的牌取消」

**问题**：桌面版原来只有「右键减一张」，手机/触屏用不了。

**改动**：

- `CountBadge`（可点角标，点一下 = −1，`event.accept()` 防止冒泡成加牌）
- `ClickTile`（可点的牌）
- `TileRow(clickable=True)` + `set_tiles(codes, payloads, tip)`
- 4 个处理函数：`on_hand_row_clicked` / `on_win_row_clicked` / `on_meld_row_clicked` / `on_flower_row_clicked`
- `_meld_items()` 改成 `(牌编码, 组号)`

**验证**：`smoke_test.py` §21（22 项）+ `probe_click_remove.py`（10 项，真机）。

### v2.7.1 —— 修 Web 端暗杠显示

**问题**：Web 端暗杠显示得像明杠。

**根因**：渲染层**没看 `concealed`**（数据层一直是对的）。

**改动**：

- 新增 `meldHtml()`（暗杠 = 面·背·背·面，用 `empty.png`）
- 副露间隔 `.meldgap`
- `render()` 改成用 `meldHtml()`

**验证**：Playwright 真机量 DOM（暗杠 4 张里中间两张的 `src` = `/tiles/empty.png`）。

### v2.7.0 —— Web 版「自适应」布局

**新增**：

- CSS 尺寸变量化（默认值 = 经典布局）
- `S.layout`（`"fixed"` 默认 / `"auto"`）
- `autoSize()` 按屏幕宽高算尺寸（20~46px）
- `localStorage.mj_layout` 记忆

**验证**：`api_smoke_test.py` 新增 10 项断言。

### v2.6.0 —— 算番入口回到「引擎 + 命令行」

**新增**：

- `mahjong_core.py` 命令行入口（`--hand` / `--win` / `--meld` / `--waits` / `--json` / `--text`）
- `score_hand()` / `score_codes()` 便捷函数
- 主程序 `main()` 检测算番参数 → **不开界面**
- `--windows-console-mode=attach`（双击纯 GUI，命令行调用能看输出）

**Web 版默认免密码**（不再自动生成 6 位口令）。

**验证**：`cli_test.py` 34 项。

### v2.5.0 —— 免口令访问白名单

**新增**：

- `ANON_GROUPS`（五组：`web` / `debug` / `info` / `score` / `waits`）
- `ANON_DEFAULT = ("web", "debug")`（＝旧行为）
- `Handler._anon_ok()`
- `ApiServer(..., anon=…)`（`anon=None` = 用默认）
- GUI：`AnonAccessDialog`

**踩坑**：
1. `"/"` 必须当精确项（写成前缀会命中一切路径）
2. `anon=None` 表示「用默认」，`anon=()` 才是「全部都口令」
3. 测试别遍历 `menuBar().actions()`（会撞到已失效的 QMenu 包装）

### v2.4.7 —— 程序图标

**新增**：

- `icon.ico`（红中牌样式，7 张尺寸各自绘制）
- `_scaffold/make_icon.py`
- `mahjong_gui.app_icon()`（读 exe 同目录，找不到安静降级）

**三处齐全**：
1. exe 文件图标 → `--windows-icon-from-ico=icon.ico`
2. 窗口/任务栏 → `app_icon()` 读 exe 同目录 + `--include-data-files=icon.ico=icon.ico`
3. `main()`（app 级）+ `MahjongFanWindow.__init__`（窗口级）**两处 `setWindowIcon`**

### v2.4.6 —— Web 端手机减牌

**改动**：

- 角标变成「−1 按钮」（`bindMinus()` 截住所有事件）
- 已选牌行可点减
- 禁掉长按系统菜单 + 文本选择

### v2.4.5 —— 修 Web 端牌池变一列

**根因**：`<div id="pool">` 漏了 `class="pool"`，而 `.pool .line{display:flex}` 靠祖先类选择器。

**修法**：CSS 增加 `#pool .line` 直接命中；HTML 补 `class="pool"`。

### v2.4.4 —— 「重置」全部复位 + Web 布局对齐桌面

**改动**：

- `on_reset()` 清全部（手牌 / 副露 / 和张 / 待选 / 模式 / **花牌 / 复选框 / 圈风风位**）+ 立即落盘
- Web 端布局对齐桌面版（牌池 4 行、花牌移到选项区）

### v2.4.3 —— 窗口收窄

**改动**：

- 默认 `680×940`（原来 `1180×960`）
- 最小 `600×560`（原来 `1020×700`）
- 旧几何 `geo_ver < 2` → 一次性收紧宽度

**校验脚本改为「完整检验（不抽样）」**（2842 万手全穷尽）。

### v2.4.2 —— 牌池紧凑

**修复**：给第 9 列挂 `setColumnStretch(1)`，剩余宽度全让给右侧空白。

### v2.4.1 —— 选项区四行

**改动**：复选框 / 圈风 / 风位 / 花牌各占一行。控件属性名与信号连接**未改**。

### v2.4.0 —— Web 客户端

**新增**：Web 客户端页面 + 静态路由 + 局域网访问 + 访问口令。

**修掉的 4 个真缺陷**（真机浏览器点出来的）：
1. 算番请求张数口径错（`concealed` 要含和张）
2. 设了和张进不了算番分支
3. 牌池点击会丢（DOM 重建）
4. 点花牌被当成手牌

### v2.3.0 —— 本地 HTTP API

**新增**：`/api/score` / `/api/waits` / `/api/fan_table` 等。

### v2.2.1 —— 七对允许 4 张同牌

**修正**：`sum(c // 2 for c in counts) == 7`（不是「7 个不同对子」）。

### v2.2.0 —— 错误类别全枚举

**修复**：**连七对跳花色误判**（只比较牌号差 6，会多算 64~88 分）。

**新增**：`error_audit.py`（25 类错误 / 29 个注入）。

### v2.1.0 —— 全量枚举校验

**修复 5 个算番缺陷**：

1. 绿一色牌面取错（`{12,13,14,16,18,32}` → `{10,11,12,14,16,32}`）
2. 三色三同顺未校验花色
3. 三色三节高用「花色并集≥3」
4. 4 副露牌型完全算不出番（`need >= 1` → `need >= 0`）
5. 一色双龙会误判（只看合计 ≥4）

**优化**：`waiting_tiles()` 用快速布尔判定（快一个数量级）。

### v2.0.0 —— 推倒重来

**重建**：`mahjong_core.py`（81 番种全部实现）+ `mahjong_gui.py`（PySide6 按设计图）。

**修复 10 个问题**（清一色/全不靠/组合龙/点炮暗刻/双箭刻/杠计分/界面不重算等）。

### v1.1.0 —— 规则数据全量整理

**新增**：`国标麻将标准规则.json`（完整规则库）+ 设置实时自动保存。

### v1.0.0 —— 早期状态

PyQt5 单文件 GUI，`check_winning_hand()` 是占位实现，`mahjong_core.py` 源码丢失（只剩 `.pyc`）。

---

## 完整迭代记录

完整迭代记录见 **`软件升级迭代记录.md`**，包含：

- 项目概况
- 主程序运行逻辑
- 逐版本详细迭代记录（v1.0.0 ~ v2.7.2）
- 功能清单
- 已知问题
- 数据来源
- 维护约定
- 代码地图
- 测试与验收
- 后续待办

---

# 第七部分 · 常见问题 FAQ

### Q1：为什么算出来只有 6 番，但程序说不能和？

**A**：国标麻将**起和分是 8 分**（花牌不算）。6 番不够 8 分，**不能和牌**。

程序会提示「番种合计 6 分，未达 8 分起和标准」，标红显示。

### Q2：为什么我这手牌明明是清一色，程序只算 24 番？

**A**：清一色**本身 24 番**，但如果同时有清龙、平和、门前清等，**会累加**。

比如：

- 清一色 24 番
- 清龙 16 番
- 平和 2 番
- 门前清 2 番
- **总计 44 番**

看番种列表，不要只看总番。

### Q3：为什么「自摸」勾上后，「抢杠和」变成了「杠上开花」？

**A**：这两个是**互斥**的：

- **未勾自摸** → 你是**点和**，第 3 个复选框是「抢杠和」
- **勾了自摸** → 你是**自摸**，第 3 个复选框是「杠上开花」

**切换时会自动清空旧勾选**，防止误算。

### Q4：暗杠为什么显示成「面·背·背·面」？

**A**：这是**国标规则要求的显示方式**。暗杠是扣着的，其他家看不到中间两张，所以程序只显示两头，中间用牌背。

### Q5：为什么「重置」点了，花牌还在？

**A**：v2.4.4 起「重置」改成**全部复位**（花牌、复选框、圈风风位都清）。如果你用的是旧版本，请升级。

### Q6：手机上怎么减牌？

**A**：**点红色数字角标** = 减一张；**点「已选牌 → 立牌」里的一张** = 减一张。v2.7.2 起桌面版也这样。

### Q7：别的程序怎么调用算番？

**A**：**不要用 HTTP**，用下面两种之一：

**Python**：
```python
from mahjong_core import MahjongFanCalculator
```

**其它语言**：
```bash
国标麻将算番器.exe --hand "11123456789999m" --win 9m --json
```

两种方式**都不需要 HTTP、不需要端口、不需要先启动谁**。

### Q8：为什么命令行输出是 `\u4e5d\u83b2...` 这样的？

**A**：这是 **JSON 的 ASCII 转义**（`ensure_ascii=True`）。中文写成 `\uXXXX`，**纯 ASCII**，任何语言读取都不乱码。**解析后就是正常中文**。

如果你想直接看中文，用 `--text` 参数。

### Q9：Web 版需要口令吗？

**A**：**默认不需要**（v2.6.0 起）。想加门槛，工具菜单 → 「设置访问口令」。

### Q10：Web 版和 API 有什么区别？

**A**：

- **Web 版** = 给**浏览器**用的网页版算番器
- **API** = 给**程序**调用的接口

**但注意**：给程序调用**推荐用 `mahjong_core`**（import 或命令行），**不是 HTTP**。

### Q11：为什么「九莲宝灯」算出来是 106 番，不是 88 番？

**A**：九莲宝灯**本身 88 番**，但还有：

- 清龙 16 番
- 四归一 2 番
- **总计 106 番**

**九莲宝灯不计清一色、不求人、门前清、幺九刻**（因为这些是必然并存）。

### Q12：为什么「大四喜」不算圈风刻、门风刻？

**A**：**大四喜**（4 副风刻）**必然包含**圈风刻、门风刻、三风刻、碰碰和、幺九刻 → 这些**不重复计分**。

这是《规则》里的「不重复原则」。

### Q13：七对能含 4 张相同的牌吗？

**A**：**能**（v2.2.1 起，按国标口径）。4 张相同 = 两对。

但**已开杠的 4 张不能当两对**（杠是副露，七对必须门前无副露）。

### Q14：为什么「12345 和 3」不算边张？

**A**：1998 首届健康麻将邀请赛领队会规定：**手牌听两种及以上的牌都不算边张 / 坎张 / 单钓将**。

`12345 和 3` 听 3、6 两种 → **不算边张**。

### Q15：程序支持天和 / 地和 / 人和吗？

**A**：**不支持**。国标 81 番种**没有**天和、地和、人和（《问答》Q12：机遇性太大，不予采用）。

### Q16：程序会记录我的牌局吗？

**A**：**不会**。程序只保存设置（窗口大小、圈风风位等），**不保存牌局历史**。

### Q17：改了规则 JSON 会怎样？

**A**：**会立即生效**（重新打开程序后）。但**建议改完跑测试**（`test_engine.py` + `error_audit.py`）。

### Q18：为什么程序算出来和我手算不一样？

**A**：可能原因：

1. **张数不对**（如 concealed 不含和张）
2. **副露 `concealed` 标记错**（明杠 vs 暗杠）
3. **口径不同**（如「边张」是否听两种）
4. **规则版本不同**（程序严格按 1998《规则》+ 《问答》）

建议对照 `国标麻将标准规则.json` 里的番种定义和「不计」字段。

### Q19：可以用于商业项目吗？

**A**：可以。本项目**MIT 许可证**，自由使用、修改、分发、商用。

但**文档内容未经书面许可不得转载或用于商业用途**（版权声明里写了）。

### Q20：遇到 bug 怎么办？

**A**：通过 **GitHub Issues** 提交：https://github.com/duma520/Mahjong_Calculator/issues

**不提供私人邮箱支持**（所有技术支持通过公开渠道，以便其他用户也能受益）。

---

# 第八部分 · 术语表

| 术语 | 定义 |
| --- | --- |
| **轮** | 行牌一周为一轮 |
| **盘** | 每次起牌到和牌或荒牌为一盘 |
| **圈** | 每圈进行四盘比赛，或四人轮流各坐一次庄 |
| **局** | 每打完四圈或达到规定时间为一局 |
| **圈风** | 每局比赛圈数的标志（东南西北风圈） |
| **门风** | 运动员每盘座位的标志（庄家为东风，下家为南风…） |
| **庄家 / 旁家** | 门风东者为庄家，其余均为旁家 |
| **手牌** | 摆在自己门前的牌，标准数为 13 张 |
| **将牌（将）** | 按基本牌型和牌时必须具备的单独组合的对子 |
| **顺子** | 3 张同花色序数相连的牌 |
| **刻子** | 3 张相同的牌（碰出的为明刻，抓在手中的为暗刻） |
| **对子** | 两张相同的牌 |
| **字牌** | 风牌（东南西北）和箭牌（中发白） |
| **幺九牌** | 序数牌中的一、九及字牌 |
| **吃牌** | 上家打出牌后，报「吃」者把自己的两张牌取出加在一起组成顺子 |
| **碰牌** | 任一家打出牌后，报「碰」者把自己的对子取出，加在一起组成一副刻子 |
| **杠** | 报开杠的 4 张相同的牌 |
| **补花** | 抓到花牌后，明放在立牌前，并从牌墙最后补一张牌 |
| **听牌** | 只差所需要的一张牌即能和牌的状态 |
| **和牌** | 符合规定的牌型条件，达到或超过起和分标准并报和牌的行为 |
| **自摸和** | 自己抓进成和的牌，并报和牌 |
| **点和** | 和他人打出的牌 |
| **报牌** | 行牌者宣布吃牌、碰牌、开杠、补花或和牌 |
| **番种** | 具有一定分值的各种牌张组合的形式或和牌方式的称谓 |
| **罚张** | 被判定受处罚的牌 |
| **单放** | 自摸成和牌的那一张牌，不可随意插入手牌之中，应单独摆放 |
| **多张 / 少张** | 和牌前，手牌数多于或少于规定的数量 |
| **荒牌** | 每盘抓完第 144 张牌，打出后仍无人和牌 |
| **诈和** | 不符合《规则》规定条件而宣布和牌 |
| **牌墙 / 牌城** | 四人各自在门前码成 18 墩牌 |
| **牌池** | 四道牌墙围起的区域 |
| **比赛分** | 用于体现竞赛成绩的衡量单位 |
| **起和分** | 和牌必须达到的最低分数（8 分） |
| **基本分** | 和牌后，各个番种分数的总和 |
| **底分** | 未和牌方必须向和牌方所付的分数（8 分） |
| **盘分** | 运动员在每盘比赛中所得失的比赛分 |
| **局分** | 一局比赛结束时，各盘比赛分及基础比赛分的总和 |
| **标准分** | 比赛中用于计算成绩的记分单位 |

---

# 附录 A · 81 番种完整清单

## 88 分（7 个）

| 番种 | 定义 | 不计 |
| --- | --- | --- |
| 大四喜 | 4 副风刻（杠） | 圈风刻、门风刻、三风刻、碰碰和、幺九刻 |
| 大三元 | 中发白 3 副刻子 | 箭刻 |
| 绿一色 | 23468 条 + 发字 | 混一色 |
| 九莲宝灯 | 一种花色 1112345678999 + 任一张 | 清一色、不求人、门前清、幺九刻 |
| 四杠 | 4 个杠 + 将 | 杠、碰碰和 |
| 连七对 | 一种花色序数相连的 7 个对子 | 清一色、不求人、单钓 |
| 十三幺 | 3 种序数牌的 1、9 + 7 种字牌 + 一对将 | 五门齐、不求人、单钓 |

## 64 分（6 个）

| 番种 | 定义 | 不计 |
| --- | --- | --- |
| 清幺九 | 序数牌一、九刻子 | 碰碰和、同刻、无字 |
| 小四喜 | 风牌 3 副刻子 + 将 | 三风刻 |
| 小三元 | 箭牌 2 副刻子 + 将 | 箭刻 |
| 字一色 | 字牌刻子（杠）+ 将 | 碰碰和 |
| 四暗刻 | 4 个暗刻（暗杠）+ 将 | 门前清、碰碰和 |
| 一色双龙会 | 一种花色两个老少副，5 为将 | 平和、七对、清一色 |

## 48 分（2 个）

| 番种 | 定义 | 不计 |
| --- | --- | --- |
| 一色四同顺 | 一种花色 4 副序数相同的顺子 | 一色三同顺、一般高、四归一 |
| 一色四节高 | 一种花色 4 副依次递增一位数的刻子 | 一色三同顺、碰碰和 |

## 32 分（3 个）

| 番种 | 定义 | 不计 |
| --- | --- | --- |
| 一色四步高 | 一种花色 4 副依次递增一位或两位的顺子 | 一色三步高 |
| 三杠 | 3 个杠 + 另一副牌组 + 将 | 双明杠、双暗杠 |
| 混幺九 | 字牌 + 序数牌一、九刻子 + 将 | 碰碰和、幺九刻 |

## 24 分（9 个）

| 番种 | 定义 | 不计 |
| --- | --- | --- |
| 七对 | 7 个对子 | 不求人、门前清、单钓将 |
| 七星不靠 | 7 个单张字牌 + 3 花色 147/258/369 中 7 张 | 五门齐、不求人、单钓 |
| 全双刻 | 2、4、6、8 序数牌刻子 + 将 | 碰碰和、断幺 |
| 清一色 | 一种花色序数牌 | 无字 |
| 一色三同顺 | 一种花色 3 副序数相同的顺子 | 一色三节高 |
| 一色三节高 | 一种花色 3 副依次递增一位数的刻子 | 一色三同顺 |
| 全大 | 7、8、9 序数牌 | 无字 |
| 全中 | 4、5、6 序数牌 | 断幺 |
| 全小 | 1、2、3 序数牌 | 无字 |

## 16 分（6 个）

| 番种 | 定义 | 不计 |
| --- | --- | --- |
| 清龙 | 一种花色 1~9 相连 | 连六 |
| 三色双龙会 | 2 种花色两个老少副 + 另一种花色 5 作将 | 喜相逢、老少副、无字、平和 |
| 一色三步高 | 一种花色 3 副依次递增一位或两位的顺子 | — |
| 全带五 | 每副牌及将牌必须有 5 的序数牌 | 断幺 |
| 三同刻 | 3 个序数相同的刻子 | — |
| 三暗刻 | 3 个暗刻（包括暗杠） | — |

## 12 分（5 个）

| 番种 | 定义 | 不计 |
| --- | --- | --- |
| 全不靠 | 单张 3 花色 147/258/369 不能错位 + 字牌 | 五门齐、不求人、单钓 |
| 组合龙 | 3 花色 147/258/369 不能错位 | — |
| 大于五 | 6、7、8、9 序数牌 | 无字 |
| 小于五 | 1、2、3、4 序数牌 | 无字 |
| 三风刻 | 3 个风刻 | 幺九刻 |

## 8 分（9 个）

| 番种 | 定义 | 不计 |
| --- | --- | --- |
| 花龙 | 3 花色 3 副顺子连接成 1~9 | 连六、老少副 |
| 推不倒 | 牌面图形没有上下区别的牌 | 缺一门 |
| 三色三同顺 | 3 花色 3 副序数相同的顺子 | — |
| 三色三节高 | 3 花色 3 副依次递增一位序数的刻子 | — |
| 无番和 | 数不出任何番种分 | — |
| 妙手回春 | 自摸牌墙最后一张和牌 | 自摸 |
| 海底捞月 | 和打出的最后一张牌 | — |
| 杠上开花 | 开杠抓进的牌成和 | 自摸 |
| 抢杠和 | 和别人自抓开明杠的牌 | 和绝张 |

## 6 分（7 个）

| 番种 | 定义 | 不计 |
| --- | --- | --- |
| 碰碰和 | 4 副刻子（或杠）+ 将 | — |
| 混一色 | 一种花色序数牌 + 字牌 | 缺一门 |
| 三色三步高 | 3 花色 3 副依次递增一位序数的顺子 | — |
| 五门齐 | 3 种序数牌、风、箭牌齐全 | — |
| 全求人 | 全靠吃碰（或明杠）+ 单钓别人打出的牌 | 单钓将 |
| 双暗杠 | 2 个暗杠 + 另 2 副牌组 + 将 | 暗杠、双暗刻 |
| 双箭刻 | 2 副箭刻 | 箭刻 |

## 4 分（4 个）

| 番种 | 定义 | 不计 |
| --- | --- | --- |
| 全带幺 | 每副牌、将牌都有幺九牌 | — |
| 不求人 | 4 副牌及将牌没有吃碰（包括明杠），自摸和牌 | 门前清 |
| 双明杠 | 2 个明杠 + 顺子 + 将 | 明杠 |
| 和绝张 | 和牌池、桌面已亮明的 3 张所剩的第 4 张 | — |

## 2 分（10 个）

| 番种 | 定义 | 不计 |
| --- | --- | --- |
| 箭刻 | 中发白 3 张相同 | 幺九刻 |
| 圈风刻 | 与圈风相同的风刻 | 幺九刻 |
| 门风刻 | 与门风相同的风刻 | 幺九刻 |
| 门前清 | 没有吃碰明杠，和别人打出的牌 | — |
| 平和 | 4 副顺子 + 序数牌作将 | — |
| 四归一 | 4 张相同的牌归于一家的顺子、刻子、对子、将牌中 | — |
| 双同刻 | 2 个相同序数的刻子 | — |
| 双暗刻 | 2 个暗刻（包括暗杠） | — |
| 暗杠 | 自抓 4 张相同的牌开杠 | — |
| 断幺 | 没有一、九及字牌 | — |

## 1 分（12 个）

| 番种 | 定义 | 不计 |
| --- | --- | --- |
| 一般高 | 一种花色 2 副相同的顺子 | — |
| 喜相逢 | 2 种花色 2 副序数相同的顺子 | — |
| 连六 | 同花色 6 张相连的序数牌 | — |
| 老少副 | 同花色 123 和 789 | — |
| 幺九刻 | 一、九序数牌及字牌的刻子 | — |
| 明杠 | 自己有暗刻碰别人打出的牌开杠 | — |
| 缺一门 | 和牌中缺少一种花色序数牌 | — |
| 无字 | 和牌中没有风牌和箭牌 | — |
| 边张 | 单和 123 的 3 及 789 的 7，或 1233 和 3、7789 和 7 | — |
| 坎张 | 和 2 张牌之间的牌，4556 和 5 | — |
| 单钓将 | 钓单张牌作将成和 | — |
| 自摸 | 自己抓进牌成和牌 | — |

## 花牌（1 分/张）

| 番种 | 定义 | 说明 |
| --- | --- | --- |
| 花牌 | 春、夏、秋、冬、梅、兰、竹、菊 | 每张 1 分，**不计入起和分** |

---

# 附录 B · 文件清单

## 根目录

| 文件 | 说明 |
| --- | --- |
| `mahjong_gui.py` | 主程序（唯一编译源，PySide6 界面） |
| `mahjong_core.py` | 算番引擎 + 命令行入口 |
| `mahjong_api.py` | Web 版服务端 + 页面内部 JSON 路由 |
| `国标麻将标准规则.json` | 规则数据源（81 番种 + 全部条款 + 问答） |
| `icon.ico` | 程序图标 |
| `mahjong_settings.json` | 运行时自动生成（设置持久化） |
| `麻将图\` | 牌面图（B1~B9 / T1~T9 / W1~W9 / F1~F4 / J1~J3 / S1~S4 / P1~P4 / empty） |
| `tiles\` | 早期牌面图（v1.x 遗留，仅作回退） |
| `软件设计构图\` | 界面设计图（11 张手机截图） |
| `1.bat` | 开发运行：`python mahjong_gui.py` |
| `0_nuitka_pyside6_clang_auto.bat` | 打包（Nuitka） |
| `msyh.ttf` | 界面字体 |
| `中国国标麻将标准PDF\` | 原始资料（PDF、OCR 文本、逐页 png） |
| `version\` | 版本备份 |
| `_scaffold\` | 脚手架（测试 / 校验 / 打包） |

## `_scaffold\`

| 文件 | 说明 |
| --- | --- |
| `test_engine.py` | 引擎自测（73 项） |
| `cli_test.py` | 命令行 / import 算番入口测试（34 项） |
| `smoke_test.py` | 界面冒烟测试（221 项） |
| `api_smoke_test.py` | Web 服务 HTTP 测试（132 项） |
| `verify_all.py` | 全量枚举 + 算番校验（2842 万手） |
| `error_audit.py` | 错误类别全枚举 + 故障注入 |
| `indy.py` | 第二套独立实现（对拍基准） |
| `golden_fans.py` | 81 番种金标准牌例 |
| `shot_ui.py` | 界面截图 |
| `make_icon.py` | 生成 `icon.ico` |
| `probe_icon_live.py` | 真机验证窗口图标 |
| `probe_api_token.py` | 排查「带口令访问却 401」 |
| `probe_click_remove.py` | 真机验证「点已选的牌取消」 |
| `build_exe.py` | Nuitka 打包 |

---

# 附录 C · 快捷键与菜单一览

## 快捷键

| 快捷键 | 功能 |
| --- | --- |
| `Ctrl+Q` | 退出 |
| `Ctrl+Alt+A` | 启动/停止 Web 版 |

## 菜单

### 文件(F)

- 退出(Q) — `Ctrl+Q`

### 工具(T)

- 启动 Web 版(W) — `Ctrl+Alt+A`
- 允许局域网访问（手机/平板）
- 设置访问口令(K)…
- 免口令访问设置(N)…
- Web 版地址与用法(I)…
- Web 版端口设置(P)…

### 帮助(H)

- 用法说明(H)
- 番种表(T)
- 关于(A)

---

# 附录 D · 技术支持与贡献指南

## 报告问题

**通过 GitHub Issues 提交**：https://github.com/duma520/Mahjong_Calculator/issues

**不提供私人邮箱支持**——所有技术支持都通过公开渠道进行，以便其他用户也能受益。

### 提交 Issue 时请附上

1. **版本号**（`mahjong_gui.py` 里的 `__version__` 或程序标题栏）
2. **操作系统**（Windows 10/11、Linux、macOS）
3. **复现步骤**（详细）
4. **期望结果 vs 实际结果**
5. **错误信息 / 截图**（如有）
6. **相关牌型**（如果和算番有关）

## 贡献代码

1. Fork 项目
2. 创建分支（`git checkout -b feature/xxx`）
3. 提交改动（`git commit -am 'Add xxx'`）
4. 推送（`git push origin feature/xxx`）
5. 创建 Pull Request

### 代码要求

- 遵循现有代码风格
- 新增功能要加测试
- 改了算番逻辑要跑 `verify_all.py` + `error_audit.py`
- 更新 `软件升级迭代记录.md`

## 开源许可证

本项目采用 **MIT 许可证**，自由使用、修改、分发、商用。

---

# 结语

**国标麻将算番器**是一个从零基础到高级应用都能用的工具。

- 你是**新手**？打开程序，点几下就知道多少番。
- 你是**老手**？听牌分析、番种细节、精确到每一番。
- 你是**裁判**？有争议的牌，算一遍就清楚。
- 你是**程序员**？`mahjong_core` 拿来即用，命令行/HTTP 两种方式。
- 你是**开发者**？MIT 许可证，随便改随便发。

**规则数据严格按 1998 年国家体育总局《中国麻将竞赛规则（试行）》+ 杜维忠《问答》实现**，81 个番种一个不少，全量枚举 2842 万手校验过。

**遇到问题请提 GitHub Issues**，不提供私人邮箱支持——这样其他用户也能受益。

祝你算番愉快！

---

**© 永久 杜玛 保留所有权利**

**本文档内容未经书面许可不得转载或用于商业用途**

**开源许可证：MIT**

**GitHub：https://github.com/duma520**

**项目地址：https://github.com/duma520/Mahjong_Calculator**

**问题报告：通过 GitHub Issues 提交**

**开发时间：2026-09-18**

**版本：2.7.2（2026-09-25）**
