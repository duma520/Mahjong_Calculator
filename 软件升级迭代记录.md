# 《国标麻将算番器》软件升级迭代记录

> 本文件是本项目的**唯一迭代档案**。每次修改主程序 `Mahjong_Calculator.py`（或引擎 `mahjong_core.py`）后，
> 必须同步更新本文件（迭代记录 / 功能清单 / 代码地图 / 维护约定四处），并同步更新主程序内的版本号
> （`Mahjong_Calculator.py` 的 `__version__ = "2.7.4"`；**认字符串不认行号**），
> 同时把主程序备份到 `version\` 子目录。

---

## 一、项目概况

| 项目 | 内容 |
| --- | --- |
| 程序名 | 国标麻将算番器（中国国标麻将/竞技麻将算番与听牌分析） |
| 主程序 | `Mahjong_Calculator.py`（**唯一编译源**，最终更新版；★ v2.7.3 起文件名 = 英文名） |
| 算番引擎 | `mahjong_core.py`（**v2.0.0 重建**，81 番种全部实现，见「二、」） |
| 规则数据 | `国标麻将标准规则.json`（81 番种 + 全部条款 + 问答，160 KB） |
| 界面框架 | **PySide6 (Qt6)**，Windows 桌面，中文界面，默认风格 |
| 牌面图片 | `麻将图\`（B1~B9 筒 / T1~T9 索 / W1~W9 万 / F1~F4 风 / J1~J3 箭 / empty.png 牌背；均 44×60 PNG） |
| 设置持久化 | `mahjong_settings.json`（程序目录/exe 目录，原子写入，实时自动保存） |
| 程序图标 | `icon.ico`（★ v2.4.7：红中牌样式，7 张尺寸；窗口/任务栏 + exe 文件图标都靠它） |
| 当前版本 | **2.7.4**（2026-09-25） |
| 版本备份目录 | `version\` |
| 脚手架目录 | `_scaffold\` |
| 原始资料 | `中国国标麻将标准PDF\`（2 本原书 PDF + OCR，只挑带文本层的两份） |

### 目录结构

```
Mahjong_Calculator\
├── Mahjong_Calculator.py          # 主程序（唯一编译源，PySide6 界面；★ v2.7.3 前叫 mahjong_gui.py）
├── mahjong_core.py                # 算番引擎（v2.0.0 重建，1682 行）+ ★ v2.6.0 命令行入口（--hand/--waits/--json）
├── mahjong_api.py                 # ★ Web 版（手机/平板浏览器算番）服务端 + 页面内部 JSON 路由（v2.3.0/v2.4.0/v2.6.0/v2.7.1/v2.7.4）
├── 国标麻将标准规则.json           # 规则数据源（81 番种 + 全部条款 + 166 条问答）
├── icon.ico                       # ★ 程序图标（v2.4.7，窗口/任务栏 + exe 文件图标；由 _scaffold\make_icon.py 生成）
├── mahjong_settings.json          # 运行时自动生成（设置实时自动保存）
├── 麻将图\                        # ✅ 牌面图（界面 + Web 端唯一图片来源）
│   ├── B1.png ~ B9.png            #   1筒 ~ 9筒
│   ├── T1.png ~ T9.png            #   1索 ~ 9索
│   ├── W1.png ~ W9.png            #   1万 ~ 9万
│   ├── F1.png ~ F4.png            #   东 南 西 北
│   ├── J1.png ~ J3.png            #   中 发 白
│   ├── S1.png ~ S4.png            #   ★ 花牌 春 夏 秋 冬（Web 端也能选）
│   ├── P1.png ~ P4.png            #   ★ 花牌 梅 兰 竹 菊
│   └── empty.png                  #   ★ 麻将背面（暗杠中间两张用它）
├── tiles\                         # 早期牌面图（v1.x 遗留，仅作向后兼容回退）
├── 软件设计构图\                   # ★ 界面设计图（11 张手机截图，v2.0.0 按此实现）
├── 1.bat                          # 开发运行：python Mahjong_Calculator.py
├── 0_nuitka_pyside6_clang_auto.bat # 打包（Nuitka，调用 _scaffold\build_exe.py）
├── buildozer.spec                 # 早期的 Android 打包配置（python-for-android，已停用）
├── 安卓端.txt                      # 早期 Android 排错笔记
├── msyh.ttf                       # 界面字体
├── 中国国标麻将标准PDF\            # 原始资料（PDF、OCR 文本、逐页 png）
├── version\                       # 版本备份（每次改版本号必须备份）
├── _scaffold\                     # 脚手架（提取/切分/反查/测试/校验/截图/打包）
│   ├── test_engine.py             #   算番引擎自测（73 项）
│   ├── cli_test.py                #   ★ v2.6.0 命令行 / import 算番入口测试（34 项）
│   ├── smoke_test.py              #   界面冒烟测试（221 项，含 Web/API 菜单、局域网、重置全部复位、程序图标、免口令设置、点已选牌取消）
│   ├── api_smoke_test.py          #   ★ 本地 API + Web 客户端真实 HTTP 测试（132 项，含 15 项布局/触摸断言、31 项免口令断言、5 项副露/暗杠渲染断言）
│   ├── verify_all.py              #   ★ 全量和牌枚举 + 算番校验（v2.1.0）
│   ├── error_audit.py             #   ★ 错误类别全枚举 + 故障注入（v2.2.0）
│   ├── sweep_qidui4.py            #   ★ 七对含四张：逐族完整扇扫（v2.2.1）
│   ├── indy.py                    #   ★ 第二套独立实现（对拍基准）
│   ├── golden_fans.py             #   ★ 81 个番种的金标准牌例
│   ├── shot_ui.py / shots\         #   界面截图
│   ├── make_icon.py               #   ★ 生成 icon.ico（v2.4.7；PIL + msyh.ttf，7 张尺寸各自绘制）
│   ├── probe_icon_live.py         #   ★ 真机验证窗口图标（Win32 WM_GETICON / GWL_HICON）
│   ├── probe_api_token.py         #   ★ 排查「带口令访问却 401」（端口撞车）
│   ├── probe_click_remove.py      #   ★ 真机验证「点已选的牌取消」（v2.7.2，10 项）
│   ├── build_exe.py               #   Nuitka 打包（被 0_nuitka_*.bat 调用；自动带上 icon.ico 的两个参数）
│   └── pdf_text\ qa_split\ legacy\ pyc_dump.txt   # v1.x 的资料提取中间产物
└── __pycache__\                   # 早期 mahjong_core.pyc（仅历史参考）
```

---

## 二、主程序运行逻辑（分析结果，改动前必读）

### 2.1 分层结构

| 层 | 文件/类 | 职责 |
| --- | --- | --- |
| 数据层 | `国标麻将标准规则.json` | 81 番种（分值/定义/不计/补充）+ 全部规则条款 + 问答 |
| 引擎层 | `mahjong_core.py` | 牌张编码、牌型分解（含七对/连七对/十三幺/全不靠/组合龙）、约 60 个番种判定函数、番种汇总与「不计」去重、和牌/听牌判定 |
| 引擎入口 | `mahjong_core.MahjongFanCalculator` | `score()` / `winning_tiles()` / `waiting_tiles()` / `fan_table()` + 旧接口 `calculate_fan()` 等 |
| 界面层 | `Mahjong_Calculator.MahjongFanWindow(QMainWindow)` | 牌选择区、模式按钮、选项、已选牌展示、听牌候选、算番结果、番表/用法三页 |
| **接口层** | `mahjong_api.py`（v2.3.0/v2.4.0） | 本地 HTTP JSON 接口 + **Web 客户端整页（手机/平板算番）**；主程序内嵌启动，也可 `python mahjong_api.py` 单独跑 |

### 2.2 引擎：牌张编码与核心算法

- **牌张编码**：34 个 `tid`（0~33）—— 筒 B1~B9 = 0~8，索 T1~T9 = 9~17，万 W1~W9 = 18~26，
  风 东南西北 = 27~30，箭 中发白 = 31~33。互转函数：`tile_of("W3") -> tid`、`code_of(tid)`、`name_of(tid)`。
- **牌型分解** `decompositions(concealed, melds)`：
  标准牌型（n 个面子 + 将，用 `walk_collect` 递归枚举**全部拆法**）、组合龙（9 张「147/258/369」占 3 组，
  用 `kind="long"` 的伪面子，避免污染顺子/刻子类判定）、七对/连七对/十三幺/全不靠/七星不靠（必须无副露、14 张）。
  — 七对要求 **7 个对子**，且**4 张相同的牌算作两对**（国标口径，v2.2.1 按用户裁定修正）；
  **连七对必须「同一花色 + 序数连续」的 7 个对子**（v2.2.0 修正：原来只比较牌号差 6，而牌号是跨花色连续编号，
  导致「筒6~9 + 索1~3」这类跳花色七对被误算成连七对 88 分）。
- **『和牌前手牌』判定**：`score()` 会先算出「去掉和张后的 13 张」的全部听牌类型数，
  `single_wait = len(waits) == 1`，只有单听才算 边张 / 坎张 / 单钓将（1998 领队会规定）。
  听牌判定用**快速布尔算法**（`_split_ok` / `_shape_win` / `_special_win_quick` / `_long_dragon_win`），
  不构造 `Decomp/Set_` 对象，比完整分解快约一个数量级。
- **番种汇总** `collect_fans(ctx)`：按 88 → 1 分逐级判定，可重复成立的番种（幺九刻 / 四归一 / 一般高 /
  喜相逢 / 连六 / 老少副 / 双同刻）**按实例计数**。
- **不计去重** `handle_exclusions(fans)`：以「高分优先」反复剔除被覆盖的番种；
  `EXCLUDE_KEEP = {"幺九刻"}` 表示「幺九刻」不做整项删除（改由 `_yao_jiu_covered()` 精确扣除被
  箭刻/圈风刻/门风刻/三风刻/混幺九/九莲宝灯 覆盖的刻子）；`EXCLUDE_DROP` 放《问答》要求反向保留的组合
  （如「三杠 + 双暗杠」）。
- **杠的计分**（《问答》第 41 条）：明杠 1 / 暗杠 2 / 双明杠 4 / **一明一暗 = 双明杠 4 + 暗杠 2 = 6** /
  双暗杠 6 / 三杠 32（另加暗杠分）/ 四杠 88。
- **起和**：`ok = base >= 8`（`base` **不含花牌**）；`base == 0` 时改记「无番和」8 分。
- **张数前置校验（v2.2.0）**：`score()` 先核对 `len(concealed) == 14 - 3×副露数`，
  不符直接报「手牌张数不足，无法算番（当前 N 张，应为 M 张）」，不再笼统地说「牌型不能和牌」。

### 2.3 界面结构（自上而下，与 `软件设计构图\` 一一对应）

1. **菜单栏**：`文件(F)` → 退出；`工具(T)` → 启动本地 API（Ctrl+Alt+A）/ **允许局域网访问（手机·平板）** /
   **设置访问口令…** / **API·Web 地址与用法…** / API 端口设置；`帮助(H)` → 用法说明 / 番种表 / 关于
   窗口：**默认 680×940、最小 600×560**（v2.4.3；牌池列宽 522 + 边距 ≈ 560，680 已留冗余）
2. **牌选择区**（`QGridLayout`，4 行 × 9 列）：**索 T1~T9 / 筒 B1~B9 / 万 W1~W9 / 东南西北中发白**。
   每张牌是一个 `TileButton`（左上角红色角标显示已选张数，选中加蓝框）；**左键加一张，右键减一张**。
   ★ 牌是**固定尺寸**（`POOL_SIZE` 54×71），间距就是 `horizontalSpacing = 4px`；**多余宽度靠
   `pool.setColumnStretch(第 9 列, 1)` 全部让给右侧空白**（v2.4.2）——否则 Qt 会把 9 列均分撑宽，
   出现大片缝隙（用户反馈过）。
3. **模式按钮行**：`立牌`（默认，蓝底）/ `吃` / `碰` / `明杠` / `暗杠`（互斥单选）+ `重置`（橙色）。
4. **选项区第 1 行**（`opt_row`，v2.4.1 起独占一行）：`自摸` / `和绝张` / 第 3、4 个复选框
   （**随自摸动态换文案**：勾上 = `杠上开花` + `妙手回春`，未勾 = `抢杠和` + `海底捞月`）
5. **选项区第 2 行**（`round_row`，v2.4.1 新增）：圈风 `东风圈/南风圈/西风圈/北风圈`（互斥）
6. **选项区第 3 行**（`opt_row2`）：`风位` 标签 + `东风位/南风位/西风位/北风位`（互斥）
7. **选项区第 4 行**（`flower_row`，v2.4.1 新增）：`花牌:` + `春 夏 秋 冬 梅 兰 竹 菊`
   八个可点小图（点一下选上、再点取消）+ 「N 张」计数（已选花牌另有展示行；v2.2.1 之前是 0~8 的 QSpinBox）
8. **已选牌区**（浅灰卡片）：`副露` 行（**明杠 = 4 张正面；暗杠 = 面·背·背·面**，中间两张用 `empty.png`）、
   `立牌` 行、`和张` 行、`花牌` 行，下方一行蓝色提示（还差几张 / 听几张 / 和张是谁）
9. **听牌候选区**：立牌选满（13 − 3×副露数）张时自动列出，**每个候选牌上方标该张的总番数**（不足 8 分标红），
   点击即设为和张并立刻出结果
10. **算番结果区**：`共 N 番`（大字，不足 8 分标红并提示）+ 番种列表（`N番  番种名`）
11. **底部导航**：`算番` / `番表` / `用法` 三页（`QStackedWidget`）
12. **番表页**：81 番种按分值分组（含定义与「不计」），顶部带搜索框
13. **用法页**：操作说明 / 特殊和牌方式 / 界面约定 / 算番口径

### 2.4 数据流与状态

```
TileButton 左键  → on_tile_clicked(code)
   ├─ 立牌模式 → _add_concealed(tid)：
   │     不满 (13-3×副露数) 张 → concealed[tid] += 1
   │     已满 → 这一张直接记为「和张」(win_tile)
   ├─ 吃 模式  → pending_chi 累计 3 张 → _finish_chi()（校验同花色且相连）
   └─ 碰/明杠/暗杠 → _finish_meld()（点一次即成组：3 张 / 4 张 / 4 张暗）
TileButton 右键  → on_tile_right_clicked(code)：立牌减一张 / 撤销吃的最后一张 / 取消和张

任何选项（自摸、和绝张、特殊番、圈风、风位、花牌）变更 → _on_option_changed()
                                                     └→ _refresh_all() + _autosave_settings()

_refresh_all()
   ├→ 牌区角标（含量 / 吃待选 / 和张）
   ├→ 副露行、立牌行、和张行 逐张重绘
   ├→ _update_wait_area()：13 张 → 列听牌候选 + 每张的番数
   └→ _update_result()：14 张 → calc.score(...) → 总番 + 番种列表
        （未指定和张但已满 14 张时，_guess_win_tile() 自动挑一张能和的牌）
```

- **界面状态**：`concealed: Dict[tid,int]`（立牌/暗手）、`melds: List[Meld]`（副露）、
  `win_tile: Optional[int]`（和张）、`mode: str`（五选一）、`pending_chi: List[int]`。
  这些是**临时状态，不持久化**。
- **设置状态**（v2.0.0 起全部实时自动保存）：圈风、风位、花牌张数、自摸、和绝张、
  第 3/4 个复选框、当前模式、当前页、窗口几何、窗口最大化。

### 2.5 与引擎的接口（`mahjong_core.MahjongFanCalculator`）

```python
calc = MahjongFanCalculator(rules_path=None)        # 默认读同目录 国标麻将标准规则.json
calc.score(melds, concealed, win_tile, opts) -> Score
calc.winning_tiles(melds, concealed, opts) -> List[Tuple[int, Score]]   # 只返回达 8 分的
calc.waiting_tiles(melds, concealed) -> List[int]                        # 全部听牌张
calc.fan_table() -> List[Tuple[int, List[Tuple[str, str]]]]              # 番表页用
# 旧接口（v1.x 兼容）
calc.calculate_fan(tiles: List[str], config: Dict) -> Dict[str, int]
calc.is_winning_hand(tiles: List[str]) -> bool
calc.get_possible_winning_tiles(tiles: List[str]) -> List[Tuple[str, Dict[str, int]]]
```

- `Meld(kind, tiles, concealed)`：`kind ∈ {"chi","pong","kong"}`；`concealed=True` 仅用于暗杠。
- `Options(tsumo, last_tile, rob_kong, kong_bloom, last_draw, last_discard,
  round_wind, seat_wind, flowers)`。
- `Score(fans, total, base, flowers, ok, message, pattern)`；`fans` 已按分值降序排序，
  `fan_map` / `text()` 为便捷方法。
- **张数口径（易错，踩过坑）**：`score()` 的 `concealed` 是「**包含和张在内的整手牌**」，长度必须等于
  `14 - 3×len(melds)`（和张必须包含在里面；`win_tile=None` 时取最后一张）；
  而 `waiting_tiles()` / `winning_tiles()` 传入的是「还没抓牌」的手牌，长度 = `13 - 3×len(melds)`。
  杠虽然占 4 张实体牌，但按 1 个面子（3 张）折算，所以 2 个杠时手牌是 8 张。

---

## 三、迭代记录

### v2.7.4（2026-09-25）—— 手机连 Web 版时，控制台不再刷「ConnectionResetError」噪声

**背景（用户原话）**：把一大段控制台输出贴了过来，问「这些信息正常吗？」
输出特征：

```
Exception occurred during processing of request from ('192.168.104.11', 52542)
...
  File "...\socket.py", line 705, in readinto
    return self._sock.recv_into(b)
ConnectionResetError: [WinError 10054] 远程主机强迫关闭了一个现有的连接。
```

**结论：正常，不是程序出错**。`192.168.104.11` 是局域网里的手机/平板浏览器；手机
**刷新页面 / 关标签页 / 切到后台 / 浏览器把预连接回收**时，TCP 会被直接掰断（RST），
而 Python 的 `socketserver` 会把 `rfile.readline()` 抛出的 `ConnectionResetError`
**连整段 traceback 打到控制台**。它对服务、对算番结果**毫无影响** —— 只是吓人，还会刷屏。

**改动（全在 `mahjong_api.py`）**

| 位置 | 内容 |
| --- | --- |
| 新增 `CLIENT_GONE_ERRORS`（**976**） | 统一判定「客户端提前断开」的异常：`ConnectionResetError` / `ConnectionAbortedError` / `BrokenPipeError` / `TimeoutError` |
| `Handler.handle_one_request()`（**997**） | 覆盖标准库实现，把这四类异常**直接吞掉**（`close_connection = True`）—— 用户贴的 traceback 正是从这里漏出来的 |
| 新增 `QuietHTTPServer`（**1200**）+ `handle_error()`（**1210**） | 兜底：`socketserver` 默认的 `handle_error()` 会把**任何**异常打一整屏；这里只对「客户端断开」静音，**其余异常照旧完整打印**（不掩盖真问题）。开 `--verbose` 时打一行提示「提前断开连接（正常现象，已忽略）」 |
| `ApiServer.start()`（**1256**） | 服务改用 `QuietHTTPServer` |

**验证**：`_scaffold\api_smoke_test.py` 新增 **【7c】6 项** —— 真实起服务，用
`SO_LINGER + close` 造 **4 次真 RST**（模拟手机掰连接），并把 `sys.stderr` 重定向到内存捕获：
断言控制台里**没有** `Exception occurred`、没有 `Traceback` / `ConnectionResetError`，
且断开之后服务照常 200 ⇒ **132 → 138 项全通过**。其余三套无回归
（`test_engine` 73 / `cli_test` 34 / `smoke_test` 221）。

### v2.7.3（2026-09-25）—— 英文名 / 文件名 / exe / 打包目录统一成 `Mahjong_Calculator`

**背景（用户原话）**：

> 程序的英文名，还有文件名，还有编译后的目录，统一改成 Mahjong_Calculator，中文名不变。

**统一后的命名**

| 项目 | 现在 | 原来 |
| --- | --- | --- |
| 英文名 | **Mahjong_Calculator**（新常量 `APP_NAME_EN`，119–121） | 无（只有中文名） |
| 主程序文件名（唯一编译源） | **`Mahjong_Calculator.py`** | `mahjong_gui.py` |
| exe 文件名 | **`Mahjong_Calculator.exe`** | 先编译成 `mahjong_gui.exe`，再重命名成「国标麻将算番器.exe」（**这个重命名已取消，`EXE_RENAME = ""`**） |
| 打包产物目录 | **`build_output\Mahjong_Calculator\`** | `build_output\mahjong_gui.dist\` |
| 中文名（界面显示） | 「国标麻将算番器」**不变** | 同 |
| 引擎 / Web 版模块 | `mahjong_core.py` / `mahjong_api.py` **不改** | 同（`import mahjong_core` 是给外部程序用的 API 契约，改了会断） |

**改动清单**

| 文件 | 改动 |
| --- | --- |
| `Mahjong_Calculator.py`（原 `mahjong_gui.py`） | 文件改名；新增 `APP_NAME_EN = "Mahjong_Calculator"`（**119–121**）；窗口标题 → `国标麻将算番器 Mahjong_Calculator v2.7.3`；关于页加一行英文名；`app.setApplicationName(APP_NAME_EN)` + `setApplicationDisplayName(APP_NAME)`；docstring / 帮助页 / Web 版提示里所有 `国标麻将算番器.exe` 示例 → `Mahjong_Calculator.exe` |
| `_scaffold\build_exe.py` | `ENTRY` / `EXE_NAME` / `DIST_NAME` 全改英文名；**去掉 exe 中文重命名**；编译完把 `Mahjong_Calculator.dist` **改名成 `Mahjong_Calculator`**（不带 `.dist`）；目标目录已存在时**改名保留**为 `Mahjong_Calculator_旧编译_时间戳`（不再直接删）；「exe 正在运行」检查覆盖三个历史名字；干跑自检挪到检查之前（开着窗口也能干跑） |
| `0_nuitka_pyside6_clang_auto.bat` | 输出目录提示 → `build_output\Mahjong_Calculator\`（仍纯 ASCII） |
| `1.bat` | `python Mahjong_Calculator.py` |
| `_scaffold\*`（`smoke_test` / `cli_test` / `shot_ui` / `probe_icon_live` / `probe_api_token` / `probe_click_remove`） | `import mahjong_gui as M` → `import Mahjong_Calculator as M`；`cli_test` 的 `GUI` 路径与日志文案同步 |
| `mahjong_api.py` / `mahjong_core.py` | 注释里的 exe 名 / 主程序文件名同步（**模块名不动**） |
| `国标麻将标准规则.json` | 「用途 / 说明」里的主程序文件名同步；**历史备忘段保留原文**并注明「v2.0.0 时叫 mahjong_gui.py，v2.7.3 起叫 Mahjong_Calculator.py」 |

**验证**

- 四套测试全通过：`test_engine.py` **73**、`cli_test.py` **34**（含「主程序带参数不启动界面」—— 现在跑的是 `Mahjong_Calculator.py`）、`smoke_test.py` **221**、`api_smoke_test.py` **132**。
- 打包干跑自检（`set EM_SKIP_BUILD=1`）：入口已是 `Mahjong_Calculator.py`，43 张牌面图与规则表都在参数里。
- **打包（真机实测，20:46 完成）**：Nuitka 4.0.7 先产出 `build_output\Mahjong_Calculator.dist\Mahjong_Calculator.exe`，
  脚本随后把目录改名成 **`build_output\Mahjong_Calculator\`**（不带 `.dist`），exe 就是 **`Mahjong_Calculator.exe`**
  （9,883,648 字节）；同目录带 `icon.ico` / `麻将图\` / 《国标麻将标准规则.json》/ PySide6 运行时。
  旧的产物目录没有丢，被自动保留为 `Mahjong_Calculator_旧编译_20260925_204608`。
  - 命令行实测：`Mahjong_Calculator.exe --hand "11123456789999m" --win 9m --json` → 退出码 **0**、**106 番**
    （九莲宝灯 88 + 清龙 16 + 四归一 2）；`--version` → `{"ok":true,"name":"\u56fd\u6807\u9ebb\u5c06\u7b97\u756a\u5668",…}`
    —— **中文名「国标麻将算番器」确实没变** ✓。
  - ★ 踩到的点：编译前检查 exe 是否在运行（覆盖三个历史名字），当时旧的「国标麻将算番器.exe」正开着 → 被拦下
    （`[error] 国标麻将算番器.exe 正在运行`）；关掉后再编译通过。**干跑自检 `EM_SKIP_BUILD=1` 现已挪到检查之前**，
    开着窗口也能干跑。

> 注：本文档 v2.7.3 **之前**各节里写的 `mahjong_gui.py`，就是现在的主程序文件 `Mahjong_Calculator.py` ——
> 历史记录保持原样，方便对照。

### v2.7.2（2026-09-25）—— 桌面版也能「点已选的牌取消」（不用右键，触屏可用）

**背景（用户原话）**：

> 服务器端 增加  点已经选择的牌，可以取消该牌的选择，就好像 手机屏幕那种 没有右键也可以操作的样子。

**改动（全在 `mahjong_gui.py`）**

| 位置 | 内容 |
| --- | --- |
| 新增 `CountBadge`（345） | 牌选择区的红角标从 `QLabel` 换成可点角标：**点一下 = 减一张**（与 Web 端「点红色数字 −1」一致）；尺寸 16→20（手指好点），且点击事件 `accept()` 吃掉，不会冒泡成「再加一张」 |
| 新增 `ClickTile`（372） | 「已选牌」区里可点的牌（外观与只读牌一致，多了悬停高亮 + 手型光标） |
| `TileRow`（471）改造 | 新增 `clickable=True` 与 `set_tiles(codes, payloads, tip)`：**payload 为 `None` 的牌不可点**；`clicked` 发 payload（立牌/和张/花牌 = 牌编码，副露 = 组号） |
| 新增 4 个处理函数 | `on_hand_row_clicked`（1675，点立牌一张 = 减一张）/ `on_win_row_clicked`（1688，点和张 = 取消和张）/ `on_meld_row_clicked`（1697，**点副露任意一张 = 撤销那一组**）/ `on_flower_row_clicked`（1713，点花牌行 = 取消那张花牌） |
| `_meld_items`（1829） | 副露渲染项改成 `(牌编码, 组号)`；`_meld_codes()`（1847）保留为 `[c for c, _ in _meld_items()]`（**老接口不变**）。**暗杠的牌背也可点**（点它同样撤销整组） |
| 文案 | 牌区小标题 → 「左键加一张；已选的牌点角标 −1，或点下方已选牌取消」；两处状态栏提示「右键减牌」→「点已选的牌减一张」；用法页新增一条「不用右键也能减牌」 |

**故意没改的**：右键减一张**保留**（鼠标用户习惯不变）；副露区**点一下撤销整组** —— 原来只能整个「重置」，正是「五、已知问题」里那条 P1。

**验证**

| 测试 | 内容 | 结果 |
| --- | --- | --- |
| `_scaffold\smoke_test.py` §21（新增 22 项） | 用 `QTest.mouseClick` 发**真实鼠标事件**（不是直接调 handler）：角标 3→2（不变成 4）、立牌一张被取消、**点暗杠的牌背撤销整组**、和张取消、花牌只取消被点那张；`_meld_codes` 与 `_meld_items` 一致；空状态/越界点击不崩 | **199 → 221 项全通过** |
| `_scaffold\probe_click_remove.py`（新增，**真机**） | 真实平台（非 offscreen）开窗再点一遍上面 5 类点击 —— 真机的窗口层级/几何与 offscreen 不同，红角标是牌按钮的子控件，只有真实点击才能证明「点角标不会反过来又加一张」 | **10 项全通过**（`_probe_click_out.txt`） |
| 其它 | `test_engine.py` 73 / `cli_test.py` 34 / `api_smoke_test.py` 132 | 全通过（无回归） |

截图 `_scaffold\shots\ui_*.png`（7 张）重新生成人工看过：角标放大到 20px 后仍不遮挡牌面，已选牌区的牌外观与之前一致。

### v2.7.1（2026-09-25）—— 修 Web 端「暗杠显示得跟明杠一样」

**背景（用户原话）**：

> web客户端的暗杠的显示不对，怎么跟 明杠 的一样了？

**根因**：Web 端副露区原来是**把每组副露的牌直接平铺**：

```js
mkTiles(el("m_melds"),S.melds.flatMap(m=>m.tiles),"（暂无吃碰杠）");
```

数据层一直是对的（`meldFromPool()` 暗杠走 `pushMeld("kong",[c,c,c,c],S.mode=="angang")` ⇒ `concealed=true`），
**只有渲染层压根没看 `concealed`**，顺带也丢了副露之间的间隔 ⇒ 暗杠跟明杠长得一模一样。

**改动（全在 `mahjong_api.WEB_CLIENT` 内联页，口径对齐桌面版 `_meld_codes()`）**

| 位置 | 内容 |
| --- | --- |
| 新增 `meldHtml()`（**600**） | 逐组渲染副露：**暗杠（`kind==="kong" && concealed && 4 张`）→ 面·背·背·面**（中间两张 `<img src="/tiles/empty.png">`，带 `alt/title="暗杠"`）；**明杠 / 碰 / 吃 → 全部正面** |
| 副露间隔 | 相邻副露之间插 `<span class="meldgap"></span>`（CSS **369**：`width:10px`），对应桌面版的 `GAP` 哨兵 |
| `render()`（**734**） | 改成 `el("m_melds").innerHTML=meldHtml()`（734/735 行）；空列表仍显示「（暂无吃碰杠）」 |
| 没动的地方 | 张数口径（`countUsed` / `totalTiles`，杠仍算 1 个面子）、算番请求体（`meldsBody()` 早就带 `concealed`）、牌背图路由（`TILE_FILE_RE` 一直允许 `empty.png`） |

**真机实测（Playwright 起服务 → 点「暗杠」→ 点牌 → 量 DOM 里每个 `img` 的 src）**

| 操作 | `#m_melds img` 的 src（已去掉 `?token=`） | `.meldgap` 个数 |
| --- | --- | --- |
| 暗杠 W5 | `W5.png, empty.png, empty.png, W5.png`（＝面·背·背·面 ✓） | 0 |
| 再明杠 B3 | `…, B3.png × 4`（全正面 ✓） | 1 |
| 再碰 T7 | `…, T7.png × 3`（全正面 ✓） | 2 |

截图 `_scaffold\shots\v271_melds.png`：暗杠一眼能看出「伍萬 + 两张牌背 + 伍萬」，与桌面版一致。

**验证**：`_scaffold\api_smoke_test.py` 新增 5 项（`meldHtml()` 存在且被 `render()` 调用、
暗杠 `codes=[t[0],"empty","empty",t[3]]` 且牌背走 `/tiles/empty.png`、只有暗杠盖牌、`.meldgap` 存在）
→ **128 → 132 项全通过**；`smoke_test.py` **199**、`test_engine.py` **73**、`cli_test.py` **34** 全通过。

**顺带实测：打包后 exe 的命令行算番（补 v2.6.0 的遗留验证）**

```powershell
cmd /c "`"G:\Python_Code\Mahjong_Calculator\build_output\mahjong_gui.dist\国标麻将算番器.exe`" --hand 11123456789999m --win 9m --json > out.txt 2>&1"
```

⇒ 退出码 **0**、输出 **106 番**纯 ASCII JSON（九莲宝灯 88 + 清龙 16 + 四归一 2），
与 `mahjong_core` 同一手牌一致 ⇒ **`--windows-console-mode=attach` 打包确实生效**。
★ **坑**：PowerShell 直接 `& exe --hand … --json` **抓不到 stdout**（attach 模式下输出走父控制台，
工具里看着像「什么都没打印」）；要 `cmd /c "… > out.txt 2>&1"` 重定向，或直接在 cmd 窗口里跑。
注：该 dist 是本次改动**之前**编译的，要分发含 v2.7.1 的 exe 需重跑 `0_nuitka_pyside6_clang_auto.bat`。
★ v2.7.3 起产物改名了：目录 `build_output\Mahjong_Calculator\`、exe `Mahjong_Calculator.exe`
（上面这条实测是旧名字时的记录，保留原样作对照）。

### v2.7.0（2026-09-25）—— Web 版新增「自适应」布局（与经典布局共存，默认仍是经典）

**背景（用户原话）**：

> 手机或者平板使用的时候，增加自适应大小的选择，避免过大或者过小的情况。
> 就是先保留现在的布局，然后新增 自适应 的布局，避免新的布局还没好，旧的布局也用不了的尴尬情况。

**改动（全在 `mahjong_api.WEB_CLIENT` 内联页）**

| 位置 | 内容 |
| --- | --- |
| CSS 尺寸变量化（关键） | 牌池外框 `--tw/--th`、牌图 `--pw/--ph`、花牌 `--fw/--fh/--fwi/--fhi`、候选小图 `--ww/--wh`、间距 `--gw/--pgap`；**默认值逐个像素等于老版本**（38/52、34/46、34/46、28/38、4/3）⇒ 「经典」布局与升级前**完全一致** |
| 两套布局共存 | `S.layout`（`"fixed"` 默认 / `"auto"`）+ `body.auto` 类；模式行新增按钮「布局：经典 ⇄ 自适应」，选择写 `localStorage.mj_layout`，刷新/下次打开都记得 |
| 自适应算法 `autoSize()` | 可用宽 = `min(视口宽, 760) - 42`；先按「一行 9 张」算，再受高度上限 `min(视口高×0.115, 74)` 约束，最后夹在 **20~46px**；花牌按 0.9、候选小图按 0.83 同比例缩放 |
| 经典还原 | 切回经典时 `LAYOUT_VARS.forEach(k=>root.style.removeProperty(k))` —— 清掉 JS 写的内联变量，回落到 CSS 默认值（像素级还原） |
| 自适应跟随 | `resize` / `orientationchange` → 120ms 防抖重算 |

**真机实测（Playwright 改视口，量 `#pool .tile` 外框 / 牌图）**

| 屏幕 | 布局 | 外框 | 牌图 | 花牌图 |
| --- | --- | --- | --- | --- |
| 768×1024（平板） | 自适应 | 52×71 | **46×63** | 41×57 |
| 390×844（手机） | 自适应 | 40×54 | 34×46 | 31×41 |
| 360×640（小屏） | 自适应 | 37×50 | 31×42 | 28×38 |
| 360×640 | **经典** | 38×52 | **32×44** | **28×38**（＝老版本原值） |

另：切成自适应后**刷新仍记得**（`localStorage.mj_layout="auto"`）；点回经典立刻变回 38×52。

**验证**：`_scaffold\api_smoke_test.py` 新增 10 项 → **118 → 128 项全通过**（CSS 变量默认值＝经典像素值、
各处尺寸都走变量、切换按钮、默认 fixed、localStorage 键、自适应算法与夹紧区间、经典还原、resize 监听）；
`smoke_test.py` **199**、`test_engine.py` **73**、`cli_test.py` **34** 全通过。

### v2.6.0（2026-09-25）—— 算番入口回到「引擎 + 命令行」（不要 HTTP / 端口）；Web 版默认免密码

**背景（用户原话）**：

> 别的程序（Python / C# / Excel / 什么语言都行）直接 `import` 或者 `subprocess` 调用这个 exe/py，
> 把牌一传，番数拿回来，就完事。不需要 HTTP、不需要端口、不需要先启动谁。
> 我要的「API」就是 `mahjong_core.MahjongFanCalculator`。
> web 客户端可以完全取消需要密码才能访问。

**改动**

| 位置 | 内容 |
| --- | --- |
| `mahjong_core.py`（新增命令行入口） | `python mahjong_core.py --hand "11123456789999m" --win 9m --json` —— 一次调用算完就退出。牌支持三种写法（`W1` / `一万` / `11123456789999m` 简写：m 万 s 索 p 筒 z 字牌）；副露 `--meld chi:123m` / `peng:11z` / `kong:5555z` / `angang:7777z`；听牌 `--waits` / `--waits-all`；选项 `--tsumo --last-tile --rob-kong --kong-bloom --last-draw --last-discard --round --seat --flowers`；另有 `--text`（人话）、`--fan-table`、`--version`、`--help`。**退出码**：0 成功 / 1 不能和牌 / 2 参数或牌面有错 |
| 输出格式 | 默认 JSON，且 **`ensure_ascii=True`（纯 ASCII，中文写成 `\uXXXX`）** —— C#/Excel/批处理按任何编码读都不会乱；`--text` 才是带中文的人话（stdout 会尝试切 UTF-8） |
| `mahjong_core.score_hand()` / `score_codes()`（新增） | 一行算番的便捷函数（返回 JSON 友好 dict），给能 import 的语言用：`score_hand("11123456789999m", win="9m")` |
| `mahjong_gui.py`（主程序 = 打包后的 exe） | `main()` 里检测到算番参数就**走命令行、不创建 QApplication** ⇒ `国标麻将算番器.exe --hand ... --json` 可用 |
| `_scaffold/build_exe.py` | `--windows-console-mode` 由 `disable` 改成 **`attach`**：双击仍是纯 GUI（不弹黑窗），但被 cmd/subprocess 调用时能挂到父控制台，`--json` 的输出才看得见（`disable` 会让 exe 彻底丢弃 stdout）★ 这是「exe 能不能被调用」的关键一条 |
| GUI 菜单重新定位 | 「启动本地 API」→「**启动 Web 版**」；「API 端口设置」→「Web 版端口设置」；「API / Web 地址与用法」→「Web 版地址与用法」。tooltip、提示框、帮助页都写明：**给程序用的 API 是 `mahjong_core`，这台 HTTP 服务只为浏览器存在** |
| Web 版默认免密码 | `_api_lan_toggle` **不再自动生成 6 位口令**（勾上局域网后手机直接打开就能用）；口令仍是可选项（工具 → 设置访问口令），免口令白名单照旧 |
| `mahjong_api.py` | 文档重新定位为「Web 版（浏览器算番）的服务端 + 页面内部 JSON 路由」，开头指明程序间调用请用 `mahjong_core` |

**验证**

- 新增 `_scaffold\cli_test.py` —— **34 项全通过**：
  - `mahjong_core.py` 命令行 22 项：九莲宝灯 106 番、中文/简写写法、省略 `--win` 取最后一张、听牌 9 张、
    `--waits-all`、副露（明杠+暗杠）听西、副露算番 26 番、`--tsumo`、`--version`、`--fan-table`、`--help`、
    **退出码 1（不能和牌）/ 2（牌数不对、认不出的牌、带副露牌数不对）**，并断言输出**是纯 ASCII**。
  - 主程序（= 打包后的 exe）4 项：`--hand ... --json` 得 106 番、**1.7 秒结束（证明确实没启动界面）**、
    `--version`、`--waits --text`。
  - import 4 项：`MahjongFanCalculator().score()` 106 番、`score_hand()`（简写/列表/中文/副露）一致、
    `winning_tiles()` 直接可用。
  - 「不依赖服务」2 项：`mahjong_core.py` 源码里**没有任何 socket / HTTPServer 代码**。
- `test_engine.py` **73** / `smoke_test.py` **199** / `api_smoke_test.py` **118** 全部通过
  （§17b 改成验证：局域网**不再自动生成口令** + 免口令直连 200 + 设了口令仍 401 / 带 token 200）。
- 打包：Nuitka 4.0.7 用 `--windows-console-mode=attach` 重新编译，exe 命令行算番实测（见下）。

**exe 实测（打包后）**

```powershell
# 编译产物：build_output\Mahjong_Calculator\（整个目录一起分发；★ v2.7.3 前叫 mahjong_gui.dist）
.\Mahjong_Calculator.exe --hand "11123456789999m" --win 9m --json
.\Mahjong_Calculator.exe --hand "11123456789999m" --win 9m --text
```

（下面「测试与验收」一节里写了这台机器上的实测结论。）

### v2.5.0（2026-09-25）—— 免口令访问白名单（可逐项放行，含 Web 客户端）

**背景**：用户提出「**增加设置，可以分别允许哪些不使用密码也可以使用，包括 web 客户端**」——
v2.4.x 是一刀切：只要设了口令，就是「Web 页面/牌面图免口令、`/api/*` 全部要口令」。

**改动（`mahjong_api.py` + `mahjong_gui.py`）**

| 位置 | 内容 |
| --- | --- |
| `mahjong_api.ANON_GROUPS`（新增） | 五组白名单：`web`（Web 客户端页面 + 牌面图 / manifest / favicon）、`debug`（`/debug`）、`info`（`/api/health`·`version`·`tiles`·`fan_table`·`help`）、`score`（`/api/score`）、`waits`（`/api/waits`·`waits_all`·`wins`）；`ANON_DEFAULT = ("web","debug")`（＝旧行为） |
| `Handler._anon_ok()`（新增） | 路径命中白名单即放行（`*` 结尾＝前缀项，否则精确相等）；`_check_token()` 先看它，口令为空时本就全放行 |
| `ApiServer(..., anon=…)`（新增参数） | 注入到 handler 类；**`anon=None` = 用默认（与旧版一致）；`anon=()` 才是「全部都要口令」** |
| `do_GET` 静态分支 | 口令检查提到静态资源**之前**（原来静态一律免口令）—— 这样「关掉 Web 客户端」才真的关得掉（用 `?token=` 打开） |
| Web 页 `img()/imgClk()` | 牌面图地址改成 `q("/tiles/xx.png")` **自动带口令** ⇒ 即使把 Web 客户端设成「要口令」，`?token=` 打开页面时**牌面图也不会裂** |
| 命令行 | 新增 `--anon web,info`（`none` = 全部要口令），启动时打印当前免口令项 |
| `mahjong_gui` 菜单 | 新增「工具 → **免口令访问设置…**」→ `AnonAccessDialog`（每项一个复选框 + 灰色说明 + 「全部允许 / 全部要口令」+ 确定/取消） |
| 设置持久化 | 新键 `api_anon`；三处成对（`_collect_settings` 收集 / `_apply_settings` 应用 / 改完 `_autosave_settings()` 落盘）。**老设置没有这个键 → 回默认，行为不变** |
| `_anon_text()`（新增） | 状态栏/提示里说人话：「未设口令 → 所有访问都不需要口令」/「全部需要口令」/「免口令：Web 客户端页面、算番接口」 |

**验证**

- `_scaffold\api_smoke_test.py` 新增【7b】**31 项** → **87 → 118 项全通过**：
  默认值/分组表、`anon=None` vs `anon=()`、只开 `info`（health·tiles 免口令 200、score 401、首页也要口令、
  带 `?token=` 首页 200、牌面图带 token 可取）、只开 `score`+`waits`（POST / GET 简写 / `waits_all` 都免口令、
  health 仍 401）、全关（连首页都要口令）、全开、**没设口令时 anon 不起作用**、未知分组键被忽略且不会误放行。
- `_scaffold\smoke_test.py` 新增 **§20 共 27 项** → **171 → 198 项全通过**：
  默认 `["web","debug"]`、键名与 `ANON_GROUPS` 一致、对话框勾选/顺序固定/一键全选全不选、
  `_collect_settings` 含 `api_anon`、`_anon_text` 三种文案、菜单项存在、**真起服务**验证
  「勾了 web → 首页免口令 / 没勾 info → `/api/tiles` 401 / 加上 info+score → 免口令 200（算番 106 番）/
  没勾 debug → `/debug` 401」、`_api_set_anon()`（假对话框）改完落盘 + 服务按新设置重启、
  新窗口恢复、**老设置回默认**。
- **真机浏览器**（`mahjong_api.py --anon none` = 全部要口令，浏览器开 `http://127.0.0.1:8721/?token=1234`）：
  页面 42 张牌图**全部加载成功**（图片 URL 形如 `/tiles/T1.png?token=1234`）、点牌池加一张 → 角标 2、
  点角标减牌 → 1 ✓（改 `imgClk` 的 src 后点击仍正常）。

**踩到的坑（已记入 `_scaffold\README.md` 与维护约定）**

1. **`ANON_GROUPS` 里的 `"/"` 必须当「精确项」**：一开始写成「以 `/` 结尾＝前缀匹配」⇒ `"/"` 前缀命中了
   一切路径 ⇒ 免口令形同虚设（`api_smoke_test` 当场抓到；已改成前缀项显式写 `*`）。
2. **`ApiServer` 的 `anon` 默认值语义**：`None` 必须表示「用默认（旧行为）」，只有显式传 `()` 才是
   「全部要口令」；否则老调用方与老测试会一夜之间全变 401（实测踩了两轮）。
3. **测试不要遍历 `win.menuBar().actions()[1].menu().actions()`**：会撞到已失效的 QMenu 包装，
   报 `RuntimeError: Internal C++ object (PySide6.QtWidgets.QMenu) already deleted`；
   菜单 action 一律存成窗口属性（`self.act_api_anon`）再引用。

### v2.4.7（2026-09-25）—— 程序图标 icon.ico（窗口 / 任务栏 + exe 文件图标）

**背景**：用户提出「**程序图标 icon.ico**」。

**改动**

| 位置 | 内容 |
| --- | --- |
| 新增 `icon.ico`（项目根） | 红中牌样式（蓝底 `#2f7ff0` + 白牌面 + 红「中」，与界面主色一致），**7 张尺寸各自单独绘制**（16/24/32/48/64/128/256，不是把同一张缩放，小尺寸下才不糊）；手写 ICO 容器，每张都是 PNG 条目（Qt 与 Nuitka 都认） |
| 新增 `_scaffold\make_icon.py` | 生成脚本（PIL + 项目自带 `msyh.ttf`）；顺带导出 `icon_preview.png` 便于人工看；Qt 自检结果写 `_icon_out.txt`。★ 脚本里做 Qt 自检**必须先建 `QGuiApplication`**，否则 `QIcon` 会 `qFatal` 直接终止进程（本次踩到，日志都没写出来） |
| `mahjong_gui.app_icon()`（新增） | 依次在 **exe/程序目录**（`app_dir()`，打包后＝exe 同目录）下找 `icon.ico`、`data\icon.ico`、脚本同目录；带缓存；**找不到/读坏 → 安静返回空 QIcon**（Qt 用默认图标），绝不因为它让程序起不来 |
| `main()` | `app.setWindowIcon(app_icon())` —— 应用级图标（任务栏分组 / Alt+Tab） |
| `MahjongFanWindow.__init__` | `self.setWindowIcon(app_icon())` —— 窗口级图标。**两处缺一就会出现「exe 有图标但窗口没有」** |
| `_scaffold\build_exe.py` | 原本已有该逻辑：`icon.ico` 存在时自动加 `--windows-icon-from-ico=icon.ico`（exe **文件自身**的图标）**和** `--include-data-files=icon.ico=icon.ico`（让运行时也能读到 → 窗口/任务栏图标） |

**验证**

- `_scaffold\smoke_test.py` 新增 **§19 程序图标（13 项）** → **158 → 171 项全通过**：
  文件存在且 >1KB、`app_icon()` 非空且含 16 与 256、**带缓存（同对象）**、窗口 `windowIcon()` 非空且与 `app_icon()` 同源、
  **图标缺失时安静降级为空 QIcon 且不抛异常**（临时把 `HERE`/`resource_path` 指到空目录再还原）、
  渲染出的 32×32 位图**有非透明像素**、源码级断言（`main()` 与窗口两处 `setWindowIcon`、打包脚本的两个图标参数）。
- `_scaffold\probe_icon_live.py`（新增，**真机**）→ `WM_GETICON` / `GWL_HICON` 都拿到非 0 句柄
  ⇒ **Windows 真的挂上了图标**（任务栏、标题栏左上角、Alt+Tab 都取这个句柄）。
- `test_engine.py` 73 项、`api_smoke_test.py` 87 项 均全通过（无回归）。
- 真机图标检查结果：`app_icon()` 7 张尺寸齐全、`pixmap(32×32)` 有 59 个非透明采样点、窗口图标与之一致。

**顺带查清一个测试「假失败」**（与图标无关，但这次被撞上了）

- 现象：`§17b 带 token 访问数据接口 → 200` 连续两次返回 **401**，而同一服务「无 token → 401」却通过。
- 排查：新增 `_scaffold\probe_api_token.py` 单独复现同一流程 → 全部 200；`netstat -ano` 也看不到 8718 的 LISTENING。
- 根因：**Windows 的 `SO_REUSEADDR` 允许同一端口被重复绑定**（Python `HTTPServer.allow_reuse_address = 1`）。
  机器上若另有真实运行的服务（或没退干净的旧实例）也听着 8718，我们的服务**照样能启动成功**，
  但请求会被**随机投给其中一台** ⇒ 出现「带着正确口令却仍 401」这种毫无道理的失败。
- 修法：`smoke_test.py` 从 §17 起改用 `free_port()` 取**空闲随机端口**，断言加**最多 3 次重试**，
  失败时把窗口侧/服务侧 token 与端口一起打印出来（便于定位而不是掩盖）。改完连跑 4 次全部 **171/0**。
- 经验写进 `_scaffold\README.md` 环境注记第 27 条。

### v2.4.6（2026-09-25）—— Web 端减牌入口改造成「手机能用」（不再依赖右键）

**背景**：用户指出「**web 是考虑手机使用，手机使用就没有右键点击这种说法的了**」——
v2.4.4 重排后牌池提示仍是「点一下加一张，长按 / 右键减一张」，而手机既没有右键，
长按又会跳出系统菜单 / 文字选择。

**改动（`mahjong_api.WEB_CLIENT`）**

| 项 | 原来 | 现在 |
| --- | --- | --- |
| 减牌入口 | 长按 450ms / 鼠标右键 | **点牌上的红色数字（角标）＝ −1**（单指即可）＋ **在「已选牌 → 立牌」里点一张也减一张**（和张同理可点掉） |
| 角标 | 纯显示 | 变成按钮：`cursor:pointer`、`pointer-events:auto`、按下缩放反馈、白描边、18px（手指好点） |
| 事件 | 点角标会冒泡成「加一张」 | `bindMinus()` 在角标上拦下 `mousedown/mouseup/click/touchstart/touchend/contextmenu`（`stopPropagation`），只有 click 走 `minus(code)` |
| 长按副作用 | 会弹系统菜单 / 选中文字 | `.tile{-webkit-touch-callout:none;user-select:none;touch-action:manipulation}` |
| 文案 | 「点一下加一张，长按 / 右键减一张」 | 「点一下加一张；点牌上红色数字 −1」＋ 提示行「手机：点牌加一张，点牌上的红色数字减一张（也可以在「立牌」里点一张减）」 |

- 长按 / 右键**仍然保留**（鼠标用户习惯不变），只是不再是唯一入口。
- Web 端「重置＝全部复位」与桌面版一致，保持不变。

**验证**（真机浏览器点击）：① 点 W1 三次 → 角标 3、立牌 3 张；② **点红色角标** → 角标 2、立牌 2 张
（**没有误加成 4**）；③ **点「已选牌 → 立牌」里那张** → 角标 1、立牌 1 张；
④ 样式实测：角标 `cursor:pointer` / `pointer-events:auto`，牌 `user-select:none`。
`_scaffold\api_smoke_test.py` 新增 **5 项**断言（角标是可点的 −1、事件被截住、已选牌可点减、
文案不再依赖右键、禁掉长按菜单/选择）→ **82 → 87 项全通过**。

### v2.4.5（2026-09-25）—— 修 Web 端牌池变成「一行一只」（CSS 类名漏掉）

**背景**：用户看 Web 版截图提问「**web 客户端的牌区为什么跟服务器端一样？为什么是一行一只牌？**」——
v2.4.4 重排 Web 版 HTML 时，牌池容器写成了 `<div id="pool">`，**丢了 `class="pool"`**；
而牌池行样式是 `.pool .line{display:flex}`（靠**祖先类选择器**）⇒ 行 `<div class="line">` 拿不到 flex，
里面每张 `.tile` 是块级 flex 容器，于是**每张牌各占一行**（竖着一列）。

**修法（`mahjong_api.py`，两处都补上，双保险）**

1. CSS 增加**直接命中 `#pool`** 的规则（不再依赖祖先类）：
   `#pool .line,.pool .line{display:flex;gap:3px;flex-wrap:wrap;margin:0 0 3px}`（最后一行 `margin-bottom:0`）。
2. HTML 容器改成 `<div class="pool" id="pool"></div>`。

**验证**（真机浏览器读 DOM 几何）：牌池 **4 行**，每行 **9 / 9 / 9 / 7** 张（索 / 筒 / 万 / 字牌），
**同一行内所有牌 top 坐标相同**（确实是横排），牌 38×52；
`_scaffold\api_smoke_test.py` 新增 **2 项**断言（`#pool .line` 必须是 flex 横排、容器必须带 `.pool` 类）
→ **80 → 82 项全通过**。

> **教训（已写进维护约定）**：Web 端样式大量使用「祖先类选择器」（`.pool .line`、`.row .lb`、`
> .tiles img`），重排 HTML 时**类名很容易被顺手删掉**，表现就是「布局突然变成一列」。
> 改完必须真机看，并且**关键样式尽量用 `#id` 再写一份兜底**（像这里 `#pool .line`）。

### v2.4.4（2026-09-25）—— 「重置」改成全部复位 + Web 客户端布局对齐桌面版

**背景**：① 用户反馈「**「重置」点了以后，全部应该重置，为什么没有完全实现？还得自己复位吗？**」——
原 `on_reset()` 只清手牌 / 副露 / 和张 / 待选 / 模式，**花牌、四个复选框、圈风、风位都不动**，也不落盘；
② 用户要求「**web 客户端的界面设置布局应该跟服务器端一样**」—— Web 端原来把「已选牌/听牌/结果」
放在最上面、牌池在最下面，花牌还混在牌池第 5 行。

**一、桌面版「重置」= 全部复位**（`mahjong_gui.on_reset`，v2.4.4 在第 1296 行）

| 原来只清 | 现在额外清 |
| --- | --- |
| 手牌 `concealed` / 副露 `melds` / 和张 `win_tile` / 待选 `pending_chi` / 模式 → 立牌 | **花牌 `flowers`（全取消）**、**自摸 / 和绝张 / 抢杠和（杠上开花）/ 海底捞月（妙手回春）四个复选框全不勾**、**圈风回「东风圈」**、**风位回「东风位」**、复选框文案复位（回抢杠和/海底捞月）、状态栏提示、**立即落盘**（`_autosave_settings()`，重启也是干净的） |

- 实现要点：用 `blockSignals(True/False)` 把 7 个控件一次性复位（否则每改一个都会触发重算 + 存盘），
  最后只 `_refresh_all()` + `_autosave_settings()` 各一次。
- `smoke_test.py` 新增第 18 节「重置 = 全部复位」：先造出「手牌 + 副露 + 和张 + 花牌 + 4 个复选框 +
  南风圈 + 北风位 + 碰模式」，再点重置 → **19 项断言**（含落盘、新实例重启干净），
  测试总数 **141 → 160 项全通过**。

**二、Web 客户端布局与桌面版逐块对齐**（`mahjong_api.WEB_CLIENT`）

| 位置 | 原来（Web 端） | 现在（＝桌面版顺序） |
| --- | --- | --- |
| ① | 已选牌 | **牌选择区**（索 / 筒 / 万 / 字牌 **4 行**） |
| ② | 听牌候选 | **模式行**（立牌/吃/碰/明杠/暗杠 + **重置**） |
| ③ | 算番结果 | **选项区固定四行**：□自摸/□和绝张/□抢杠和·杠上开花/□海底捞月·妙手回春 → 风圈 → 风位 → **花牌（8 张小图 + 「N 张」）** |
| ④ | 模式 + 选项（3 行） | 已选牌（副露 / 立牌 / 和张 / 花牌 + 提示行） |
| ⑤ | 牌池（5 行，含花牌） | 听牌候选 |
| ⑥ | 重置等按钮（在牌池卡片里） | 算番结果 |

- **花牌从牌池移出**：与桌面版一样只出现在选项区第 4 行（`#flowers`）：
  `buildFlowers()` 只建一次 + `updateFlowers()` 只改选中态，点一下选/再点取消；
  顺序固定为 **春夏秋冬梅兰竹菊**（`FLOWER.filter(...)`），右侧显示「N 张」；
  已选花牌仍在「已选牌 → 花牌」行展示。
- 选项区按钮加 `☐/☑`（对应桌面版 QCheckBox）与 `○/●`（对应 QRadioButton）前缀，
  一眼能看出哪些是多选、哪些是单选。
- **Web 端「重置」也改成全部复位**（花牌 + 选项 + 圈风风位 + 模式 + 手牌），与桌面版一致。
- 底部按钮改为「↑ 牌池 / ↓ 结果」（牌池已在顶部）。
- `_scaffold\api_smoke_test.py` 新增 **8 项**布局断言（选项四行齐全、花牌已移出牌池、
  四块顺序、花牌行只建一次、花牌固定顺序、重置全部复位、底部按钮文案），测试总数 **73 → 81 项**。

### v2.4.3（2026-09-25）—— 窗口收窄（默认 680×940）+ 旧几何一次性迁移；校验脚本改为「完整检验（不抽样）」

**背景**：① 用户反馈「**界面不用这么宽，好像图二的宽度也可以，是不是？或者再留点冗余就差不多了**」——
牌池已经收拢（v2.4.2），窗口却还是 1180×960、最小宽度 1020，右边一大片空白；② 用户要求
「**不要抽样，要完整检验，避免稀有错误漏检**」——原 `verify_all.py` 有 4 处随机抽样（七对 12 万、
七对含四张 1~2 个四张 12 万、对拍 12 万、和张扫描 1500 手）。

**一、窗口尺寸（`mahjong_gui.py`）**

| 项 | 原来 | 现在 |
| --- | --- | --- |
| 默认尺寸 | `1180 × 960` | **`680 × 940`**（常量 `WIN_W, WIN_H`） |
| 最小尺寸 | `1020 × 700` | **`600 × 560`**（常量 `MIN_W, MIN_H`） |

- 依据：牌池 9 列 × 牌宽 54 + 间距 4 = **522px**，加左右边距 24 + 滚动条 ≈ **560px**；
  所以 680 已是「够用 + 一点冗余」（用户原话「再留点冗余就差不多了」）。
  实测 680 宽下最靠右的元素是牌池的「9 万」（右边界 529），窗口还余约 150px。
- **旧几何一次性迁移**：设置里新增 `geo_ver`（= `GEO_VER` 2）。`_apply_settings` 恢复几何时，
  若 `geo_ver < 2`（旧版存下的；旧版最小宽度就是 1020，所以必然是宽的）→ **只把宽度收紧到
  `WIN_W`**，高度/位置保留；用户手动拉宽后会存成 `geo_ver=2`，以后不会再被动收紧。
- 截图脚本 `_scaffold\shot_ui.py` 原来硬编码 `resize(1180, 980)`（截图比真实默认宽得多），
  改为 `resize(M.WIN_W, M.WIN_H)`，以后截图就是真实默认外观。
- 实测（离屏读坐标）：默认 680×940；旧几何 `[100,80,1020,960]` → 恢复为 **680×960**；
  `geo_ver=2` 的 `[100,80,900,800]` → 原样 **900×800**；保存后 `geo_ver=2` 落盘；
  680 宽下所有控件右边界 ≤ 529、选项区仍是 **4 行**；`smoke_test.py` **141 项全通过**。

**二、校验脚本改为「完整检验（不抽样）」（`_scaffold\verify_all.py`）**

- **牌型空间 100% 穷尽**（原来有 4 处抽样）：

| 族 | 原来 | 现在 |
| --- | --- | --- |
| 标准型（4 面子 + 将） | 全量 | 全量 |
| 组合龙 / 十三幺·连七对·全不靠·七星不靠 | 全量 | 全量 |
| 七对 | 子空间 480,700 + 随机 12 万 | **全空间 C(34,7) = 5,379,616** |
| 七对含四张 k=3 | 全量 185,504 | 全量 185,504 |
| 七对含四张 k=2 | 随机抽样 | **全量 2,782,560** |
| 七对含四张 k=1 | 随机抽样 | **全量 8,069,424** |

  → 合计约 **2842 万手**，逐手校验（不再有随机抽样）。
- **内存安全**：全部改成生成器 + 分批（`--batch`，默认 30 万手/批），不再一次性构造上千万手的
  list（那样会吃到 2~3 GB）；进度写 `_verify_progress_<阶段>.json`，**中断重跑自动续跑**
  （`--restart` 才从头跑）。
- **阶段化**（`--phases`）：`main`（逐手自洽 + 独立实现对拍）/ `waits`（逐手听牌对拍）/
  `score`（逐手公开接口 `score()`）/ `sweep`（每一手的**每一个和张** × 点炮/自摸）/
  `neg`（逐手 2 个确定性变异）/ `meld`（副露手牌）/ `golden`（81 番种金标准）。
  默认 `main,waits,score,neg,meld,golden`；`--phases all` 把 sweep 也带上。
- **族顺序改为「稀有族优先」**：组合龙 → 特殊牌型 → 七对含四张 k=3 / k=2 / k=1 → 七对 → 标准型。
  这样标准型（约 1150 万手，最大的一块）还没跑完，**稀有牌型的完整结论就已经先出来了**。
- **只保留「两边都实现」的对拍维度**：新脚本一开始把「终局不计表」与「自摸口径」也拿来跨实现
  对拍，结果每批都刷差异 —— 查清后是**口径差、不是缺陷**：① `indy.ref_final` 只按规则 JSON 的
  「不计」字段剔除，而引擎另有 `EXTRA_EXCLUDES`（必然并存补充表）；② indy 的 raw 不抑制
  「已计不求人不再计自摸」（它交给终局表）。这两项因此从对拍里去掉（终局口径由引擎自洽检查 +
  81 番种金标准 + `error_audit.py` 覆盖），并用 `_norm_ind()` 显式记录这条例外。
  **教训：跨实现对拍只能比「双方都实现的口径」，否则噪音会淹没真实缺陷。**
- 实测吞吐（每进程）：`main` ≈ **123 手/秒**、`waits` ≈ 173 手/秒、`score` ≈ 126 手/秒
  → 2842 万手整轮在 4 核机器上约需**数十小时**，可分批/过夜跑，随时中断续跑。
- 原「抽样快速版」保留：`--quick` / `--sample`。
- `--families` 可按族名关键字筛选（例如 `--families "组合龙,特殊,k=3"`），用于分族分批跑。

### v2.4.2（2026-09-25）—— 牌选择区牌与牌之间不再被撑开（按用户反馈）

**背景**：用户看界面截图后反馈「**牌与牌之间不需要间隔这么宽**」——牌池里 9 张牌之间留了大片空白。

**原因（不是间距参数写大了）**：`TileButton` 是 `setFixedSize(POOL_SIZE + 6)` 的固定尺寸，牌池
`QGridLayout` 的 `horizontalSpacing` 本来就只是 **4px**；但因为**没有任何一列设置拉伸**，
窗口（更准确说是滚动区）比牌池宽时，Qt 会把多出来的宽度**平均分给 9 列**，
每列变宽后牌在列里居中 ⇒ 视觉上就变成「牌与牌之间很宽的缝」。窗口越宽缝越大。

**修法（`_build_calc_page` 第 524 行）**：

```python
pool.setColumnStretch(max(len(row) for row in POOL_ROWS), 1)
```

在**第 9 列（最后一行字牌只有 7 张，这一列本来就是空的）**上挂拉伸因子 1 ⇒ 全部剩余宽度被这一列
吸收，牌只按自己的固定宽度 + 4px 间距从左往右排，多余空白全留在右侧。

**验证（离屏渲染后读控件坐标）**：

- 索行 x = `12 / 70 / 128 / 186 / 244 / 302 / 360 / 418 / 476`，**相邻间距恒为 58 = 牌宽 54 + 4**；
- 索 / 筒 / 万三行 x **完全一致**（列对齐）；字牌行 7 张同样 4px 间距；
- 1180 宽窗口下牌池右边界 530，其余宽度全在右侧（不再被均分）；
- `_scaffold\smoke_test.py` **141 项全通过**、`_scaffold\test_engine.py` **73 项全通过**；
- `_scaffold\shot_ui.py` 重新生成 7 张截图人工确认。
- 版本号：`mahjong_gui.py` → `2.4.2`；备份 `version\mahjong_gui - 2.4.2 选项区四行 牌池紧凑.py`。

> 同类注意：**凡是「固定尺寸的牌铺在 QGridLayout 里」的地方，都要给一个空列（或空行）挂拉伸**，
> 否则就会被均匀撑开。牌池现在只有 4 行，最后一行 7 张，正好用第 9 列当「吃剩余宽度」的空列。

### v2.4.1（2026-09-25）—— 算番页选项区改为四行（按用户设计图）+ 引擎口径补充修正

**背景**：① 用户给出设计图：「**自摸哪里的布局我想改成这样**」——原来把 4 个复选框和 4 个风圈挤在
同一行、风位和 8 张花牌挤在另一行，字挤在一起看不清；② 上一轮（v2.2.2 口径修正）只改了引擎、
没升版本号也没进文档，一并在本版补记。

**一、界面：选项区固定四行**（`mahjong_gui.py` → `_build_calc_page`，第 565–635 行）

| 行 | 内容 | 布局对象 |
| --- | --- | --- |
| 第1行 | `□ 自摸` `□ 和绝张` `□ 抢杠和`（勾自摸变「杠上开花」）`□ 海底捞月`（勾自摸变「妙手回春」） | `opt_row` |
| 第2行 | `○ 东风圈` `○ 南风圈` `○ 西风圈` `○ 北风圈` | `round_row`（**新增**） |
| 第3行 | `风位` ＋ `○ 东风位` `○ 南风位` `○ 西风位` `○ 北风位` | `opt_row2` |
| 第4行 | `花牌:` ＋ 8 张小图（**春 夏 秋 冬 梅 兰 竹 菊**）＋ 右侧张数（如 `8 张`） | `flower_row`（**新增**） |

- **控件属性名与信号连接一律未改**（`cb_tsumo` / `cb_last_tile` / `cb_special_a` / `cb_special_b` /
  `wind_round_group` / `cb_round` / `wind_seat_group` / `cb_seat` / `btn_flowers` / `lbl_flower_count`），
  因此 `_refresh_all` / `_apply_settings` / `_connect_autosave` / `_options` 以及三套测试**零改动**。
- 花牌按钮间距由 14 收窄到 6（8 张图一行放得下），张数标签与最后一朵花之间留 4px。
- 文件头 docstring 补了这四行的说明（供后来者一眼看懂布局约定）。
- 实测（离屏渲染后读控件坐标）：四行 y = **399 / 424 / 449 / 488**，四行各不相同；
  同行内 x 递增顺序为 **春<夏<秋<冬<梅<兰<竹<菊**（= `FLOWER_CODES` 顺序）。

**二、补记：引擎口径补充修正（v2.2.2，本次一并进文档）**

| 修正 | 原来 | 现在 | 依据 |
| --- | --- | --- | --- |
| **大三元 不再在七对里计** | `all_counts[d] >= 3` 会把「4 张相同的牌」当成刻子 → 七对牌型多算 **88 分** | `fan_da_san_yuan` 遇 `ctx.special`（七对/连七对/十三幺…）直接返回 False | 全量 `verify_all.py` 跑出 **7 例**「引擎={大三元:88} 独立={}」差异 |
| **清幺九 接受七对** | 只在标准型/碰碰和里判 | `ctx.tag in ("七对","连七对") or ctx.all_sets_triplets` | 《问答》Q71 牌例2「此牌除计七对 24 分，还应另加计清幺九 64 分」 |
| **混幺九 接受七对** | 同上 | 同上 + 必须有字牌、全部为幺九 | 《问答》Q71 牌例6：混幺九 32 + 五门齐 6 |
| **连七对必须同花色** | 只比首尾 tid 之差 = 6 →「筒6~9＋索1~3」被算成连七对 88 分 | 加 `suit_of` 同花色判断，跨花色只算七对 24 分 | v2.2.0 已修，此处同步说明 |
| **杠与七对互斥** | — | 结构性保证：所有特殊牌型都由 `if m == 0 and sum(counts) == 14` 把关，**任何杠都是 Meld ⇒ m>0**，因此「4 张明杠/暗杠 + 七对」在结构上不可能成立；`四归一` 在特殊牌型里直接返回 0 | 用户要求「这 4 张牌不能明杠或者暗杠」；`error_audit.py` C06b 专项注入验证 |

**三、验证（本版实跑）**

- `_scaffold\test_engine.py`：**73 项全通过**
- `_scaffold\smoke_test.py`：**141 项全通过**（布局改动未影响任何功能断言）
- `_scaffold\shot_ui.py`：重新截图 **7 张**（`_scaffold\shots\ui_1..7_*.png`），四行布局人工确认
- 版本号：`mahjong_gui.py` 第 59 行 → `2.4.1`；备份 `version\mahjong_gui - 2.4.1 选项区四行布局 引擎口径补充.py`

### v2.4.0（2026-09-25）—— Web 客户端：手机/平板浏览器直接算番（局域网）

**背景**：用户要求「**增加 WEB 客户端使用方式（访问方式），方便其他设备使用该程序进行算番**」。
v2.3.0 的本地 API 只有 JSON 接口 + 一张自测页，手机用不了；本版把**整套手机版界面做进服务端**（单文件 HTML，内联 CSS/JS）。

**新增**

1. **Web 客户端页面**（`mahjong_api.WEB_CLIENT`，手机优先布局，与桌面版能力一致）：
   - 已选牌（副露 / 立牌 / 和张 / 花牌）→ 模式（立牌/吃/碰/明杠/暗杠）→ 选项（自摸·和绡张·抟杠和/杠上开花·海底捞月/妙手回春）+ 圈风风位
   - 牌池 5 行（索/筒/万/字/**花牌 春夏秋冬·梅兰竹菊**）：点一下加一张，**长按或右键减一张**；牌面直接用 `/tiles/<编码>.png`（读《麻将图》原图）
   - 立牌满 13−3×副露数 张时**自动列出听牌候选（带每张总番数，不足 8 分置灰）**；点候选或点牌池里的牌即出总番 + 番种列表
   - 底部「↑结果 / ↓牌池」快捷滚动；可「添加到主屏」（`/manifest.webmanifest`）
2. **静态路由**：`GET /`、`/web`、`/index.html` = Web 客户端；`GET /debug` = 接口自测页；
   `GET /manifest.webmanifest` = PWA 清单；`GET /tiles/<编码>.png` = 牌面图（白名单正则 + 404，**拒路径穿越**）；
   `GET /favicon.ico` / `/apple-touch-icon.png` = 用「中」当图标。**静态资源免口令**，`/api/*` 仍要口令
3. **菜单新增**：「允许局域网访问（手机/平板）」（勾选后改绑 `0.0.0.0` 并**自动生成 6 位口令**）、
   「设置访问口令…」、「API/Web 地址与用法」改为同时给出**手机链接**（含 `?token=`）并复制到剪贴板
4. **新增设置项**：`api_lan`（是否允许局域网）、`api_token`（访问口令），按约定三处成对实现
   （`_collect_settings` / `_apply_settings` / `_connect_autosave`，改完立即重启服务并保存）

**本版修掉的 4 个真缺陷（全部是「真机浏览器里点出来的」，不是看代码看出来的）**

1. **算番请求张数口径错**：`/api/score` 的 `concealed` 必须**含和张**（= 14−3×副露数 张），
   Web 端只传了 13 张 → 一直 400「应为 14 张」。现改为 `hand = concealed + [win]` 再提交。
   （`mahjong_api` 模块文档里「concealed 是和张之外的手牌」这句提示也是错的，已改正）
2. **设了和张进不了算番分支**：`compute()` 先判 `n===need`（13 张）→ 总落到「听牌候选」分支，
   `#tot` 永远显示「—」。改为**先判 `withWin >= need+1`（算番）再判听牌**。
3. **牌池点击会丢**：原页面每次 `render()` 都重建整个牌池 DOM（44 张图），快速连点时
   mousedown/mouseup 之间的节点被换掉 → **点击丢失（实测 13 次丢 2 次）**。
   改为 `buildPool()` 只建一次 + `updatePool()` 只改 `sel` 类与角标文字（也快得多）。
4. **点花牌被当成手牌**：牌池里点 S1~P4 会进 concealed → 送进引擎报「无效牌张代码」。
   改为 `tap()`/`longTap()` 遇 `/^[SP]/` 一律走 `toggleFlower()`（并且花牌也在牌池里高亮、显角标）。

**验证**

- `_scaffold\api_smoke_test.py`：**48 → 73 项**（新增 Web 客户端页面特征、`/debug`、`/manifest.webmanifest`、
  `/tiles/*.png`（含花牌图、非法名 404、路径穿越被拒）、免口令静态资源 vs 口令接口、
  **张数口径回归 4 项**、以及 4 条白盒回归（JS 里 `hand.push(S.win)` / `withWin>=need+1` / `buildPool` / 花牌分支）
- `_scaffold\smoke_test.py`：**123 → 141 项**（新增「17b Web 版：局域网 + 口令」：默认只绑本机、口令生成、
  勾选后绑 `0.0.0.0`、手机链接含 IP/端口/口令、设置持久化、**真起服务验证**无 token 401 / 带 token 200 /
  首页免口令 / 花牌图可取、取消局域网回落本机）
- **真机浏览器端到端实测**（`python mahjong_api.py --host 0.0.0.0 --token 1234` + 内置浏览器）：
  点 13 张 → 列出九莲宝灯听牌 9 张（1/9万 106 番、2/8万 91 番…）→ 点第 14 张设为和张 →
  **106 番（九莲宝灯 88 + 清龙 16 + 四归一 2）** → 加一张花牌 → **107 番**，
  与桌面引擎同一手牌计算结果**逐项一致**

### v2.3.0（2026-09-25）—— 本地 HTTP API（JSON 接口，给别的程序/设备调用）

**背景**：用户要求「**增加 API 接口**」。

**新增**（`mahjong_api.py`，约 1076 行，可单独运行也可由主程序内嵌启动）

| 接口 | 说明 |
| --- | --- |
| `GET /api/health` | 存活探测 |
| `GET /api/version` | 版本 / 番种数 81 / 规则文件是否加载 |
| `GET /api/tiles` | 34 张牌编码表（tid/code/name/suit/num） |
| `GET /api/fan_table` | 81 番种（按分值分 12 档 + 定义） |
| `POST /api/score`（也支持 `GET …?concealed=[…]&win=W9&tsumo=1`） | 算番 |
| `POST /api/waits` / `/api/waits_all` | 听牌（返回每张的总番数与番种；`_all` 不筛起和分） |
| `GET /api/help` | 接口说明（就是模块 docstring） |
| `GET /debug` | 浏览器自测页（表单 + 实时 JSON） |

- 请求体：`melds`（`[{kind:chi/pong/kong, tiles:[…], concealed:bool}]`）+ `concealed`（或 `counts`）+ `win`
  + `options`（`tsumo/last_tile/rob_kong/kong_bloom/last_draw/last_discard/round_wind/seat_wind/flowers/flower_tiles`）
- **前置校验返回 400 并带人话**（不合法不返回 0 番）：张数不对 / 和张不在手牌 / 同种超过 4 张 /
  非法牌张代码 / 非法吃 / 非法风位 / 花牌超 8；内部错误 500
- 跨域头 `Access-Control-Allow-Origin: *`，`OPTIONS` 预检 204（方便网页/小程序直接调）
- 可选口令：`X-Api-Token` 头或 `?token=`（默认不校验；局域网访问时自动启用）
- 主程序：「工具 → 启动本地 API」（Ctrl+Alt+A）启动/停止，端口可改（`QInputDialog`，1024~65535），
  端口与「是否随程序自启动」写进设置；退出 `closeEvent` 先停服务再保存
- `threading` 服务器 + 引擎锁，**不阻塞界面**；启动失败/端口被占会给出人话提示且不影响算番

**验证**：`_scaffold\api_smoke_test.py`（真实起服务 + 真实 HTTP）**48 项全通过**（v2.4.0 扩到 73 项）。

### v2.2.1（2026-09-24）—— 按国标口径修正：七对允许 4 张相同的牌（算两对）

**背景**：用户裁定「**七对可以含 4 张相同的牌，因为国标这是允许的**」。
v2.2.0 曾把这一点登记为「口径不确定项」（按严格口径实现），现按国标口径改正。

**修改**

1. `mahjong_core.decompositions()`：七对成立条件从
   「每张牌数∈{0,2,4} 且**7 个不同对子**（`sum(1 for c if c>=2) == 7`）」
   改为「每张牌数∈{0,2,4} 且**对子数 = 7**（`sum(c // 2 for c in counts) == 7`）」
   ⇒ `一一一一 二二 三三 四四 五五 六六`（1 个四张 + 5 对）、
   两个四张 + 3 对、三个四张 + 1 对 均为七对。
2. `_special_win_quick()`（听牌快速判定）同步：否则「听含四张的七对」会漏报。
3. `_scaffold\indy.py`（第二套实现）同步；**并修正一处对拍口径**：
   特殊牌型（七对等）中的「四归一」按《问答》Q71 属必然并存、**根本不产生**，
   之后 `indy.ref_detect` 与引擎一致（之前它会在 tag 分解里多算四归一，靠不计表才被剔掉）。
4. **连七对仍要求「同一花色、序数连续的 7 个对子」**：含 4 张同牌的七对不可能变成连七对，
   这一条与 v2.2.0 的修正不冲突（两者同时成立）。
5. `mahjong_gui.HELP_TEXT`（用法页 · 算番口径）补上这一条，让用户看得到。

**校验与回归**

- `verify_all.py` **新增「七对含四张」牌型枚举**：3 个四张 + 1 对 **完整枚举 185,504 手**，
  1~2 个四张 **随机抽样 120,000 手**（全空间约 1103 万手）——否则新口径的牌型根本不在校验范围内。
- `test_engine.py` 新增回归 **9 项**（含四张仍是七对 / 不计四归一 / 含两个四张 / 含四张+清一色 /
  含四张不判连七对 / 对子数≠7 不算七对 / 听牌要认含四张的七对 / 含四张的七对·字牌 = 88 番），
  共 **60 项**。
- `error_audit.py` 新增注入 **E29「七对退回旧口径（4 张不算两对）」**，要求被 D4/D6b 抓到；
  口径审计改成「已裁定」并记录影响面。共 **29 个注入**。

**本版校验结果**

| 阶段 | 规模 | 结果 |
| --- | --- | --- |
| 枚举全部和牌型 | 2,118,xxx 手（标准型 120 万抽样 + 组合龙 11,016 + 特殊 742 + 七对子空间 480,700 + 七对随机 12 万 + **七对含四张 185,504 完整 + 12 万抽样**） | —— |
| 逐手校验 | 见 `_verify_out.txt` | 见日志 |
| 独立实现对拍 | 40 万手 | 见日志 |
| 错误类别故障注入 | 25 类 / **29 个注入** | **29/29 被拓到，盲区 0** |

### v2.2.0（2026-09-24）—— 错误类别全枚举 + 故障注入验证 + 修 1 个「多算 88 分」缺陷

**背景**：用户要求「**枚举所有可能存在的错误**，从而验证是否会发生类似的错误，算番是否正确，
避免算多了 / 算少了 / 算漏了」。v2.1.0 是「穷举**牌型**」，本版是「穷举**出错方式**」——
把算番器可能出错的地方按机制分成 **25 类**，逐类**注入一个真实缺陷**，
再看现有校验体系能不能抓到：抓不到的就是校验盲区。

**新增 `_scaffold\error_audit.py`（错误类别全枚举 + 故障注入）**

| 组成 | 内容 |
| --- | --- |
| 错误类别目录 `CATEGORIES` | **25 类**：花色边界算术 / 花色过滤缺失 / 可选择性未穷举 / 组合取法被截断 / 分解完备性 / 特殊牌型判定 / 听牌判定 / 快速判定与完整判定不一致 / 取优（就高不就低）/ 多重番种计数 / 番值表错 / 「不计」表错 / 幺九刻保护 / 必然并存反向保护 / 起和分口径 / 无番和兜底 / 副露口径 / 杠的计分 / 风位圈风 / 选项类番种 / 结构不变量 / 输入校验 / 参照实现自身出错 / 结果与过程不一致 / 口径不确定项 |
| 检测器 D1~D10 | D1 规则数据一致性（番值表·不计表 vs 《规则》JSON）· D1b 不计表 vs 参照表（**多余项会少算**同样报）· D2 和牌判定 · D2b 听牌判定 · D3 分解集合 · D4 特殊牌型 tag · D5 同分解逐番对拍 · D6 取优 · **D6b 终局番数对拍（独立实现口径）** · D7 起和/花牌/无番和口径 · D8 81 番种金标准 · D9 结构不变量 · D10 接口边界 |
| 断言参照物 | 一律取**注入前快照**（`PRISTINE`）与**第二套独立实现**（`indy.py`），不用被检查对象自身做断言（否则缺陷会自己掩盖自己） |
| 牌池 | 大池 1419 手（基线用：金标准 81 + 随机和牌 + 8 种选项组合 + 副露·杠 + 听牌 + 定向反例）、小池 408 手（逐类注入用） |

**28 个故障注入全部被抓到（校验盲区 0）**，其中 6 个正是本项目历史真缺陷：
E01 连七对只比较牌号差（本版真缺陷）· E03 三色三同顺不查花色 · E04 三色三节高用花色并集 ·
E06 4 副露牌型不可分 · 另加 E02 顺子跨花色、E05 组合龙只实现一种花色排列、
E08a/E08b 不计表丢项/多项、E09/E10 番值被改/缺番种、E11 只取第一个分解、E12 多重番种压到 1、
E13 `EXCLUDE_KEEP` 丢失、E14 `EXCLUDE_DROP` 丢失、E15 起和恒真、E16 花牌并进起和分、
E17 无番和兜底丢失、E18 听牌返回空、E19 快速判定丢七对、E20 明刻当暗刻、E21 双明杠失效、
E22 门风恒按东算、E23/E24/E25 特殊牌型过宽、E26/E27 结构不变量破坏、
**E28 参照实现自己多算**（证明对拍对「基准出错」同样敏感）。

**修复（本版查出的真缺陷）**

6. **连七对跳花色误判（会多算 64 ~ 88 分）**：`decompositions()` 里只判断「七个对子的牌号差 6」，
   而牌号是**跳花色连续编号**（筒 0~8 / 索 9~17 / 万 18~26），于是
   「筒6筒6筒7筒7筒8筒8筒9筒9 + 索1索1索2索2索3索3」这种**跳两个花色的七对**
   被当成连七对（88 分）⇒ 改成必须**同一花色 + 序数连续**。
   影响面：跳花色的七对（4+3、3+4、2+5 拆分）全部被多算。

**顺带加固（防同类缺陷再溜过去）**

- `verify_all.py` 逐手阶段（覆盖**全部**枚举牌型，含 48 万手七对子空间）新增
  **用独立实现核对特殊牌型 tag** —— 这类「tag 判错」的缺陷以后必然被全量抓出；
- `verify_all.py` 新增 `--diff-limit` 参数，可放大对拍样本（本次用到 **40 万手**）；
- `mahjong_core.score()` 增加**张数前置校验**：张数不对时明确报
  「手牌张数不足，无法算番（当前 N 张，应为 M 张）」，不再笼统地说「牌型不能和牌」；
- `test_engine.py` 增加连七对回归 9 项（现 **51 项**）。

**本版校验结果（全量）**

| 阶段 | 规模 | 结果 |
| --- | --- | --- |
| 枚举全部和牌型 | 1,801,567 手（标准型 120 万抽样 + 组合龙 11,016 完整 + 特殊牌型 742 完整 + 七对子空间 480,700 完整 + 七对随机 12 万） | —— |
| 逐手：和牌判定/分解一致/逐分解算番/取优/番值/并存/丢番 | 1,801,567 手 | **异常 0 条**（单手最高 171 番） |
| `score()` 公开接口全路径 | 40,000 手 | 异常 0 条 |
| 与独立实现同分解逐番对拍 | **400,000 手** | **完全一致 400,000，差异 0** |
| 全和张 × 点炮/自摸 扇扫 | 22,090 次算番 | 差异 0 条 |
| 81 番种金标准牌例 | 81 | **81/81** |
| 变异牌型判定比对 | 59,692 手 | 不一致 0 条 |
| 副露专项对拍 | 6,000 手 | 差异 0 条 |
| 番种覆盖率 | —— | 81/81 |
| **错误类别故障注入** | 25 类 / 28 个注入 | **28/28 被抓到，校验盲区 0** |

**结论**：多算 / 少算 / 漏算 均为 0；「可能出错的方式」全部能被现有校验体系抓到，
即以后若引入同类缺陷，`error_audit.py` 与 `verify_all.py` 会立刻报出来。

**口径不确定项（已登记，非 bug）**

- **七对能否含 4 张相同的牌**：《规则》牌型记法为「1=单张 11=将 111=刻子 1111=杠」，
  七对写作 7 组「11」；本实现按**严格口径**（必须 7 个不同对子），
  故「4 张同牌 + 5 个对子」这类牌型**不算七对**。若按「两张相同的牌为一对」的宽松口径，
  另有 **11,037,488 手**会成立（1 个四张 8,069,424 + 2 个四张 2,782,560 + 3 个四张 185,504）。
  如赛事规程另有约定，改 `decompositions()` 的七对条件并同步改 `indy.ind_special` 即可。

### v2.1.0（2026-09-24）—— 全量和牌枚举校验 + 修复 5 个算番缺陷 + 听牌判定提速

**背景**：用户要求「枚举所有和牌的组合（包括不同的方式和方法），检查算番是否正确，
避免数多了 / 算少了 / 算漏了」。为此新建了一整套**全量校验工具**，并因此查出并修正了 5 个真缺陷。

**新增（校验工具，均在 `_scaffold\`）**

| 文件 | 作用 |
| --- | --- |
| `verify_all.py` | 主校验：A 枚举所有和牌型 · B 和牌判定 · C 分解集合一致 · D 同分解逐番对拍 · E 取优（取番数最高的分解方式） · F 「不计」并存/无故丢番 · G 覆盖率 · H 变异反例 · I 听牌一致 · J 副露专项 · K `score()` 全路径 |
| `indy.py` | **第二套独立实现**（不共用任何判定代码）：自己的牌张编码、自己的分解递归、81 个番种另写一遍、自己的「不计」闭包 |
| `golden_fans.py` | **81 个番种的金标准牌例**（每个番种一手构造牌，证明都能被算出来） |
| `dbg_diff.py` / `dbg_search.py` | 差异定位（引擎 vs 独立实现） |

校验规模（全量）：**标准型 + 组合龙 + 七对子空间完整枚举 + 十三幺/连七对/全不靠/七星不靠 全部枚举**，
逐手做「分解集合一致 + 逐分解算番 + 取优 + 番值 + 并存违规 + 无故丢番」六项检查；
另抽 12 万手做**同分解逐番对拍**、5 千余次「全和张 × 点炮/自摸」扇扫、1200 手副露专项对拍。

**修复（校验查出的真缺陷）**

1. **绿一色牌面取错**（`GREEN_TILES`）：写成 `{12,13,14,16,18,32}`（＝索4、5、6、8 与**一万**），
   按规则原文「由 23468 条及发字组成」应为 `{10,11,12,14,16,32}`（索2、3、4、6、8 + 发）。
   ⇒ 修正前绿一色永远算不出、且会把带一万的牌误判成绿一色。
2. **三色三同顺未校验花色**：只数「同数字的顺子共 3 副」，
   于是「筒123 ×2 + 万123」也被误判 ❌ ⇒ 改成必须**三种不同花色各一副**。
3. **三色三节高用「花色并集≥3」代替可选择性**：可能选不出三副互不相同花色的刻子 ❌
   ⇒ 改成枚举 3 种花色的排列逐一校验（同 三色三步高 的做法）。
4. **4 组副露的牌型完全算不出番**：`decompositions()` 里条件写成 `need >= 1`，
   导致 `need == 0`（4 副露 + 将，如**四杠 88 番**、四副露全求人）直接返回空 ⇒ 「牌型不能和牌」。
   ⇒ 改成 `need >= 0`。
5. **一色双龙会误判**：原来只看「123 与 789 合计 ≥ 4 副」，
   于是「筒123×3 + 筒789」也被误判 64 番 ❌ ⇒ 改成 **123、789 各自 ≥ 2 副**且总面子为 4。

**优化**

- `waiting_tiles()` 改用**快速布尔判定**（`_split_ok` / `_shape_win` / `_special_win_quick` /
  `_long_dragon_win`，不再构造 `Decomp/Set_` 对象），听牌判定快约一个数量级；
  界面每次点牌、每次选项变更都要重算听牌，千手重算从秒级降到毫秒级。
  结论一致性由 `verify_all.py` 的「听牌对拍」逐手比对（独立实现 `indy.ind_waits`）。
- 校验工具本身也用 `ProcessPoolExecutor` 并行，并实时写 `_scaffold\_verify_out.txt`。

**校验中发现但属于「工具口径」而非引擎问题的点（已修正工具）**

- 独立实现漏了**组合龙**与**点炮补成刻子算明刻**两条规则；
- 对拍时把「七对/十三幺等特殊牌型分解」（`tag` 非空）混进标准分解集合比较；
- 金标准牌例里我最初造错了 6 手牌（张数不对 / 与其它番种必然并存被剔掉 / 同一种牌超过 4 张）；
- 副露牌例生成器早期允许生成「同一张牌 6 张」的非法牌型。

### v2.0.0（2026-09-24）—— 推倒重来：PySide6 重写 + 算番引擎重建 + 按设计图实现全部交互

**背景**：v1.1.0 的界面（PyQt5）与设计图（`软件设计构图\`）差距大，「立牌/吃/碰/明杠/暗杠」全是空按钮，
且 `mahjong_core.py` 源码丢失（只剩 `.pyc`），实际上算不了番。本次按用户要求**推倒重来**。

**新增/重构**

1. **`mahjong_core.py` 全量重建**（1565 行，取代丢失的旧版）：
   - 牌张编码 34 个 `tid` + `code_of/tile_of/name_of` 互转；
   - 牌型分解：标准型全部拆法 + **七对 / 连七对 / 十三幺 / 全不靠 / 七星不靠 / 组合龙**；
   - **81 个番种的判定函数全部实现**（`fan_xxx(ctx) -> bool`）+ 可多次成立番种的实例计数；
   - **「不计」去重**：直接读 `国标麻将标准规则.json` 的 `不计` 字段，再合并《问答》补充的必然并存关系；
   - 和牌 / 听牌判定、单听判定（边张/坎张/单钓将）、起和 8 分校验、花牌另计、无番和兜底；
   - 读取的规则文件名改为 `国标麻将标准规则.json`（旧版读的是根本不存在的 `rules.json`）。
2. **主程序改用 PySide6 重写**（`mahjong_gui.py`，1217 行），**严格按 `软件设计构图\` 的 11 张设计图实现**：
   - 牌选择区四行（索/筒/万/字），牌面图取自 `麻将图\`，红角标显示已选张数；
   - 模式按钮行 `立牌 / 吃 / 碰 / 明杠 / 暗杠 / 重置`，**全部真正实现**（先点按钮再选牌）；
   - 复选框随「自摸」动态换文案（杠上开花·妙手回春 ⇄ 抢杠和·海底捞月）；
   - 圈风、风位互斥单选，**任何时候都能点，点了立即重算**；
   - 已选牌区分三行显示 **副露 / 立牌 / 和张**，**暗杠按设计图显示为「面·背·背·面」**（中间两张用 `empty.png`）；
   - 听牌候选区：每个候选牌上方标出该张的**总番数**，点击即出总番数 + 番种列表；
   - 底部三页导航 `算番 / 番表 / 用法`，番表页含 81 番种（定义 + 不计）与搜索框。
3. **设置实时自动保存（延续 v1.1.0 约定）**：`mahjong_settings.json` + 500ms 防抖 `QTimer`；
   新增设置项：模式、当前页；`_on_option_changed()` 保证「选项一变立刻重算 + 立刻落盘」。
4. **打包脚本重建**：`0_nuitka_pyside6_clang_auto.bat`（纯 ASCII）→ 调用 `_scaffold\build_exe.py`
   （用 Python 传参以正确处理中文路径），带上 `国标麻将标准规则.json` 与整个 `麻将图\`。
5. **测试与截图脚手架**：`_scaffold\test_engine.py`（引擎 42 项）、`_scaffold\smoke_test.py`
   （界面 101 项）、`_scaffold\shot_ui.py`（真实平台截图到 `_scaffold\shots\`）。

**修复（重建过程中发现并已改正）**

1. `清一色` 判定把「一种花色 + 字牌」也算成清一色（`_same_suit` 把字牌花色直接 discard 掉了）
   → 拆成 `_same_suit()`（清一色，禁止字牌）与 `_one_number_suit()`（混一色，允许字牌）。
2. `全不靠 / 七星不靠` 只按「单张是否属于 147/258/369 集合」判断，导致五花色的牌也被误判和牌
   → 改为 `_is_bu_kao()`：三门花色必须分别落在**不同的列**上（不能错位）。
3. `组合龙` 的 9 张牌被当成 3 个顺子，污染 平和 / 清龙 / 全带五 等判定
   → 改用 `kind="long"` 的伪面子。
4. **点炮补成的刻子被算成暗刻**（四暗刻/三暗刻/双暗刻计数错误）
   → `Ctx.concealed_triplets` 在非自摸且该刻子正好由和张补成时按明刻处理（暗杠不受影响）。
5. `双箭刻 / 箭刻 / 圈风刻 / 门风刻` 的「不计幺九刻」被当成整项删除，把**别的**幺九刻也一起删了
   → 改为 `EXCLUDE_KEEP = {"幺九刻"}` + `_yao_jiu_covered()` 精确扣除。
6. 杠的计分只处理了 1 个杠的情况，多个杠时明杠/暗杠分全部丢失
   → 按《问答》第 41 条补全（含「一明一暗 = 6 分」）。
7. 特殊番种/风圈/风位勾选后**界面不重算**（只保存不刷新）
   → 新增 `_on_option_changed()` 统一接所有选项信号。
8. 番表搜索「无结果」时不会提示（把分组标题也当成命中）
   → 改为只收集命中行并带出所属分组，无命中给明确提示。
9. 布局清空统一走 `clear_layout()`（`takeAt` → `hide` → `setParent(None)` → `deleteLater`），
   避免历史上出现过的「悬空指针踩堆」问题。
10. 手牌未选满/未选和张时，结果区误报「牌型不能和牌」
    → `_update_result()` 区分「手牌不足」「还未选和张」「牌型不能和牌」三种状态分别提示。

**与设计图的已知差异（有意为之，按《规则》原文口径）**

| 设计图 | 本程序 | 依据 |
| --- | --- | --- |
| 「5番 明暗杠」 | 双明杠 4 + 暗杠 2 = **6 番** | 《问答》第 41 条：一明一暗按 6 分计 |
| 九莲宝灯那手共 118 番（含 4 番不求人） | **106 番**（88+16+2，无「不求人」） | 《问答》：九莲宝灯必然并存门前清/不求人，不重复计分 |
| 设计图仅作界面参考，番数由设计者估算 | 番数一律以《国标麻将标准规则.json》为准 | 《中国麻将竞赛规则(试行)》1998 |

### v1.1.0（2026-09-24）—— 规则数据全量整理 + 设置实时自动保存（PyQt5 版，历史）

**新增**

1. **`国标麻将标准规则.json` 全量重构**（由 18 KB 的早期残缺摘录 → 完整规则库）：
   - 依据 `中国国标麻将标准PDF\` 内两本原书逐条整理：
     `中国麻将竞赛规则——1998年国家体育总局颁布 ocrmypdf.pdf`（111 页，带文本层）、
     `《中国麻将竞赛规则试行》问答——作者：杜维忠 orcmypdf.pdf`（235 页，带文本层）。
   - 内容：`meta` / `牌具与器材` / `术语`（35 条）/ `和牌规定`（5 种基本牌型 + 3 种特殊牌型）/ `比赛程序` /
     `番种体系`（12 分值级、9 大系列、81 番种）/ **`番种`（81 个，含定义、不计番种、补充说明）** /
     `计分`（公式 + 5 条计分原则）/ `成绩与名次` / `罚则` / `申诉与仲裁` / `裁判工作` / `守则` /
     `附表与附件` / `历史与由来` / **`问答`（166 条）** / `程序实现要点`。
2. **主程序设置实时自动保存**：新增 `mahjong_settings.json` + 500ms 防抖 `QTimer`；控件变更 →
   `_autosave_settings()` → `_save_settings_now()`（原子写入 tmp + `os.replace`）；`closeEvent` 强制保存；
   启动 `_apply_settings()` 恢复（全程 `blockSignals` 防止加载时回写）。
3. **版本号与关于菜单**：新增 `__version__`、`APP_NAME`；窗口标题带版本号；菜单新增「关于」「规则数据来源」。
4. **起和分校验**：结果显示「起和分 N 分，达到/未达到 8 分起和标准」（花牌分不计入）。
5. **资源路径适配打包**：新增 `app_dir()` / `resource_path()`。
6. **容错导入 `mahjong_core`**：改为 `try/except`，缺失时窗口仍可打开并给出说明。

**修复**

1. 删除 `TileButton.max_count` 中的死代码（不存在的牌种），统一 `max_count = 4`。
2. 界面常量去重（`SPECIAL_FANS` / `WIND_NAMES` / `FLOWER_NAMES`）。
3. 早期残缺的旧 `国标麻将标准规则.json` 归档到 `_scaffold\legacy\`。

**脚手架（`_scaffold\`）**

| 脚本 | 用途 |
| --- | --- |
| `extract_pdf_text.py` | 从 PDF 提取全文（`--stat` 只统计文本层，用于挑出带 OCR 文本层的版本） |
| `split_qa.py` / `split_qa2.py` / `split_qa3.py` | 《问答》OCR 正文切分成「问/答」（v3 用「题号近似递增」锁定题目起始行） |
| `compact_qa.py` | 压缩问答文本（剔除牌例图噪声行），便于人工整理 |
| `inspect_pyc.py` | **反查 `mahjong_core.cpython-310.pyc`**（重建引擎时的参考，v2.0.0 已不再需要） |
| `smoke_test_v1_pyqt5.py` | v1.1.0（PyQt5）的冒烟测试，已归档 |
| `legacy\` / `pdf_text\` / `qa_split\` / `pyc_dump.txt` | 中间产物 |

### v1.0.0（2026-09-24 之前，历史状态）

- 主程序为 PyQt5 单文件 GUI；`check_winning_hand()` 与 `analyze_ting()` 为**占位实现**，
  算番依赖 `mahjong_core.MahjongFanCalculator`，但该模块源码已丢失
  （仅 `__pycache__\mahjong_core.cpython-310.pyc` 残留 14,367 字节）。
- 无版本号、无设置持久化、无菜单栏。

---

## 四、功能清单（当前版本 v2.7.4）

| 功能 | 状态 | 说明 |
| --- | --- | --- |
| 34 种牌的可视化选择（左键 +1 / 右键 −1） | ✅ | 每种最多 4 张，红色角标显示张数；**牌间距 4px、多余宽度全在右侧（v2.4.2）** |
| **立牌**（手上其他牌） | ✅ | 选满 13 − 3×副露数 张即进入听牌分析 |
| **吃**（先点按钮再选 3 张相连同花色牌） | ✅ | 不合法时状态栏明确提示 |
| **碰 / 明杠 / 暗杠**（先点按钮再点牌，一次成组） | ✅ | 明杠自动补 4 张，暗杠标记为暗 |
| **副露展示：暗杠 = 面·背·背·面** | ✅ v2.7.1 | 桌面版按设计图，中间两张用 `empty.png`；**v2.7.1 起 Web 端同样如此**（明杠/碰/吃仍全正面；副露之间留 10px 间隔） |
| 和张（点候选牌 / 点牌区的牌自动设定 / 再点取消） | ✅ | 未指定时自动挑一张能和的牌 |
| **听牌候选 + 每张候选的总番数** | ✅ | 不足 8 分起和的候选标红 |
| **算番结果：总番数 + 番种列表（含牌型）** | ✅ | 番种按分值降序；不足 8 分明确提示 |
| 自摸 / 和绝张 | ✅ | 复选框 |
| 杠上开花 / 妙手回春 ⇄ 抢杠和 / 海底捞月 | ✅ | **随「自摸」动态换文案**，切换时清空旧勾选 |
| 圈风（东南西北圈）/ 风位（东南西北位） | ✅ | 互斥单选，**任何时候都能点，立即重算**；v2.4.1 起圈风、风位各占一行 |
| **选项区四行布局**（v2.4.1） | ✅ | ①复选框 ②风圈 ③风位 ④花牌；控件名与信号未变，老设置照常恢复 |
| 花牌 8 张（点图选/取消，显示张数） | ✅ | 春 夏 秋 冬 梅 兰 竹 菊；每张 1 分，**不计入 8 分起和分** |
| 番表页（81 番种 + 定义 + 不计 + 搜索） | ✅ | 按分值分组 |
| 用法页（操作说明 / 特殊和法 / 界面约定 / 算番口径） | ✅ | |
| 重置 | ✅ v2.4.4 | **全部复位**：手牌 · 副露 · 和张 · 待选 · 模式 · **花牌** · **四个复选框** · **圈风风位**，并立即落盘 |
| **不用右键也能减牌**（点已选的牌取消） | ✅ v2.7.2 | ① 牌选择区**点红色角标 = −1**；② 「已选牌」区里**点某一张 = 取消这一张**：点立牌一张 = 减一张、**点副露一张 = 撤销那一组**（原来只能整个「重置」）、点和张 = 取消和张、点花牌行 = 取消那张花牌。右键（鼠标）照旧可用 |
| 设置实时自动保存（防抖 500ms） | ✅ | 模式、页面、圈风风位、花牌、各复选框 |
| 窗口尺寸（默认 680×940、最小 600×560） | ✅ v2.4.3 | 牌池只需 ~560px；旧版存下的宽几何首次升级会**一次性**收紧宽度 |
| 窗口几何 / 最大化恢复 | ✅ | 存 `geometry` + `geo_ver`（迁移标记）+ `window_maximized` |
| 关于 / 用法 / 番表 菜单 | ✅ | |
| 打包后资源定位（`麻将图\` 与规则 JSON） | ✅ | `app_dir()` / `resource_path()` |
| **英文名 / 文件名 / exe / 打包目录统一** | ✅ v2.7.3 | 全部叫 `Mahjong_Calculator`：主程序 `Mahjong_Calculator.py`、exe `Mahjong_Calculator.exe`、产物目录 `build_output\Mahjong_Calculator\`；**界面中文名「国标麻将算番器」不变**（`APP_NAME`） |
| **Web 版日志干净**（手机断开不刷屏） | ✅ v2.7.4 | 手机刷新 / 关页面导致的 `ConnectionResetError`（“Exception occurred during processing of request…”）一律静音；**其它异常照旧完整打印**（不掩盖真问题），`--verbose` 时只给一行提示 |
| **程序图标 icon.ico**（窗口/任务栏 + exe 文件） | ✅ v2.4.7 | 红中牌样式、7 张尺寸各自绘制；`app_icon()` 读 exe 同目录、读不到安静降级为空图标；打包自动带 `--windows-icon-from-ico` 与 `--include-data-files` |
| **给程序用的算番入口**（无需 HTTP/端口） | ✅ v2.6.0 | ① `import mahjong_core`（`MahjongFanCalculator` / 便捷函数 `score_hand()`）② 命令行/子进程：`国标麻将算番器.exe --hand "..." --json`（牌支持 W1/一万/11123456789999m 三种写法；副露 chi/peng/kong/angang；退出码 0/1/2） |
| **Web 版**（手机/平板浏览器算番） | ✅ v2.4.0–v2.7.1 | 菜单「工具 → 启动 Web 版」；勾「允许局域网访问」后手机直接打开就能用，**默认不需要口令**（口令与免口令白名单都是可选项）；**牌尺寸两套布局共存**：默认「经典（固定）」与老版本逐像素一致，可切「自适应」按屏幕缩放（选择记在 localStorage）；**副露显示与桌面版同口径（v2.7.1）：暗杠＝面·背·背·面、明杠/碰/吃全正面、副露之间有间隔** |
| **免口令访问白名单**（可分项放行，含 Web 客户端） | ✅ v2.5.0 | 工具菜单「免口令访问设置…」：Web 客户端页面 / 自测页 / 查询类接口 / 算番接口 / 听牌接口 逐项勾选；默认 web+debug（＝旧版行为）；老设置无此键时自动回默认 |
| **本地 JSON API**（算番/听牌/番表/牌张表） | ✅ v2.3.0 | 工具菜单开关，默认只绑 127.0.0.1:8718，可选口令 |
| **Web 客户端（手机/平板浏览器算番）** | ✅ v2.4.0 | 牌池/副露/选项/听牌/算番全套，与桌面版口径一致；**v2.4.4 起布局也与桌面版一致** |
| **局域网访问 + 访问口令** | ✅ v2.4.0 | 勾一下自动生成 6 位口令 + 弹出手机链接，静态资源免口令、接口需口令 |
| **可加到手机主屏**（PWA 清单 + 图标） | ✅ v2.4.0 | `/manifest.webmanifest`、`/favicon.ico`、`/apple-touch-icon.png` |
| 天和 / 地和 / 人和 | ✅ 按国标**不提供** | 国标 81 番种中没有（《问答》Q12） |

---

## 五、已知问题（按优先级）

| 级别 | 问题 | 影响 | 建议修法 |
| --- | --- | --- | --- |
| P1 | 罕见牌型的「必然并存」判断仍有主观取舍（如 清幺九 是否另计幺九刻） | 极少数牌型可能差 1~2 分 | 以 `国标麻将标准规则.json` 的「不计」字段为准；有疑问时查《问答》，并在 `EXTRA_EXCLUDES` 里注释依据 |
| P1 | 「吃/碰/杠」后无法单独撑销某一组副露 | 只能整手「重置」 | 后续可加「副露右键删除」（Web 端同理） |
| P2 | ✅ **七对能否含 4 张相同的牌——已裁定（v2.2.1）：国标允许，4 张算两对** | 口径修正后，1~3 个四张的七对牌型（约 1103 万手）全部按七对计分 | 已按国标实现（`decompositions()` 与 `indy.ind_special` 同步）；含四张时「四归一」按《问答》Q71 属必然并存不计 |
| P2 | 花牌只输入张数，不区分具体花名 | 不影响算番（同分） | 如需展示花名可扩展 |
| P2 | `indy.py` 与主引擎共用「牌张编号约定」，若以后改了编号方案，两份都要改 | 影响校验准确性 | 修改编码约定时同步改 `indy.i_suit/i_num` |
| P2 | 局域网服务一旦开启，同一 WiFi 下知道口令的人都能用 | 仅影响自家网络，不影响算番准确性 | 口令只防随手访问；不用时取消「允许局域网访问」（回到只绑本机） |
| P2 | 打包脚本尚未在本机完整验证（需已安装 nuitka） | 分发 exe 前需先跑一次 | `0_nuitka_pyside6_clang_auto.bat`（内部调用 `_scaffold\build_exe.py`） |
| P3 | `tiles\`（v1.x 牌面图）仍在仓库里 | 仅作回退用，不影响 | 可在确认无回退需求后删除 |

> **P0 问题（引擎源码丢失 + 读错规则文件名 + 立牌/吃碰杠无实现）已在 v2.0.0 全部解决。**
> **v2.1.0 又修了 5 个算番缺陷（绿一色牌面 / 三色三同顺 / 三色三节高 / 4 副露牌型 / 一色双龙会），详见「三、迭代记录 v2.1.0」。**
> **v2.2.0 再修了 1 个算番缺陷（连七对跳花色），并把「可能出错的方式」穷举成 25 类、逐一注入缺陷验证——29/29 全部能被抓到（盲区 0），详见「三、迭代记录 v2.2.0」。**
> **v2.2.2（口径补充，见 v2.4.1 小节）：七对里不再计大三元、清幺九/混幺九 接受七对（按《问答》Q71），杠与七对结构性互斥。**
> **v2.4.0 把工具推到手机：局域网 + Web 客户端（详见「三、迭代记录 v2.4.0」）；v2.4.1 把算番页选项区改成四行、v2.4.2 把牌池的牌收拢（只留 4px 间距），引擎未动，测试仍全绿。**
---

## 六、数据来源（规则 JSON 的依据）

| 来源 | 文件 | 页数 / 文本层 | 用途 |
| --- | --- | --- | --- |
| 《中国麻将竞赛规则(试行)》1998 国家体育总局审定 | `中国国标麻将标准PDF\中国麻将竞赛规则——1998年国家体育总局颁布 ocrmypdf.pdf` | 111 页，**有**文本层 | 全部条款、81 番种分值表与定义、附件、后记 |
| 同上（影印版 / 抽页） | `…颁布.pdf`、`… 34-38.pdf` | **无**文本层 | 仅作原图核对 |
| 《中国麻将竞赛规则(试行)》问答 | `中国国标麻将标准PDF\《中国麻将竞赛规则试行》问答——作者：杜维忠 orcmypdf.pdf` | 235 页，**有**文本层 | 166 条问答（尤其番种计入/不计、程序、罚则） |
| 同上（影印版 / 抽页 / 逐页 png） | `…作者：杜维忠.pdf`、`… 224-228.pdf`、`…png\` | 无文本层 | 仅作原图核对 |
| 早期残缺摘录 | 根目录旧 `国标麻将标准规则.json` | —— | 已归档 `_scaffold\legacy\`，被 v1.1.0 全量版取代 |
| **牌面图片** | `麻将图\*.png`（35 个文件，均 44×60，`empty.png` 为牌背） | —— | 界面牌面（v2.0.0 起） |
| **界面设计** | `软件设计构图\Screenshot_*.jpg`（11 张） | —— | v2.0.0 界面的唯一设计依据 |
| **校验参照** | `_scaffold\indy.py`（第二套独立实现）、`_scaffold\golden_fans.py`（81 番种金标准牌例） | —— | v2.1.0 全量校验的对拍基准 |
| **错误注入参照** | `_scaffold\error_audit.py`（25 类错误 / 28 个缺陷注入 / D1~D10 检测器） | —— | v2.2.0 「枚举所有可能错误」的验证体系 |

**关键结论（易踩坑，勿重复考证）**

1. 国标番种共 **81 个**，12 个分值级（88/64/48/32/24/16/12/8/6/4/2/1）；
   各分值个数 = 7/6/2/3/9/6/5/9/7/4/10/13。
2. **国标没有「天和、地和、人和」**（《问答》Q12：机遇性太大，不予采用）。
3. **花牌（春/夏/秋/冬/梅/兰/竹/菊）每张 1 分，但不计入 8 分起和分**。
4. 计分公式：自摸 `(底分8 + 基本分) × 3`；点和 `底分8 × 3 + 基本分 × 1`。
5. 计分五原则：不重复、不拆移、不得相同、就高不就低、套算一次。
6. **不求人 一旦成立就不再计自摸**（番种补充原文：「自摸是不求人的前提条件，已计不求人不再计自摸分」）；
   但花牌补花成和**仍算自摸分、不算杠上开花**。
7. **边张/坎张/单钓将：手牌听两种及以上的牌都不计**（1998 首届健康麻将邀请赛领队会规定）；
   `1233 和 3`、`7789 和 7` 按「边张」计，`4556 和 5` 按「坎张」计（番种补充原文）。
8. **杠的计分**（《问答》第 41 条）：明杠 1、暗杠 2、双明杠 4、**一明一暗 6**、双暗杠 6、
   三杠 32（其中暗杠另加）、四杠 88（四暗杠另加四暗刻 64）。
9. **暗杠既算暗刻（用于三暗刻/四暗刻）也单独计 2 分**（《问答》：双暗杠 6 分 + 另含一暗刻时另计三暗刻 16）。
10. OCR 常见误字：麻将→厅将/肥将、和牌→有牌、碰→磁/倍、杠→枉/柱、掷骰→掷山/掷敏。

---

## 七、维护约定（每次改动都要遵守）

1. **文档 + 版本号同步（强制）**：修改 `Mahjong_Calculator.py`（或 `mahjong_core.py`）后必须
   ① 更新本文件（迭代记录 / 功能清单 / 代码地图 / 维护约定四处）；
   ② 更新 `Mahjong_Calculator.py` 的 `__version__`（窗口标题、关于对话框都引用它）。
   ⚠ 行号会随改动漂移，**改版本以 `__version__ = "…"` 这行本身为准**，不要死记行号。
2. **版本号更新必须备份**：把主程序复制到 `version\`，命名
   `Mahjong_Calculator - X.X.X 简短说明.py`（改动后要重新拷贝一次，确保与主程序字节一致）。
   （v2.7.2 及以前的历史备份仍叫 `mahjong_gui - X.X.X …py` —— 它们就是当时的文件名，不再改名。）
3. **唯一编译源**：`Mahjong_Calculator.py` 是最终更新版，打包只使用这个文件（引擎 `mahjong_core.py` 会被
   Nuitka `--follow-imports` 自动带上；**规则 JSON 与 `麻将图\` 必须用 `--include-data-files` 显式带上**）。
   产物：`build_output\Mahjong_Calculator\` + `Mahjong_Calculator.exe`（由 `_scaffold\build_exe.py` 保证）。
4. **脚手架入子目录**：所有脚本/测试/中间产物放 `_scaffold\`，不散落根目录。
5. **设置实时自动保存（强制）**：新增设置项必须**三处成对**实现 ——
   `_collect_settings()`（收集）+ `_apply_settings()`（应用，`blockSignals` 防回写）+
   `_connect_autosave()`（接信号）；退出走 `closeEvent` 强制保存。
   另外：**凡是会影响算番结果的控件**（自摸/和绝张/特殊番/圈风/风位/花牌）还必须接到
   `_on_option_changed()`，保证「一改就重算」。
6. **记忆原有逻辑**：改动前先看本文「二、主程序运行逻辑」，改完如有变化必须同步更新该节。
7. **API Key / 数据库密码**：一律放 `.env`，**不要写进代码**（本项目当前无此类配置；
   参考 `.env.example`，`.env` 不纳入分发）。
8. **英文名 / 文件名 / exe / 产物目录统一叫 `Mahjong_Calculator`（v2.7.3 起）**：
   主程序 `Mahjong_Calculator.py`、exe `Mahjong_Calculator.exe`、产物 `build_output\Mahjong_Calculator\`；
   **界面中文名「国标麻将算番器」不变**（`APP_NAME`；英文名是常量 `APP_NAME_EN`）。
   引擎 `mahjong_core.py` / Web 版 `mahjong_api.py` **故意不改名**（`import mahjong_core` 是给外部
   程序用的 API 契约，改名会直接弄断）。**任何新写的脚手架脚本一律 `import Mahjong_Calculator as M`**，
   不要再写 `mahjong_gui`。
9. **别把「客户端提前断开」当错误处理（v2.7.4，用户拿控制台输出问「这些信息正常吗？」）**：
   手机/平板浏览器**刷新页面、关标签页、切后台、回收预连接**时 TCP 会被直接掰断（RST），
   `socketserver` 会把 `rfile.readline()` 的 `ConnectionResetError` **连整段 traceback 打到控制台**
   （`Exception occurred during processing of request from ('192.168.x.x', 52542)`）——
   **那是噪声，不是程序出错**。已在 `mahjong_api.py` 两处静音：
   ① `CLIENT_GONE_ERRORS`（976）+ `Handler.handle_one_request()`（997）吞掉异常；
   ② `QuietHTTPServer.handle_error()`（1200/1210）兜底不打印。
   ★ **原则：只静音这四类「客户端断开」异常，其它异常照旧完整打印**（别图省事把 `handle_error` 全吞了，
   那会把真 bug 一起藏起来）。新增 Web 端后台线程/长连接时也要照这个口径处理。
   验证走 `api_smoke_test.py` 的【7c】（真发 RST + 捕获 stderr）。
10. **不要每次改动都编译（v2.7.4 起，用户点名「不用每次都编译，因为我可能还有东西要改」）**：
   日常改动只做三件事 —— ① 改源码；② 改本文件（迭代记录 / 功能清单 / 代码地图 / 维护约定）+ 版本号；
   ③ 把主程序（改了 `mahjong_api.py` 就一并）备份到 `version\`。
   **Nuitka 编译只在「要分发 / 当场要用 exe」时做一次**：用户自己双击
   `0_nuitka_pyside6_clang_auto.bat`，或明确说「编译一下」。
   ★ 记住：**改完源码不编译，用户手上的 exe 就是旧的**（例如 v2.7.3 的改名、v2.7.4 的静音修复
   都只先进 py 源码）—— 汇报时要讲清「源码已改」还是「exe 已更新」，别让人以为双击 exe 就生效了。
8. **改完必跑四套测试**（当前：引擎 73 项 + 界面 141 项 + API 73 项 + 全量校验 + 故障注入，全通过）：
   ```powershell
   & "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\test_engine.py"
   & "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\smoke_test.py"
   & "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\api_smoke_test.py"   # 改了 API/Web 页面必跑
   & "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\verify_all.py" --quick   # 改算番逻辑必跑
   & "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\error_audit.py"            # 改算番/不计表/番值必跑
   ```
   日志同时写 `_scaffold\_engine_out.txt` / `_smoke_out.txt` / `_api_out.txt` / `_verify_out.txt` / `_audit_out.txt`（UTF-8）。
   看界面外观：`& "…\python.exe" -u _scaffold/shot_ui.py` → `_scaffold\shots\ui_*.png`。
   **只要动了番种判定/分解/不计表，就必须跑 `verify_all.py`**（它会用第二套独立实现对拍）。
9. **文件编码/换行**：所有源码与文档 **UTF-8 无 BOM + CRLF**；bat 脚本必须**纯 ASCII**。
10. **PowerShell 调用坑**：调用带路径的 exe 要用 `&`；读 UTF-8 输出前先
    `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`，否则控制台会乱码（文件本身没问题）。
11. **算番相关改动一定要做「双实现对拍」**：不要只靠手写牌例（很容易漏掉组合情形）。
    改完番种判定 / 分解 / 不计表，至少跑 `verify_all.py --quick`（约 2 分钟），
    大改则跑全量（1150 万手，6 进程约 20~30 分钟）。
12. **新增/修改番种判定后还要跑 `error_audit.py`**（约 3 分钟）：它会注入 29 个「典型缺陷」
    并要求全部被检测器抓到；若出现「★盲区★」，说明新改动让某类错误变得无法被发现，
    必须补检测器（这是 v2.2.0 「枚举所有可能错误」的强制环节）。
13. **API / Web 客户端一律写进 `mahjong_api.py`**（不另建文件、不另建静态目录）：
    - 页面直接内联在 `WEB_CLIENT` 字符串里（Nuitka 不需要额外数据文件，`--follow-imports` 会把
      `mahjong_api` 一起带上）；牌面图运行期从《麻将图》读，所以 `麻将图\` 仍必须用 `--include-data-files` 带上。
    - **接口口径铁律**：`/api/score` 的 `concealed` 是**含和张的整手牌**（14−3×副露数 张），
      `/api/waits` 的 `concealed` 是 **13−3×副露数 张**（少了报错）；这两个数字搞反是踩过的坑。
    - 新增路由时：页面/图片/manifest 类**免口令**，`/api/*` 必须走 `_check_token()`。
    - **改完 JS 要真机验一遍**（`python mahjong_api.py --host 0.0.0.0 --token 1234` + 浏览器点几下）：
      Python 测试只能盯页面文本特征，点不点得动、番数对不对只有浏览器能证明——
      v2.4.0 的 4 个缺陷全部是这么发现的（建议测试时至少走一遍「13 张→看听牌→点第 14 张→看总番」）。
14. **新增设置项必须三处成对**（同第 5 条）；API 相关设置目前有 `api_port` / `api_auto_start` /
    `api_lan` / `api_token` 四个，改了一个要四个一起看（尤其是开关服务时要先 `_api_stop` 再 `_api_start`）。
15. **选项区布局约定（v2.4.1 起）**：算番页选项区固定**四行**，依次为 ①复选框（自摸/和绝张/抢杠和·杠上开花/
    海底捞月·妙手回春）②圈风（东风圈…北风圈）③风位（风位标签 + 东风位…北风位）④花牌（`花牌:` + 8 张图 + 张数）。
    **调整布局时只许动 `QHBoxLayout` 的组装，不许改控件属性名**（`cb_tsumo` / `cb_last_tile` / `cb_special_a` /
    `cb_special_b` / `wind_round_group` / `cb_round` / `wind_seat_group` / `cb_seat` / `btn_flowers` /
    `lbl_flower_count`）——它们被 `_refresh_all` / `_apply_settings` / `_options` 和三套测试引用，
    改名会同时弄坏「老设置恢复」和「测试」。改完界面**必须跑 `shot_ui.py` 看 `_scaffold\shots\ui_*.png`**
    （Python 测不了像素；但可以用离屏渲染后读控件坐标验证是几行，见 v2.4.1 小节的做法）。
16. **牌池「牌间距」约定（v2.4.2 起）**：`TileButton` 是 `setFixedSize` 的**固定尺寸**，牌间距就是
    `pool.setHorizontalSpacing(4)`。**必须给一个空列挂拉伸**（`pool.setColumnStretch(9, 1)`），
    否则窗口一变宽，Qt 会把 9 列均分撑开，牌与牌之间出现大片空隙（用户反馈过）。
    要调间距就改 `setHorizontalSpacing`，**不要靠列拉伸去调间距**；同理，以后在别处把固定尺寸的牌
    铺进 `QGridLayout`，也要记得给最后一列（或最后一行）留一个「吃剩余尺寸」的空位。
17. **窗口尺寸与几何迁移（v2.4.3 起）**：默认 **680×940**、最小 **600×560**（常量
    `WIN_W/WIN_H/MIN_W/MIN_H`）；依据是牌池 9 列×58 ≈ 522 + 边距，**不要再放大默认值**。
    设置里存 `geometry` + `geo_ver`；`_apply_settings` 里 `geo_ver < GEO_VER(2)` → 宽度**一次性**
    收紧到 `WIN_W`（只收宽度，高度/位置保留）。以后要再改默认尺寸，就把 `GEO_VER` +1，
    旧设置才会被再次收紧（否则用户已经存过 geo_ver=2，改默认值对他无效）。
18. **完整检验（不抽样）的用法**（v2.4.3）：`_scaffold\verify_all.py` 不带参数就是**完整检验**
    （牌型空间 2842 万手全穷尽，逐手校验）；`--quick` / `--sample` 是旧的抽样快速版。
    - 默认阶段 `main,waits,score,neg,meld,golden`（不含 `sweep`，它约为 main 的 5 倍耗时）；
      要 100% 全维度就 `--phases all`。
    - 耗时长（4 核约数十小时）→ **可随时 Ctrl+C，重跑同一条命令会自动续跑**（进度文件
      `_verify_progress_<阶段>.json`；重头跑加 `--restart`）。
    - 族顺序是**稀有族优先**（组合龙→特殊牌型→七对含四张 k=3/k=2/k=1→七对→标准型），
      所以先看稀有牌型结论只需几分钟到几小时。
    - 分族跑：`--families "组合龙,特殊,k=3"`。
    - **跨实现对拍只能比「两边都实现的口径」**：indy 不实现「终局不计表补充项」与「不求人
      抑制自摸」，这两项不要拿去对拍（否则每批都刷假差异）—— 见 `_norm_ind()`。
    - 改算番逻辑后的**必跑顺序**：`test_engine.py` → `smoke_test.py` → `error_audit.py`（29 注入）
      → `verify_all.py --quick`（快）→ 需要定论时再跑完整检验。
19. **「重置」必须是全部复位**（v2.4.4，用户点过名）：桌面版 `on_reset()` 要清
    手牌 / 副露 / 和张 / 待选 / 模式 / **花牌** / **四个复选框** / **圈风风位**，并
    `_autosave_settings()` 立即落盘；Web 端 `resetAll()` 同理。
    **新增任何状态（新控件、新状态变量）时必须同步加进这两处重置**，否则用户又会遇到
    「点了重置还得自己复位」。`smoke_test.py` 第 18 节有 19 项断言盯着这件事。
20. **Web 端布局必须跟桌面版一致**（v2.4.4，用户点名）：顺序＝
    ① 牌选择区（索/筒/万/字牌 **4 行**）② 模式行（含重置）③ **选项区四行**
    （□选项 / 风圈 / 风位 / 花牌 8 张图 + 「N 张」）④ 已选牌 ⑤ 听牌候选 ⑥ 算番结果。
    **花牌不在牌池里**（桌面版就放选项区第四行）。改 Web 布局后 `api_smoke_test.py` 有
    10 项布局断言会挡住，同时要**真机浏览器看一眼**（Python 只能查源码特征）。
21. **Web 端样式大量用「祖先类选择器」，重排 HTML 别把类名弄丢**（v2.4.5 踩过）：
    `.pool .line`、`.tiles img`、`.row .lb` 都要祖先带类名才生效。v2.4.4 重排时牌池容器漏了
    `class="pool"`，结果**每张牌各占一行**（块级 flex 竖着排）——用户一眼就看出来了。
    所以：① 重排 HTML 后**逐块对照 CSS 需要的类名**；② 关键布局样式**同时用 `#id` 写一份兜底**
    （现已是 `#pool .line,.pool .line{display:flex…}`）；③ 改完必真机看（拿 DOM 几何量
    「每行张数 / 同行 top 是否相同」比等用户发现快）。
22. **Web 端一切交互都要「单指可完成」，别依赖右键/悬停**（v2.4.6，用户点名「手机没有右键」）：
    减牌不能只给「长按 / 右键」——现在是**点红色角标 −1** ＋ **「已选牌→立牌」点一张减一张**；
    角标上的 `mousedown/mouseup/click/touchstart/touchend/contextmenu` 都要 `stopPropagation`
    （否则会冒泡成「加一张」）；同时 `-webkit-touch-callout:none` + `user-select:none`
    避免长按弹系统菜单。**新增 Web 交互时先问一句：手机上单手能不能完成？**
23. **程序图标要「三处齐全」**（v2.4.7）：① exe **文件自身**的图标靠打包参数
    `--windows-icon-from-ico=icon.ico`；② 运行时**窗口/任务栏**图标靠程序自己 `app_icon()`
    读 **exe 同目录**的 `icon.ico`（所以必须 `--include-data-files=icon.ico=icon.ico` 打进 dist，
    否则「exe 有图标但窗口/任务栏没有」）；③ `main()`（app 级）与 `MahjongFanWindow.__init__`
    （窗口级）**两处 `setWindowIcon` 都要写**。图标读不到时必须**安静降级为空 QIcon**，
    决不能因为它让程序起不来。改图标外观就改 `_scaffold\make_icon.py` 后重跑（生成的 `icon.ico`
    要一起提交）；验证 = `smoke_test.py` §19（离屏断言）+ `probe_icon_live.py`（真机看 Win32 句柄）。
24. **测试别用默认端口 8718，会「撞」出假失败**（v2.4.7 踩过）：Windows 的 `SO_REUSEADDR`
    （Python `HTTPServer.allow_reuse_address = 1`）**允许同一端口被重复绑定** —— 机器上若另有
    真实运行的服务也听着 8718，我们的服务照样能启动成功，但**请求会被随机投给其中一台**，
    现象就是「无 token 401 通过、带着正确口令却仍 401」。测试里用 `free_port()` 取**空闲随机端口**，
    断言加**最多 3 次重试**并在失败时打印窗口侧/服务侧 token（便于定位）；**别用重试掩盖真失败**。
25. **免口令白名单（v2.5.0，用户点名「分别允许哪些不使用密码也可以使用，包括 web 客户端」）**：
    - 分组表在 `mahjong_api.ANON_GROUPS`，**只有两个地方能改行为**：`Handler._anon_ok()`（匹配）
      与 `ApiServer(..., anon=…)`（注入）。GUI 侧菜单「工具 → 免口令访问设置…」→ `AnonAccessDialog`。
    - **`anon=None` 表示「用默认 ANON_DEFAULT（＝旧行为）」，`anon=()` 才是「全部都口令」**——
      千万别把默认值改成 `()`，否则老调用方/老测试一夜间全变 401（v2.5.0 试过两轮）。
    - **匹配规则**：项以 `*` 结尾＝**前缀**（如 `/tiles/*`），否则**精确相等**。
      `"/"` 必须当精确项：写成前缀会命中一切路径，免口令就形同虚设（v2.5.0 踩过，测试当场抓到）。
    - 「关掉 Web 客户端」后，页面要用 `http://IP:端口/?token=口令` 打开；页面里的牌面图靠
      `q()` 自动带上口令（`img()/imgClk()` 已改），所以**图不会裂**。
    - 新增设置项必须三处成对：`_collect_settings()` / `_apply_settings()` / 改完 `_autosave_settings()`；
      **老设置缺 `api_anon` 键时回默认**，不要让它变成空（否则升级后老用户突然全要口令）。
26. **不要把菜单 action 从 `menuBar()` 里反查**（v2.5.0 踩过）：测试里写
    `win.menuBar().actions()[1].menu().actions()` 会撞到已失效的 QMenu 包装，报
    `RuntimeError: Internal C++ object (PySide6.QtWidgets.QMenu) already deleted`。
    菜单 action 一律**存成窗口属性**（`self.act_api` / `self.act_api_lan` / `self.act_api_anon` /
    `self.act_api_token`）再引用。
27. **Web 端副露的「画法」必须与桌面版逐字对齐**（v2.7.1，用户点名「web客户端的暗杠的显示不对，
    怎么跟 明杠 的一样了？」）：桌面版 `_meld_codes()`（约 1675 行）是**唯一口径** ——
    **暗杠 = 面·背·背·面**（中间两张用 `empty.png` 牌背）、明杠 / 碰 / 吃全正面、
    相邻副露之间留间隔（桌面版 `GAP` 哨兵 ⇒ Web 端 `<span class="meldgap">`）。
    Web 端由 `mahjong_api` 的 `meldHtml()`（600）渲染 `#m_melds`，`render()`（734）调用它。
    **踩坑教训**：出问题时数据层（`S.melds` 的 `kind/concealed`、`meldsBody()`、`countUsed()`）
    **一个字都没错**，纯粹是渲染层漏看 `concealed` —— 所以「Web 端显示不对」**先去看桌面版怎么画**，
    再逐组对照，别只盯数据。改完必真机量 DOM：暗杠 4 张里中间两张的 `src` 必须是 `/tiles/empty.png`，
    明杠 4 张必须全是正面（`api_smoke_test.py` 5 项断言 + 真机 Playwright 各跑一遍）。
28. **桌面版「不用右键也能减牌」（v2.7.2，用户点名「就好像 手机屏幕那种 没有右键也可以操作的样子」）**：
    - 三个入口：① 牌选择区**红角标**（`CountBadge`，345）点一下 = −1；② 「已选牌」区
      （`TileRow(clickable=True)` + `ClickTile`，372）**点某一张 = 取消这一张**：立牌 = 减一张、
      副露 = 撤销那一组、和张 = 取消和张、花牌行 = 取消那张花牌；③ 右键
      （`TileButton.rightClicked` / `on_tile_right_clicked`）**保留**给鼠标。
    - **角标点击必须 `event.accept()`**：它是牌按钮（`QToolButton`）的子控件，不 `accept`
      就会冒泡成「再加一张」（测试里专门断言「点角标后 3→2，不是变成 4」）。
    - 副露渲染改成 `_meld_items()`（1829）→ `(牌编码, 组号)`；**`clickable` 且 payload 非 None**
      才可点；**暗杠的牌背（`BACK_TILE`）同样可点**（点它撤销整组）。`_meld_codes()`（1847）保留为
      老接口（只取编码），**别改它的签名** —— 其它地方/测试还在用。
    - 新增「已选牌」区时，payload 一律用**牌编码或组号**（别传控件）；`_refresh_all`（1794）里四处
      `set_tiles(codes, payloads, tip)` 要一起改，**payloads 长度必须与 codes 相同**（否则那张牌不可点）。
    - 验证：`smoke_test.py` §21（22 项，`QTest.mouseClick` 发真实点击）+ 真机 `probe_click_remove.py`
      （10 项）—— **offscreen 过了也别省真机**（角标是子控件，层级/命中在真实窗口里才最有说服力）。

---

## 八、代码地图

### `Mahjong_Calculator.py`（v2.7.4，共 2024 行，行号按 VS Code/编辑器计数）

> ★ v2.7.3 起主程序文件叫 `Mahjong_Calculator.py`（英文名 = 文件名 = exe = 打包目录），
> 本文档之前各节里写的 `mahjong_gui.py` 指的是同一个文件。
> 行号按 **v2.7.3 实测值**（v2.7.4 只改了 `__version__` 字符串，行号未变）；旧版本行号已作废
> （v2.4.7 图标、v2.5.0 免口令、v2.6.0 加 CLI 分支与「Web 版」措辞、v2.7.2 加「点已选牌取消」控件、v2.7.3 改名）。
> 改完代码请按下面锚点重新核对（`grep` 最省事）。

| 行号 | 符号 | 说明 |
| --- | --- | --- |
| 1–117 | 文件 docstring + import | 交互约定（**含「不用右键也能减牌」v2.7.2**）、**英文名/文件名/产物目录＝`Mahjong_Calculator`（v2.7.3）**、**「给其它程序调用的 API ＝ mahjong_core」（v2.6.0）**、Web 版（只给浏览器，默认免密码）、设置约定、窗口尺寸、程序图标 |
| **119 / 120 / 121** | `__version__` / `APP_NAME` / **`APP_NAME_EN`** | 版本号唯一来源 / **中文名（界面显示，不变）** / ★ v2.7.3 英文名＝文件名＝exe＝打包目录 |
| 136–166 | 常量区 | `POOL_SIZE` 136 / **窗口常量 144** / `FLOWER_CODES` 150 / **`ANON_ITEMS` 154 · `ANON_KEYS` 163 · `ANON_DEFAULT` 164** |
| 173 / 180 / 184 | `app_dir` / `resource_path` / `settings_path` | 路径（兼容 `sys.frozen`，打包后取 exe 同目录） |
| **191** | **`app_icon()`** | 程序图标：找 exe 同目录 `icon.ico`（兼容 `data\icon.ico`），读不到就安静返回空 QIcon，带缓存 |
| 215 / 222 / 231 | `_atomic_write_json` / `load_settings` / `save_settings` | 设置读写（原子写入） |
| ≈261 / 273 | 引擎导入（**含 `cli_main`、`cli_wanted`**）/ 兜底桩 `lan_ip_()`、`DEFAULT_API_PORT` | 缺文件也能跑；端口默认 8718 |
| 284 / 292 / 311 / 316 | `_tile_file` / `tile_pixmap` / `tile_icon` / `tile_label` | 牌面图加载与缓存（只读牌） |
| 335 | `clear_layout` | **安全清空布局** |
| **351 / 378** | **`class CountBadge`** / **`class ClickTile`** | ★ v2.7.2：**可点的红角标**（点一下 −1）/ **可点的牌**（点一下＝取消这张） |
| 410 / 477 | `class TileButton` / `class TileRow` | 牌控件；`TileRow` 支持 `clickable` + `set_tiles(codes, payloads, tip)` |
| **525 / 619** | `HELP_TEXT` / `ABOUT_TEXT` | 用法页（v2.7.2「不用右键也能减牌」、v2.7.3 exe 名）/ 关于（含英文名） |
| **631** | **`class AnonAccessDialog(QDialog)`** | 免口令访问设置（`__init__` / `_set_all` / `selected`） |
| **693** | `class MahjongFanWindow(QMainWindow)` | 主窗口（窗口标题 `国标麻将算番器 Mahjong_Calculator v…`） |
| 735 / **777** | `_build_ui` / `_build_calc_page` | 三页 `QStackedWidget` + 底部导航 / 算番页（选项区四行、牌池拉伸、**四个可点牌行 897–942 附近**） |
| 1034 / 1065 / 1138 | `_section_title` / `_build_fan_page` / `_build_help_page` | 小工具 / 番表页 / 用法页 |
| **1147** | `_build_menu` | 菜单栏（**「启动 Web 版」等；`self.act_api` / `act_api_lan` / `act_api_token` / `act_api_anon` 都在这里存成属性**） |
| 1200 / 1204 | `_goto_page` / `show_about` | 切页、关于 |
| **1215–1363** | Web 版段 | `_api_running` 1215 / `_api_host` 1218 / `_api_gen_token` 1223（**备用，v2.6.0 起不再自动生成口令**）/ `_api_watch_url` 1231 / **`_api_start` 1239（传 `token` + `anon`）** / `_api_stop` 1279 / `_api_toggle` 1297 / **`_api_show_info` 1305（写明「程序请用 mahjong_core」）** / **`_api_lan_toggle` 1345（不再自动生成口令）** / `_api_set_token` 1363 |
| 1379 / **1388** / 1405 | `_anon_text` / `_api_set_anon` / `_api_set_port` | 免口令文案 / 免口令设置 / 端口设置 |
| 1423 / 1446 / 1511 | `_collect_settings` / `_apply_settings` / `_connect_autosave` | **设置三处**（`geo_ver<2` 的宽度收紧、**老设置没 `api_anon` 时回默认**都在 `_apply_settings`） |
| 1522 / 1527 / 1533 / **1536** | `_autosave_settings` / `_save_settings_now` / `_load_settings` / `closeEvent` | 实时自动保存链路（关闭前先停 Web 版服务） |
| 1547 / 1555 / 1585 | `_wind_round` / `_options` / `_need_concealed` | 圈风风位 / 组装 `Options` / 状态换算 |
| 1596 / 1617 | `_on_option_changed` / `on_flower_clicked` | 选项一改就重算 / 花牌 |
| **1631** | `on_reset` | ★ **全部复位**（手牌/副露/和张/待选/模式/花牌/四个复选框/圈风风位 + 落盘） |
| 1663 / **1682 / 1695 / 1704 / 1720** | `on_tile_right_clicked` / **`on_hand_row_clicked` / `on_win_row_clicked` / `on_meld_row_clicked` / `on_flower_row_clicked`** | 右键减牌（保留） / ★ v2.7.2 **点已选牌取消**：立牌 −1 / 取消和张 / 撤销那一组副露 / 取消那张花牌 |
| 1725 / 1753 | `on_tile_clicked` / `_finish_meld` | 选牌 / 副露 |
| **1801** | `_refresh_all` | 统一刷新（**四个牌行都带 payloads + 点击提示**） |
| **1836 / 1854** | **`_meld_items`** / `_meld_codes` | 副露项 `(牌编码, 组号)` / 只取编码（老接口，等于 `[c for c, _ in _meld_items()]`） |
| 1858 / 1921 | `_update_wait_area` / `set_win_tile` | 听牌候选 / 和张 |
| 1931 / 1988 | `_update_result` / `_guess_win_tile` | 结果显示 / 自动判定和张 |
| **2002** | `main` | 入口：**先 `cli_wanted(sys.argv[1:])` 判断走命令行（不开界面）**，否则 `QApplication` + 窗口（`setApplicationName(APP_NAME_EN)` / `setApplicationDisplayName(APP_NAME)`） |

### `mahjong_api.py`（v2.7.4，共 1350 行）—— Web 版（浏览器算番）服务端 + 页面内部 JSON 路由

| 行号 | 符号 | 说明 |
| --- | --- | --- |
| 1–96 | 模块 docstring | 分清「给程序用的 API 是 `mahjong_core`，本文件只为浏览器服务」；接口一览、牌张编码、张数口径、Web 布局约定、免口令白名单 |
| 101 / 112 | `class ApiError` / `class Engine` | 错误类型（带 `status`）/ 引擎包装 |
| 313 / **320 / 330** | `TILE_FILE_RE` / **`ANON_GROUPS` / `ANON_DEFAULT`** | 牌面图白名单（含 `empty.png`）/ 免口令分组表（`*` 结尾＝前缀，其余精确相等）/ 默认 `("web","debug")` |
| **332–902** | `WEB_CLIENT` | Web 客户端整页（内联 CSS+JS） |
| **520 / 531** | `q()` / `el()` | 口令拼 URL / 取元素 |
| **536 / 542 / 553 / 554 / 584** | **`LAYOUT_KEY` / `setLayout` / `toggleLayout` / `autoSize` / `autoSizeLater`** | ★ v2.7.0 两套布局：`mj_layout` 记忆、切换、按屏幕算尺寸、防抖重算 |
| **589 / 591** | **`img()` / `imgClk()`** | 牌面图（地址自带口令；已选牌可点减） |
| **593 / 600** | `mkTiles` / **`meldHtml()`** | 通用铺牌 / ★ v2.7.1 **副露渲染：暗杠→面·背·背·面（`empty.png`）、明杠/碰/吃全正面、副露间插 `.meldgap`（CSS 369）** |
| 700 / 707 / **734** | `resetAll`（全部复位）/ `minus` / `render` | 重置 / 减一张 / 重绘（**v2.7.1：734/735 用 `meldHtml()` 画副露**） |
| 764 / 780 / 789 | `buildFlowers` / `buildPool` / `bindMinus` | 花牌行与牌池只建一次；角标 −1 事件绑定 |
| 879 / 881 | `saveToken` / `loadToken` | 口令存 `localStorage` / 从 `?token=` 初始化 |
| 903 / 948 | `DEBUG_HTML` / `MANIFEST` | 接口自测页 / PWA 清单 |
| 959 | `_tile_image_bytes` | 读《麻将图》牌面图（非法名 404；`empty.png` 允许） |
| **976** | **`CLIENT_GONE_ERRORS`** | ★ v2.7.4 统一判定「客户端提前断开」：`ConnectionResetError` / `ConnectionAbortedError` / `BrokenPipeError` / `TimeoutError` |
| **980 / 997** | `class Handler` / **`handle_one_request()`** | 路由基类（注入 `engine`/`token`/`anon`）/ ★ v2.7.4 **吞掉客户端断开异常（手机刷新不再刷 traceback）** |
| **1026 / 1041** | **`_anon_ok`** / `_check_token` | 免口令匹配 / 口令校验 |
| **1110 / 1180** | `do_GET` / `do_POST` | 路由：口令检查在静态分支之前；静态与 `/api/*` 都按白名单决定要不要口令 |
| **1200 / 1210** | **`class QuietHTTPServer(ThreadingHTTPServer)`** / **`handle_error()`** | ★ v2.7.4 兜底静音：只对「客户端断开」不打印，**其余异常照旧完整打印**；`--verbose` 时给一行提示 |
| **1220 / 1256 / 1273** | `class ApiServer` / `start` / `stop` | 线程 HTTP 服务（用 `QuietHTTPServer`；`anon=None` 用默认、`anon=()` 全部要口令；`port=0` 可用） |
| 1287 / 1301 | `lan_ip` / `main` | 局域网 IP 提示 / 命令行入口（`--host/--port/--token/--anon/--verbose`） |

### `mahjong_api.py`（v2.4.0，共 1076 行）—— 本地 JSON API + Web 客户端

| 行号 | 符号 | 说明 |
| --- | --- | --- |
| 1–69 | 模块 docstring | 接口一览、牌张编码、请求体示例、**张数口径（含和张）** |
| 72 / 74 / 75 | `APP_NAME` / `DEFAULT_HOST` / `DEFAULT_PORT` | 名称、默认只绑 `127.0.0.1`、默认端口 8718 |
| **91** | `class Engine` | 引擎包装：`tiles_of` / `parse_melds` / `parse_concealed` / `parse_options` / `_check_counts` |
| 191 / **202** / 220 | `score_json` / **`score`** / `check_shape` | 结果序列化 / 算番 / **前置校验（400 而不是 0 番）** |
| 239 / 262 / 269 / 276 | `waits` / `tiles_table` / `fan_table` / `version` | 听牌（`only_reach` 控制是否筛起和分）/ 牌张表 / 81 番表 / 版本 |
| 286 | `HELP_TEXT` | 接口说明（就是模块 docstring） |
| **292 / 294** | `TILE_FILE_RE` / **`WEB_CLIENT`** | 牌面图白名单正则 / **Web 客户端整页（内联 CSS+JS，约 400 行）** |
| 703 / 748 | `DEBUG_HTML` / `MANIFEST` | 接口自测页 / PWA 清单 |
| 759 | `_tile_image_bytes` | 读《麻将图》（回退 `tiles\`）里的牌面图，非法文件名直接 None |
| **774** | `class Handler(BaseHTTPRequestHandler)` | HTTP/1.1，`protocol_version`，per-instance 注入 `engine`/`token` |
| **875** | `do_GET` | **路由**：`/`·`/web`·`/index.html`=Web 客户端；`/debug`；`/manifest.webmanifest`；`/favicon.ico`·`/apple-touch-icon.png`；`/tiles/<编码>.png`（**免口令**）→ 然后 `/api/*`（**要口令**） |
| 942 | `do_POST` | 写接口（`/api/score`、`/api/waits*`，**要口令**） |
| 962 / 995 / 1011 | `class ApiServer` / `start` / `stop` | 线程 HTTP 服务（`port_actual` 支持 `port=0`；`url` 把 `0.0.0.0` 显示回 `127.0.0.1`） |
| 1025 | `lan_ip` | 猜局域网 IP（仅供提示） |
| 1039 | `main` | 命令行启动：`--host/--port/--token/--verbose` + 横幅（含手机链接） |

### `mahjong_core.py`（v2.6.0，共 2000 行）

| 行号 | 符号 | 说明 |
| --- | --- | --- |
| 64–65 | `GREEN_TILES` / `PUSHABLE_TILES` | 绿一色（索2,3,4,6,8+发）、推不倒（按规则原文） |
| 92–140 | `suit_of`(92) / `num_of`(99) / `is_suit`(108) / `code_of`(116) / `tile_of`(120) / `name_of`(124) / `counts_of`(134) … | 牌张编码互转 |
| 152 / 185 / 211 / 233 | `Meld` / `Set_` / `Score` / `Options` | 副露、面子、结果、选项（均为 frozen dataclass） |
| 261 / 282 / 287 | `DEFAULT_FAN_TABLE` / `DEFAULT_EXCLUDES` / `fan_value_map` | 分值表与不计表（JSON 缺失时兜底） |
| 300–380 | `Decomp` / `walk_collect` / `_standard_splits` / `_long_dragon_patterns` / `LONG_PATTERNS` | 拆牌基础 |
| **383 / 440** | `decompositions` / `_is_bu_kao` | **牌型分解**（标准型 + 组合龙 + 七对/连七对/十三幺/全不靠/七星不靠；v2.1.0 修 `need>=0`，v2.2.0 修连七对跳花色，**v2.2.1 七对允许 4 张同牌**） |
| 459–519 | `THIRTEEN_IDS`(459) / `_split_ok`(462) / `_shape_win`(490) / `_special_win_quick`(503) / `_long_dragon_win`(519) | **听牌快速布尔判定**（v2.1.0 新增，v2.2.1 同步七对口径） |
| 537 | `Ctx` | 判定上下文（`kong_count` / `concealed_triplets` / `chow_groups` / `triplet_nums`…；`ctx.special` 用于特殊牌型分支） |
| 670–692 | `_all_tiles_in` / `_same_suit` / `_one_number_suit` / `_nums_within` / `_step_series` | 判定辅助 |
| 702–1010 | `fan_xxx(ctx)`（约 60 个） | **番种判定函数**（`fan_da_si_xi` … `fan_ping_he`；**v2.2.2：`fan_da_san_yuan` 遇 `ctx.special` 直接 False，`fan_qing_yao_jiu` / `fan_hun_yao_jiu` 接受 `七对`/`连七对`**） |
| 1018–1108 | `_count_si_gui_yi` / `_count_yi_ban_gao` / `_count_xi_xiang_feng` / `_count_lian_liu` / `_count_lao_shao_fu` / `_yao_jiu_covered` / `_count_yao_jiu_ke` / `_count_shuang_tong_ke` | 可多次成立番种的实例计数（`_count_si_gui_yi` 对特殊牌型直接返回 0） |
| 1115→**1127** | `wait_shape` | 边张 / 坎张 / 单钓将 |
| **1155 / 1204 / 1207** | `EXTRA_EXCLUDES` / `EXCLUDE_KEEP` / `EXCLUDE_DROP` | 补充「不计」表（29 条）、保护项、反向保留项 |
| **1210** | `collect_fans` | **番种汇总**（含杠的计分） |
| **1439** | `class MahjongFanCalculator` | 引擎入口（`__init__` 1448 / `_load_rules` 1466 / `handle_exclusions` 1499 / `waiting_tiles` 1518 / **`score` 1541** / `winning_tiles` 1613 / 旧接口 `calculate_fan` 1626·`is_winning_hand` 1646·`get_possible_winning_tiles` 1652 / `fan_table` 1662） |

### `mahjong_core.py` 命令行（v2.6.0 新增；无 Qt、无网络、无端口）

| 行号 | 符号 | 说明 |
| --- | --- | --- |
| **1694** | `CLI_FLAGS` | 命令行参数名单 —— `Mahjong_Calculator.cli_wanted()` 靠它判断「要不要开界面」 |
| 1698 / 1699 | `_CLI_SUIT_BASE` / `_CN_TO_CODE` | 简写花色基数（m/s/p）/ 中文名→代码 |
| **1702 / 1710 / 1742** | `cli_wanted` / `_cli_norm_tiles` / `_cli_parse_melds` | 判断 / 牌串解析（`W1`、`一万`、`11123456789999m`）/ 副露解析（chi/peng/kong/angang） |
| **1774 / 1794 / 1824 / 1837** | `score_hand` / `score_codes` / `_score_to_dict` / `_score_text` | 一行算番（给 import 用）/ 核心实现 / JSON 化 / 文本化 |
| **1855** | `cli_main` | 命令行主流程（默认 JSON + `ensure_ascii=True`；退出码 **0 成功 / 1 不能和牌 / 2 参数错**） |

### `国标麻将标准规则.json`（v1.0，顶层键）

`_说明` · `meta` · `牌具与器材` · `术语` · `和牌规定` · `比赛程序` · `番种体系` · **`番种`**（88/64/48/32/24/16/12/8/6/4/2/1 分 + 花牌，共 81 条，字段 `序号/name/fan/definition/不计/补充`） · `计分` · `成绩与名次` · `罚则` · `申诉与仲裁` · `裁判工作` · `守则` · `附表与附件` · `历史与由来` · **`问答`**（166 条） · `程序实现要点`

---

## 九、测试与验收

| 脚本 | 内容 | 当前结果 |
| --- | --- | --- |
| `_scaffold\test_engine.py` | 引擎：九莲宝灯/七对/**含四张的七对**/**连七对（含跨花色反例）**/十三幺/七星不靠/组合龙/大四喜字一色/碰碰和双同刻/平和断幺/明杠暗杠/听牌分析/起和门槛/自摸互斥/花牌口径/旧接口/非法输入/番表 | **73 项全通过** |
| **`_scaffold\cli_test.py`**（v2.6.0） | **「不要 HTTP / 端口」的算番入口**：`mahjong_core.py` 命令行（106 番/中文与简写写法/省略 --win/听牌 9 张/副露听西 26 番/--tsumo/--version/--fan-table/--help/退出码 1 与 2/输出是纯 ASCII）+ **主程序（=exe）带参数不启动界面（1.7s 结束）** + `import` 与 `score_hand()` + 「core 里没有任何 socket/HTTPServer 代码」 | **34 项全通过** |
| **`_scaffold\smoke_test.py`** | 界面：结构、复选框动态文案、选牌与减牌、听牌候选与算番、碰/明杠/暗杠/吃、**暗杠面背面渲染**、设计图那手牌、重置、风圈风位、**花牌按钮与恢复**、设置自动保存与恢复、番表/用法页、引擎一致性、**Web 版菜单启停与端口持久化**、**§17b 局域网（★ v2.6.0：不再自动生成口令 + 免口令直连 200 + 设口令仍 401/带 token 200）**、**§18 重置=全部复位**、**§19 程序图标**、**§20 免口令访问设置**、**§21 点已选的牌取消（v2.7.2，22 项，用 `QTest.mouseClick` 发真实点击：角标 −1 不误加、立牌−1、点暗杠牌背撤销整组、取消和张、取消单张花牌、空态/越界不崩）** | **221 项全通过** |
| **`_scaffold\api_smoke_test.py`** | **Web 版服务 + Web 客户端**：真实起服务 + 真实 HTTP。21 个接口行为 + Web 页面特征 + 静态资源（`/debug`、`/manifest.webmanifest`、`/tiles/*.png`、非法名/路径穿越、免口令 vs 口令）+ **张数口径回归 4 项** + 4 条页面 JS 白盒回归 + **15 项布局/触摸断言（v2.4.4–v2.4.6）** + **【7b】31 项免口令白名单断言（v2.5.0）** + **10 项「经典/自适应」布局断言（v2.7.0）** + **5 项副露/暗杠渲染断言（v2.7.1）** + **【7c】6 项「客户端提前断开不刷屏」（v2.7.4：真发 4 次 RST + 捕获 stderr，断言无 Exception occurred / traceback，且服务照常可用）** | **138 项全通过** |
| **`_scaffold\verify_all.py`** | **全量枚举 + 算番校验**（**v2.4.3：默认就是「完整检验（不抽样）」**，牌型空间 100% 穷尽 ≈ **2842 万手**：标准型全量 + 组合龙 11,016 + 特殊 742 + **七对全空间 C(34,7)=5,379,616** + **七对含四张 k=3/k=2/k=1 = 185,504 / 2,782,560 / 8,069,424**；逐手：和牌判定 / 分解集合 / 特殊 tag / 逐分解逐番对拍 / 取优 / 并存违规 / 无故丢番 / 番值；另有 `waits`（逐手听牌对拍）、`score`（逐手公开接口）、`sweep`（每一手的每一个和张 × 点炮/自摸）、`neg`（逐手 2 个确定性变异）、`meld`、`golden` 阶段；**分批 + 进度文件，可中断续跑**） | 见 `_verify_out.txt`；4 核吞吐：main≈123 手/秒/进程、waits≈173、score≈126 |
| 同上（抽样快速版） | `--quick` / `--sample`：保留旧路径（标准型抽 8 万 + 分层对拍 1.2 万 + 和张扫描 300 手） | 改代码后先跑这个（约 2 分钟） |
| **`_scaffold\error_audit.py`** | **错误类别全枚举 + 故障注入**：25 类「可能出错的方式」，逐类注入一个真实缺陷（**29 个**），要求被 D1~D10 检测器抓到；含「七对 4 张同牌」口径影响量测算与口径回归防护 | **基线 0 问题 / 29 个注入 29/29 被拓到 / 盲区 0** |
| `_scaffold\indy.py` | **第二套独立实现**（自己的编码/分解/81 番种判定/不计闭包），供 `verify_all.py` 与 `error_audit.py` 对拍 | 工具 |
| `_scaffold\golden_fans.py` | **81 个番种的金标准牌例**（一手一定命中） | 工具 |
| `_scaffold\shot_ui.py` | 真实平台截图 → `_scaffold\shots\ui_*.png`（人工肉眼验收用） | **7 张**（含 `ui_7_flowers_fantable.png`：点花牌 + 番表）；v2.4.1 用于验收「选项区四行」，v2.4.2 用于验收「牌池间距」 |
| **离屏坐标自检**（临时片段，不入库） | 建窗口 → `show()` → `processEvents()` → 打印控件 `mapTo(window, rect().center())`：验证「选项区是四行」（y 有 **4 个不同值**，v2.4.1 = 399/424/449/488）与「牌是 4px 间距」（v2.4.2 索行相邻 x 差恒为 **58** = 牌宽 54 + 4，索/筒/万三行对齐） | 人工确认 |
| `_scaffold\probe_icon_live.py` | **真机图标探针**（v2.4.7）：真实平台开窗后用 Win32 `WM_GETICON` / `GWL_HICON` 看系统是否真的拿到图标句柄 | 实测 big/small/class 三个句柄均非 0 ⇒ 任务栏/标题栏确实有图标 |
| **`_scaffold\probe_click_remove.py`**（v2.7.2） | **真机点击探针**：真实平台开窗后，用 `QTest.mouseClick` 再点一遍「红角标 / 立牌一张 / 副露一张（暗杠牌背）/ 和张 / 花牌行」——红角标是牌按钮的子控件，真机才能证明「点角标不反过来加一张」 | **10 项全通过**（`_probe_click_out.txt`） |
| `_scaffold\make_icon.py` | **生成 `icon.ico`**（v2.4.7）：红中牌样式，7 张尺寸各自绘制；手写 ICO 容器（PNG 条目）；顺带导出 `icon_preview.png` | 7 张尺寸 Qt 全部可读 |
| `_scaffold\smoke_test_v1_pyqt5.py` | v1.1.0（PyQt5）的旧冒烟测试 | 已归档 |
| `_scaffold\build_exe.py` | Nuitka 打包（由 `0_nuitka_pyside6_clang_auto.bat` 调用，处理中文路径的数据文件）；**v2.6.0 起 `--windows-console-mode=attach`**（双击纯 GUI，命令行调用时 stdout 可见）；**v2.7.3：入口 `Mahjong_Calculator.py` → 产物 `build_output\Mahjong_Calculator\` + `Mahjong_Calculator.exe`**（编完自动把 `.dist` 改名；旧目录保留为 `…_旧编译_时间戳`） | 备用 |

运行方式（PowerShell 需用 `&` 调用，且**不要依赖 `cd`**——终端工具可能把开头的 `cd` 简化掉，
用绝对路径最稳）：

```powershell
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\test_engine.py"
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\smoke_test.py"
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\api_smoke_test.py"
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\shot_ui.py"
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\error_audit.py"
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\make_icon.py"          # 重新生成 icon.ico（v2.4.7）
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\probe_icon_live.py"    # 真机看窗口/任务栏图标（v2.4.7）
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\cli_test.py"           # 命令行 / import 算番入口（v2.6.0）
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\_scaffold\probe_click_remove.py"   # 真机验证「点已选的牌取消」（v2.7.2）
```

**Web 客户端的人工验收（Python 测不了点击，只能真机看）**

```powershell
# 开一个开着局域网的临时服务（口令 1234），然后用浏览器/手机打开：
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\mahjong_api.py" --host 0.0.0.0 --port 8718 --token 1234
# 本机： http://127.0.0.1:8718/?token=1234      手机： http://<上面提示的局域网 IP>:8718/?token=1234
# 验证免口令白名单（v2.5.0）：--anon none = 全部要口令；--anon web,info = 只放行这两组
#   关掉 web 后再用 ?token= 打开，页面里的牌面图会自带 token（图不会裂）
& "D:/Program Files/Python310/python.exe" -u "G:\Python_Code\Mahjong_Calculator\mahjong_api.py" --port 8721 --token 1234 --anon none
```

验收动作（每一条都要真点一遍）：① 点 13 张→出听牌候选（每张带番数）；② 点第 14 张→出总番 + 番种；
③ 长按/右键一张牌→能减；④ 点花牌→花牌行出现且总番 +1；⑤ 切成「碰/明杠/暗杠/吃」→能成组；
⑥ 改圈风/风位/选项→番数跟着变；⑦ 手机浏览器上同样能操作（按钮要够大、不报错）。

**免口令白名单的真机验收（v2.5.0）**：用 `--anon none`（全部要口令）起服务，浏览器开
`http://127.0.0.1:8721/?token=1234`（不带 token 应打不开）：
① 页面 42 张牌图**全部加载成功**（图片 URL 形如 `/tiles/T1.png?token=1234`）；
② 点牌池加一张 → 红色角标显示 2；③ 点角标 → 变 1；④ 不带 token 直接请求 `/api/health` 应 401。

**测试注意事项**

1. 界面测试须 `QT_QPA_PLATFORM=offscreen`（脚本内已 `setdefault`）；offscreen 下 `QMessageBox` 会
   永久阻塞，测试里必须替换成空实现。
2. 测试必须把 `Mahjong_Calculator.settings_path` 指到临时文件，**不要污染真实设置**。
3. 每次从干净状态开始：先删临时设置文件（否则上一次保存的「当前页/模式」会被恢复，导致截图串页）。
4. PowerShell 读 UTF-8 输出前先 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`。
5. **`error_audit.py` 的断言全部用「注入前快照 + 第二套独立实现」做参照**：
   若直接用被检查对象自身做断言，注入的缺陷会自己掩盖自己（这是 v2.2.0 踩过的坑）。
6. **开局域网/API 服务的测试别用固定端口 8718**（v2.4.7 踩过）：Windows 的 `SO_REUSEADDR`
   允许同一端口被重复绑定，机器上若另有真实服务也听着 8718，请求会被随机投给其中一台
   （表现为「带着正确口令却 401」）—— 用 `free_port()` 取空闲随机端口，断言加 2~3 次重试
   并在失败时打印窗口侧/服务侧 token，**重试只用来遮瞬时抖动，不能用来遮真 bug**。

---

## 十、后续待办（建议顺序）

1. 副露支持单独删除/修改（目前只能整手重置；**Web 端同理**）。
2. 番种「牌例」解释：算番结果里给出「为什么不计 X」的依据（规则 JSON 已含牌例要点）。
3. 组合龙等罕见牌型补充测试用例。
4. ~~口径确认：「七对能否含 4 张相同的牌」~~ → **v2.2.1 已裁定：国标允许（4 张算两对）**；
   仍可跑 `_scaffold\sweep_qidui4.py` 做逐族完整扇扫交叉验证。
5. 若以后新增番种/改判定，先跑 `error_audit.py`（它会把 29 个典型缺陷逐个注入并验证能被拓到）。
6. 打包脚本实机验证，并把 `国标麻将标准规则.json` + `麻将图\` 一起分发
   （`mahjong_api.py` 不需要额外数据文件，但**牌面图要带上**，否则 Web 端看不到牌）。
7. ~~若需要手机端~~ → **v2.4.0 已用 Web 客户端实现**（同一 WiFi 打开即用）；
   下一步可选：把 Web 页面做成可安装 PWA（图标/离线缓存已预留 `manifest.webmanifest`）、
   或给 Web 端加「牌谱导入/导出」与「算番历史」。
8. Web 端可选增强：副露右键删除、算番结果一键复制、多语言（已预留 `__APP__` 占位符）。
